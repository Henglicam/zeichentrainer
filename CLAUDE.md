# CLAUDE.md — 识字 Zeichentrainer

Working language: **English.** Reply to H in English. Short, direct, no excessive politeness.

## What this is
Chinese character trainer for adults (spaced repetition), a reinterpretation of 悟空识字 without the kids' aesthetic.
User: H, product manager, Beijing, **phone-only (Android/Xiaomi, Chrome, VPN), no computer**.
UI language: English. Learning content: Chinese + pinyin + English meaning.

## Live deployment
- Repo: `henglicam/zeichentrainer` · GitHub Pages, branch `main`, folder `/ (root)`.
- URL: `https://henglicam.github.io/zeichentrainer/`
- Push to `main` → Pages rebuilds automatically (~1–2 min). `index.html` must stay in the repo root.
- The site is **public** (free plan). User data lives exclusively on the device (IndexedDB), never in the repo; the only thing the app sends on its own is the daily anonymous usage row (v170, below), and that can be switched off.

## Current state (PWA v285, 2026-09-07)
- **Files:** `index.html` · `styles.css` · `lang.js` (the app's texts in other languages, v253) · `app.js` · `manifest.webmanifest` (with the `share_target` for screenshots) · `sw.js` · `privacy.html` (the Play Store's privacy page, not in the shell; since v170 it also describes the daily usage counts, since v173 the framed area to the AI) · `signs.json` (phrasebook) · `nmt-model.json` (translation-model manifest) · `icon-192/512/maskable-512.png` · `vendor/` (Tesseract + the simplified and traditional readers + dictionaries + OpenCC's character tables both ways + the stroke medians of 9,534 characters `strokes.txt.gz` 2.4 MB, ~16 MB, `vendor/nmt/` Bergamot engine 5 MB + zh→en model 50 MB, licenses in `vendor/LICENSES.txt`) · `.github/workflows/fetch-nmt-model.yml` · `SPEC-sign-cards.md` · `README.md` (German, the one-time hosting steps for the phone — not a feature description). Vanilla JS, no frameworks, no build step. `app.js` in reading order: helpers and state → IndexedDB → online AI → study screen → cards, detail, edit form → More → camera and inbox → reading pipeline (`readPass`, `readPassTra`/`traWorker`/`t2s`, `secondLook`, `readingScore`, `deskewBlob`, `textRowExtent`, `toBW`/`chromaCanvas`/`toChroma`/`inkHeight`) → character picker and drawing sheet → sign editor and save → service worker, mirror, shell check and the shared screenshots (`takeShared`). The usage counters and the owner's gate sit with More. Small shared helpers: `askSheet` (a question as the app's own sheet, v222), `putCard`, `pySpaced`, `eachLine`, `slineHTML`/`wireSlines`, `urlOf`, `fullPhoto`, `pageVersion`, `median`, `readingStatus`, `logErr`/`diagText`/`shareDiag`, `busyHTML`, `apiErrText` (the error text from a provider's or the relay's JSON body).
- **PWA:** installed and verified on the Xiaomi; runs offline (SW cache-first); `navigator.storage.persist()` granted. **Versioning:** SW cache `zt-vN` in `sw.js` and the label `PWA vN` in `index.html` (`.ver` — **hidden since v225**, H's "Go" on the design review: a developer's line, not a user's; it stays in the page because the shell check, the mirror update and About read it, and More → About shows "PWA vN." to the user) move in lockstep — bump both on **every** change to cached files. The app updates itself (update check on load/foreground, auto-reload on `controllerchange` — deferred to the next foreground while the app is in use since v279); the worker's install fetches the shell with `cache:"reload"` — GitHub Pages sends `max-age=600`, and after two deploys within ten minutes a new worker once filled its cache with the previous shell (the phone showed the v70 page with the v69 script: header v70, no drag in the sheet). **Boot self-check** (`shellCheck`): `APP_V` in `app.js` must equal the `PWA vN` label — **bump both, plus `zt-vN`, on every change**; if they differ, the page posts `refresh` to the worker, which refills its cache from the server (`cache:"reload"`) and the page reloads once (through `reloadSoon` since v279, see below; guard: at most once per 10 min, setting `shellFixAt`); it runs after registration and on the first `controllerchange`; the vendor cache `zt-ocr-v1` survives shell updates — bump it only when vendor files change.
- **The reload after an update waits until the app is idle (v279, H after eight builds in two hours: "nach Laden eines neuen Builds flackert die App … Passiert das nach jedem Update auch bei den Usern?" → the answer: one reload per update, two with a mixed shell; the flicker was the pace of the deploys → "Go" on the deferral):** until v278 the page reloaded the moment the new worker took over, in the middle of whatever H was doing. Now `reloadSoon()` decides: within `RELOAD_GRACE` 3 s of the load (the app was just opened, the first screen is up) or while the app is in the background the page reloads at once, as before; later `RELOAD_DUE` is set and the reload comes when the app next comes to the foreground (`visibilitychange`). The same for the shell check's "refreshed" reload and the hourly mirror check's "updated"; a tap on the mirror row's **Check now** still reloads at once (`MIRROR.forced`) — the user asked for it. The old page keeps running meanwhile with the new worker's cache, which is harmless (the vendor cache is separate, the page's own files are already loaded). Suite `verify-reload` (a synthetic `controllerchange` in the first seconds reloads at once, one after the grace leaves the page with `RELOAD_DUE`, a hide changes nothing, the return to the foreground reloads, an update while hidden reloads at once); the shell and english suites pass. **The mirror suite fails in this session on both v278 and v279** with "The mirror is not reachable: Failed to fetch" from the worker's fetch of the suite's fake mirror — a sandbox or harness problem, not the app (the shell suite, which uses the same reload path, passes). Not yet field-checked — the next update after v279 is the check: H should see no reload while using the app, and the new version on the next return to the foreground or the next open.
- **No VPN needed to use the app (H's requirement):** the installed app runs from its cache, DeepSeek is reachable from China, and updates also arrive through a **mirror** — More → Updates without a VPN (setting `mirror`, default jsDelivr `cdn.jsdelivr.net/gh/henglicam/zeichentrainer@main/`, **field-verified reachable from H's phone without VPN**). The page posts `mirror-update` (with its own version label) to the worker on start/foreground (at most hourly) or on "Check now"; the worker reads `PWA vN` from the mirror's index.html and, if newer, fetches the shell assets into the current cache — content types by extension, because jsDelivr hands HTML out as text/plain — and the page reloads (`mirrorUpdate` in `sw.js`, `mirrorCheck` in `app.js`). **Vendor files come from the mirror too** (H: "text recognition does not work without VPN" — the reader's ~12 MB are fetched on first use, and github.io was unreachable): the worker tries the origin, and on failure or a non-OK answer fetches `MIRROR + vendor/…` (all OCR/dictionary files are under jsDelivr's 20 MB cap; types by extension), caching the result in `zt-ocr-v1`; the page tells the worker the mirror setting on start (`tellMirror`). **A silent origin counts as a failure** (v189, H's phone without the VPN: "Reading the text … still at: loading the reader …" for good — github.io behind the wall answers with nothing, and the fallback waited for an error that never came): a vendor fetch from the origin gets `ORIGIN_WAIT` 6 s, then the mirror takes over, and after one such failure the mirror is asked first for `ORIGIN_DOWN` five minutes (`withTimeout`, `originDownUntil` in `sw.js`); measured: the first file 6 s, the rest milliseconds, the whole reader download 6.6 s against a silent origin. **The worker helps only while it controls the page:** H's diagnostics of the hanging reading said `SW no` with a desktop user agent (no "Mobile") — Chrome's "Desktop site" switch, ticked for the Bailian key page, was on for the app too, and a page without the worker fetches every vendor file straight from github.io. More → **Text recognition** shows how many of the ten reader files are on the phone and offers **Download** (`OCR_FILES`, `renderOcrRow`). The worker script itself only ever comes from github.io (it just carries the cache name). Still VPN-only: a first install if github.io is blocked, the 50 MB model download (jsDelivr caps files at 20 MB), the Claude provider. **VPN-off only: Qwen** (v173 — Bailian answered the phone without the VPN and refused through it).
- **Persistence:** IndexedDB `zeichentrainer` v3 — `progress` (keyPath `id`), `custom` (keyPath `id`, all cards; **the key is an id, not the text** — v118, H: the same character from another photo is a new card; `cardId(c)` = the text while it is free, else text + `#` + timestamp; cards and progress rows of v2 keep their text as the id, migrated in `onupgradeneeded`; every card is addressed by `id` — `cardOf`, the queue, `S.detail`, `S.editing`, `S.single`, `QSCARD`, `THUMB`, the list rows' `data-id` —, and `c` is only the text), `inbox` (keyPath `id`, photos), `settings` (keyPath `k`). No built-in deck; `deck()` is `S.custom`, sorted by creation time `at` (oldest first, so cards are learned in the order they were taken; the Cards list shows newest first). At boot, progress rows without a card are pruned.
- **Tabs:** Learn · Cards · Camera · More. The app opens on Learn. A tab tap always leaves the edit form; tapping **Cards** while a card is open returns to the list. **Rejected: a start screen** (v181–v182, H: "you either want to take a picture or use flash cards — a more user-friendly entry page" → two tiles, Learn and Take a photo, with a Home tab; then "two huge tiles, less text, more icon"; then H: "I'm not sure, maybe my idea was not good. Please find out if you find any good reference from other apps. If not, go back to the previous version" — no reference found: Anki opens on the deck list, Duolingo on the lesson path, Pleco on the dictionary, Google Translate on the translate screen with the camera one tap away, and Apple's guidance lands people in content, not a menu; reverted in v183 to the landing on Learn with four tabs. Do not bring the start screen back unasked.) **Rejected: the Camera tab as the shutter** (v184, H: "if I tap the camera icon I should immediately be using the camera" — the tab rendered the inbox page and clicked the camera input in the same gesture; reverted in v186, H: "I don't like the current camera implementation (direct capture), please revert" — the tab opens the inbox page with Take photo and From album. A raised tint circle for the Camera tab was proposed with it and not asked for. Do not bring the direct capture back unasked.)

- **More in the learner's order (v257, H: "Polish. Consider die Reihenfolge der Options unter More"; since v275 **Learning first** — H on the Progress dashboard: "das gehört ganz nach oben, an erste Stelle"):** Learning (Progress, Card order) · Share (Share the app, Feedback — the first section from v211 to v274, H's own call then: "place the share function more easy to find, for example on top") · Help · Language (Language, Meanings) · Online AI review (AI review with the owner's setup form, Review queue) · Your data (Export, Import, Flagged cards, Photos, Storage) · Privacy (Usage sharing) · Advanced settings (Admin log in with the password, or Logged in as admin with Log out) · **the owner's rows all together after the Unlocked row** — Downloads (Offline translation, Text recognition), Updates without a VPN (Mirror and its address), Diagnostics (Diagnostics, All users, Feedback), Start over (Reset) — · About. Until v256 the owner's rows were scattered between everyone's (Translation before the AI, Text recognition inside it, Mirror after Learning, Diagnostics before Advanced settings), Progress, Usage sharing and Photos sat under "On this phone", and Storage in the AI section. The rows with a long sentence and a wide button (Progress, Review queue, Meanings) carry the button under the text (`.fieldacts`, as the Feedback row's Send), since a button beside four lines squeezed the text into a narrow column. Checked by screenshot in English locked and unlocked and in German dark; the english, admin, feedback, users, order, shareapp, photostore, export, translate, relay and picture suites pass.
- **Every group of More ends in rounded corners, and the owner's inputs sit inside their rows (v282, H's screenshot of the Locked box: "warum sind die Ecken der weißen Boxen unten nicht rund?"):** two faults. The rounding rule for a group's last row was `.mrow:last-of-type`, and the section heads are divs like the rows, so only About's row — the page's last — ever got its bottom corners; every other group ended square (since More has had section heads). Now `.mrow:has(+.listhead)` rounds the row before every head too. And the password field stood as a `.field` under the Locked row, outside the white surface; it sits inside the row since — the input with its placeholder "Password" and Unlock on one line under the sentence (`.inrow`, the Feedback box's field look on `--card2`), "Wrong password." under it —, and the owner's Mirror address the same way with Check now beside it (until v281 a `.field` under the Mirror row). **The row is one quiet line since v283** (H: "Remove the description for the locked area, just call it admin log in … not very prominent"): **one line** since v284 (H: "polish") — the small grey label "Admin log in" (13 px), a lower password field (36 px, 15 px) and **Log in** as plain tint text (`.del`, no pill) side by side (`.inrow.admin`); v283 had the label above a full-height field and a soft button; no title and no sentence; the unlocked row reads "Logged in as admin" with **Log out** (its sentence stays, the owner's English); the keys "Locked", "Unlock" and the sentence left `lang.js`, "Admin log in" and "Log in" came in six languages; the admin suite's ids are unchanged. Measured in the harness: the Storage, Usage sharing, Locked and Mirror rows carry the 12 px bottom radius, a middle row none; the rows checked by screenshot in light and dark; the admin, english, order, shareapp, guide, feedback, users and progress suites pass. Not yet field-checked.
- **How to use the app (v259, H's to-do "a user guide (Gebrauchsanleitung)" for his friends — described first: in-app under More, six sections, text only; "Go"):** More → **Help** → "How to use the app" with the sentence "Six short sections: photo, characters, learning, cards, language, what stays on the phone." and **Open** (`#guide-open`, the section sits under Share); `S.mode="guide"`, `renderGuide` draws one white card (`.guide`, a `section` per topic with a hairline between, h2 17 px, p 16 px at line height 1.45) with ← Back and the badge "How to use the app"; the More tab stays lit, a tab tap leaves it, Back returns to More. The six sections (`GUIDE()`, eleven paragraphs, every text through `t()` in six languages): Take a photo (the frame the app draws, corners, the handle, the reading by itself, the tap outside the frame; Save now), Fix the characters (the strip's buttons, other readings, drawing, the line input, Select; pinyin and meaning follow, the AI check, the flag), Learn (due cards then up to eight new, the two taps and the speaker; the four grades, Pull the next cards forward), Cards (newest first, search, filters, the detail's Test/Edit/Delete, + New with a drawn character; tags and the Learn filter), Language and meanings (the switch, the AI's meaning in that language, Translate all cards, the pill), What stays on the phone (cards and photos only here — export now and then; what the AI check sends; the daily counts and the Privacy switch; Feedback). **The rule:** the guide describes what the app does today, nothing planned; a screen that changes takes the guide's sentence with it in the same PR. No screenshots in it (they would go stale). Alternative rejected in the description: a page beside `privacy.html` — VPN for a friend in China, outside the language switch, nothing without a network. Suite `verify-guide` (the row after Share, Open, the six headings and eleven paragraphs, no "OCR" and no "·" in the text, the lit More tab, Back, a tab tap, the German headings and a German sentence); the guide checked by screenshot in English, German dark and Japanese; the english, admin, shareapp, feedback and order suites pass.
- **Three from the improvement list (v266, H: "Go 3, 4, 5"):** **the text goes to DeepSeek when it can** (`textProvider()`: a picture-capable active provider — Qwen on H's phone, slow on text since v208 — hands the text check, the picker's candidates and Translate all to DeepSeek when a DeepSeek key or the relay is there; DeepSeek itself or a provider without pictures stays; the picture keeps `pictureProvider()`; `aiAsk` and `aiCharAlternatives` take key, model and base from it, the AI row's line says "On: DeepSeek (deepseek-chat) checks the text, Qwen (qwen3.7-plus) reads the framed area …"; the providers and picture suites expect it) · **the notices as the app's own sheet** (`noteSheet(title,text)` = `askSheet` with `cancel:false` and OK alone, the backdrop and Escape close it: the clipboard fallbacks, No flagged cards, the export and import errors, and the import's sentence under the title "Import" — no `alert(` is left; the ask suite expects no browser dialog at all and checks the notice; screenshot in light and dark) · **the empty deck points to the guide** ("New here? The guide explains the app in six short sections." and a link "How to use the app" under Take a photo, `#go-guide`, `.done .hint`; screenshot in English and German dark).
- **Undo after Delete (v268, idea 3 of the improvement list, H: "Go 3"):** the card's Delete in the detail and the Edit form and the inbox photo's Delete act **at once, no sheet**, and a line slides up above the tab bar — "Deleted “学”" (a card without text: "Card deleted"; a photo: "Photo deleted") with a tint **Undo** — for `UNDO_MS` 5 s (`showUndo`, `#undo`, `.undo` on the card's surface with the hairline and a shadow, `role="status"`); a second deletion meanwhile joins it ("Deleted 2 cards", "Deleted 2 cards and 1 photo", the timer restarts), the line stays through tab changes, and goes when the time is up (`hideUndo`). **Undo** (`undoDelete`) puts everything back: the card at its old place in `S.custom` with its progress row (`delCustom` keeps card, progress and index on the item, IndexedDB is written again, the deleted counter goes back down — `bump("deleted",-1)`), a card deleted from the study back returns as the next card of the session, the photo into the inbox at its place with the copy `keepPhoto` made onto its cards dropped again (`delShot(id,true)` from the inbox row only). The deletion itself is real from the first moment — a reload after five seconds shows the card gone. Photos → Delete N, the import and Reset keep their sheet. Suite `verify-ask` (no sheet, the line's text, button and role above the tab bar, card and progress gone from IndexedDB, Undo bringing both back at the old index, two deletions joined and both restored, the line gone after 5.4 s with the card still gone across a reload, the study-back deletion continuing the session and Undo showing the card again, the photo's deletion with the card's copy made and Undo dropping it again); the line checked by screenshot in light, dark, German and Japanese; the cards, edit, english, detail, photostore and export suites pass. Field-confirmed 2026-09-06 (H: "Both look fine").
- **Share a card (v269, idea 5 of the improvement list, H: "5"):** a **Share** button in the card detail's action row (`#d-share`, beside Delete card — the row is Test this card, Edit | Flag, Share | Delete card since; a waiting card without text has no Share and a full-width Delete) sends **one image** through the share sheet (`shareCard`, a PNG `zeichentrainer-card.png` with the card's text as the title and "坚果供应 — jiān guǒ gōng yìng — nut supply" as the text): `cardImage(d)` draws on a canvas at `SHARE_W` 1080 px, always in the light look — the crop in the front's 16:9 box with the rounded corners, the hairline and the blurred fill in the photo's colours behind it (a card without a picture gets the text alone), the characters in the Hanzi font fitted to the width (the traditional form when the card has one, the photo's lines as on the front, `frontLines`), the pinyin semibold in the tint, the meaning wrapped (`wrapText`, by word, by character when a word is wider than the line — a Japanese meaning), and "识字 Zeichentrainer" in grey at the foot. Nothing is written to the phone (hard constraint 6); without a share sheet the notice "Sharing is not available here." shows. Suite `verify-share` (the button's place beside Delete under Edit, light and dark; a word card's PNG at 1080 px with its title and text, a two-line sign card's taller image with the wrapped meaning, a traditional card's text, a card without a picture shorter, the waiting card without Share, the notice without a share sheet, the German label); the four images and the row in light, dark and German checked by screenshot; the detail, cards and english suites pass. Field-confirmed 2026-09-06 (H: "Both look fine"); whether WeChat keeps the text beside the image H has not said.
- **Dropped: a card's history (idea 4 of the improvement list, 2026-09-06, H: "Why would i want the history of a card?" → "Drop it"):** a screen with the card's creation, every review with its grade and the AI's answer would answer a question the app already answers — a card that keeps failing is flagged after four fails in a row, and the detail shows interval, ease, review count and the next date. Not built; do not bring it back unasked.
- **The app's errors in the daily row (v271, H: "Can the app send automatic error logs from other users?" — described first, "Go"):** `reportData()` carries `errors` — the last 20 entries of the error log (`reportErrors`: the minute, the kind and the message, cut to 200 characters, **any Chinese characters replaced by an ellipsis** through `noHan`, so the row keeps its "no card text" promise) — under the existing Usage sharing switch; nothing new for the user, no new table, no SQL change (`usage_latest()` hands the whole `data` back). More → All users shows a head row "with errors in the last 7 days N", in each phone's block "errors 2, last 2026-09-06 reading", and a section **"Errors on phones (N)"** at the end with every phone's messages ("Phone <id>, app version 271", then "2026-09-06 12:30 [reading] Reading failed: still at: loading the reader …" per line — these lines may wrap, like the device line). The Usage sharing sentence says "…, AI checks, and the app's error messages.", the guide's last paragraph "anonymous usage counts and the app's error messages", both in six languages; `privacy.html` names the messages and the removed characters (updated 6 September 2026). What it cannot do: a hang without an error (the silent reader without the VPN) leaves no trace — Diagnostics from the phone still tell those; a phone with the switch off sends nothing. Suite `verify-users` (the head row, the block line, the section with two phones' messages in order, the width rule exempting the message lines, `reportData().errors` after a logged error with its Chinese character stripped); the report's section checked by screenshot in light and dark, the Usage sharing row in German; the wechat, english, admin and guide suites pass; the static key check reports no missing key. Not yet field-checked — the first row with `errors` from H's phone is the check.
- **Progress as a dashboard (v274, H: "Mach den learning process report mit mehr useful und sexy für die user" — described first, "Go"):** More → Learning → Progress shows, instead of one long sentence, **four tiles** on the row's grey surface (Day streak, Cards learned, Due today, Reviews this week; `.ptiles` 2×2, the number 28 px bold), **the last 30 days** as a strip of 30 blocks in the tint shaded by the day's reviews (`.pdots`, four shades — the busiest day full tint, a single review the lightest; the streak's `days` list gives one review to days from before v274), **the deck as a bar** of New (grey) / Still learning (tint, an interval under `KNOWN_DAYS` 21) / Known (green, 21 days and more) with a legend, and the line "Coming up: N due tomorrow, N this week." (`progressData`, `progressHTML`; the tiles' numbers from `learnStats` and the progress rows). **Reviews per day** are counted since v274 in setting `daily` `{day: count}`, the last 60 days, bumped by every grade beside the streak's days list (`dailyBump`); nothing leaves the phone. The owner-ish counts (app opens, AI checks, the reader's and the models' work) left the row and stay in the shared usage report (`usageText`, Share report under the tiles). **Share report sends one image of the dashboard since v277** (v276 had put the dashboard's figures first in the text report — H: "Und beim Sharen bitte genauso" —, then: "You're still sharing too much! Only this please, sexy! The rest goes to the user reports"): `progressImage` draws the tiles, the 30-day strip, the deck bar with its legend and the week ahead on a canvas at 1080 px in the light look, like the shared card, with "识字 Zeichentrainer" and the day at the foot, and `shareProgress` hands the PNG `zeichentrainer-progress.png` to the share sheet with the title and "Day streak N, Cards learned N" as the text; without a share sheet the notice shows. The text report of v162–v276 (`usageText`: version, device, opens, cards, AI counts, the models' work) is gone — the owner's All users report carries those figures from the daily row. The progress suite checks the PNG's name, type, title, text and width and saves it for a look; the admin suite expects the PNG in place of the text file; the picture suite no longer reads the report. Ten labels in six languages ("Day streak", "Cards learned", "Due today", "Reviews this week", "Last 30 days", "Still learning", "Known", "Coming up: …"; "New" was there). The Learning section sits at the top of More since v275 (H: "das gehört ganz nach oben, an erste Stelle"), Share second; the shareapp and guide suites read the sections' order accordingly, and the card-order suite waits for the chip's re-render and for the setting's write before its reload — both had raced the heavier More render. Suite `verify-progress` (a seeded deck of 3 known, 2 learning and 20 new cards with five review days: the four tiles, 30 blocks with the shades in order, the bar's three flex values and the legend, the two lines, Share report kept, no opens or AI counts in the row, a grade counting the day in `daily` and the second grade too, the count across a reload, the German tiles, legend and line); the row checked by screenshot in light, dark and German; the admin, english, order and guide suites pass, the static key check reports no missing key. Not yet field-checked.
- **The app's language (v253, H's to-do "Multi language UI and translations" — "We don't need 中文, because 中文 is already the original of the characters. We need German, English, French and other very important languages that most commonly need to be translated from Chinese"; described first with six languages and the meanings from the AI, built on "Go, with the button"):** English, German, French, Spanish, Japanese and Korean. **How it works:** `lang.js` (loaded before `app.js`, in the shell list of `sw.js`) holds `LANGS`, `LANG_LOCALE`, `L10N` — one column per language, **English is the key**: `t("Save card")` returns the translation or the English text itself, so a missing string shows English, never a key; `{0}`, `{1}` are filled by `t()`; `nOf(n,"card")` takes the count words from the table too, and a translation may carry the number as `{0}` where the language puts it elsewhere ("カード{0}枚", "{0}-mal"). `LANG` is set at boot from setting `lang`, else from the phone's language (`langDefault`, the first two letters of `navigator.language`), before the first render; `applyLangStatic` writes the header's capsules and the tab labels, which sit in `index.html`, and `document.documentElement.lang`; everything else asks `t()` while rendering. More → **Language**: its own section under Learning with the sentence "The app's own texts. Cards keep their Chinese, pinyin and meaning." and a chip per language in its own name (English, Deutsch, Français, Español, 日本語, 한국어); a tap switches at once (`setLang`) and keeps the choice. **Done in v253 (the first of five PRs):** the groundwork, the header and tabs, Learn (the empty deck, the done screen, the front's pill and hints, the back's pinyin, meaning, parts row, linked row, flag box, AI box, the grades with their intervals, the study actions), Cards (the search, + New, the AI bar, the chips, the rows' pills and status, the empty texts), the card detail (the badge, the hints, the actions, the SRS line with the date in the language's locale, the Delete sheet) and the sheet's Cancel; the leech flag's note is written in the current language. The suites run in English (the harness's locale) and pass; German and Japanese checked by screenshot on the front, the back, the list, the detail, the sheet and the Language row; the detection (a German and a Japanese browser locale open in that language), the switch and the setting across a reload measured in the harness. **Done in v254 (PR 2):** the Camera tab — the lead line, Take photo and From album, the inbox's head and rows with their date in the language's locale, Crop, Delete and Cancel, the frame's hints (the draw-a-frame sentence, the proposal's and the frame search's bars, the frame's title), the preview with its sentences, Read now and Image only, the reading bar with Save now, the saved lines, the failure sentences (`failText`: "Reading failed: {0}" and "Frame too small" in the language, while the step texts under "still at" and in Diagnostics stay English), the editor (Characters, Pinyin, Meaning, the flag with its note, Tags with its placeholder — shared with the forms —, Save card, Ask AI, Cancel, the head lines, the source lines under the meaning through "Meaning {0}, unverified", the picture mark's title, `aiErrText`), the strips' hints, the Select row, the script switch, the picker (its head with "Replace {0} with:" / "Add a character {0}:", Remove, Not here? Draw it, Ask AI, + before / + after, the AI's answers), the drawing sheet (both heads, Undo, Clear, Done, the status lines) and the notes written onto a card (the Save-now flags, the failed re-crop, "the reading looks wrong"); the header's Done capsule has its own key `capsule:Done` with its English in an `en` column, since Japanese and Spanish want another word on the capsule than on the Done buttons (`t()` falls back to `en`, then to the key). Checked by screenshot in German and Japanese: the inbox, the proposed frame with its preview, the editor after a reading, the picker, the drawing sheet, the saved note. **Done in v255 (PR 3):** More for everyone — Share the app and Feedback with their statuses, Your data (Export with the backup note and the photos checkbox, Import, Flagged cards), Online AI review (the state line in its four forms, the What-is-sent sentence, the checkbox, the Review queue's sentences and button), Storage, Learning with the Card order chips (`LEARN_ORDERS` labels through `t(l)`), On this phone (Progress with `workLines` — "on-device reader" and the count words reading/check translated —, Usage sharing with its sentence, state and id, Photos with its Delete button and sheet), the Advanced settings' locked row with Password, Unlock and "Wrong password.", About's sentence (`aboutText`); the Add form (every label and button, the placeholders, the errors, the autofill hint, "“{0}” added."); the Edit form (← Back, the Edit badge, the image field with Crop again and Remove image, the labels, the context word field, the flag, the AI button, Save changes / Cancel, Delete card, its sheet, the errors, the AI error line); the notices (the clipboard fallbacks, the export and import alerts, the import sheet and its sentence, the Reset sheet). Two local variables named `t` were renamed so they no longer shadow `t()` (`backupNote`'s `last`, the feedback handler's `tx`). Checked by screenshot in German and Japanese: More in light and dark, the Add form with its error, the Edit form and its Delete sheet; the static key check reports no missing key. **The tone (H, on seeing PR 3: "Be relaxed. This is a fun app. No formal, boring translations within the app. Customers are passionate language learners. Same for the design and layout."):** every language speaks to the learner as a friend — German in "du", French in "tu" (the whole column was switched from "vous" in v255, the imperatives with it), Spanish in "tú", Korean in the friendly 해요 style (every 합니다 ending switched in v255, "감사합니다" → "고마워요"), Japanese in the plain-polite です/ます that apps use — nothing stiff, no "Veuillez", no "Sie". **Done in v256 (PR 4): the meaning in the chosen language from the AI** — H chose the button over an automatic run, since it costs an AI call per batch of cards. Both prompts are functions now (`aiSystem()`, `picSystem()`) and ask for "natural German meaning … in German only" through `meaningLangName()` (`LANG_NAME` in `lang.js`, the languages' English names); every AI answer carries `ml` = the language asked for (`aiAsk`, `aiReadPicture`), and **a card stores the language of its meaning as `ml`** (absent = English — the dictionary, the phrasebook, the offline model and every card before v256; `mlOf`, `setMl`): the Read preview's card takes it from the AI's answer, a meaning typed by hand in the preview, the Add form or the Edit form counts as the app's language, the Edit form's AI button and the Add form's autofill carry the answer's, Accept on a suggestion takes the suggestion's, an untouched meaning keeps its language. **The meaning check by language** (`saneM`, in place of the v97 "no Latin letters" rule): an answer that is the Chinese text itself is dropped, an all-Han answer too — except in Japanese, where a meaning may be kanji alone (供給); kana and hangul count as a meaning. More → Language: the sentence reads "The app's own texts and the meaning of new cards. Cards keep their Chinese and pinyin." (v278, H: "Why mention the AI here?" — the meaning comes in the chosen language through the AI check, which every phone has through the relay; the rare English card carries its pill; until v277 the sentence said "and, with the AI, the meaning"), and under the chips a row **Meanings** — "N cards have their meaning in another language." with the button **"Translate all cards into Deutsch"** (the language's own name; `translateRowHTML`, `#translate-all`, shown only while such cards exist and an AI is set up, in English too: a phone switched back to English sees "Translate all cards into English" for its German cards): `translateAll` sends the cards whose `ml` differs from the app's language to the AI in batches of `TRANSLATE_BATCH` **5** (20 until v260 — H's screenshot of 99 Korean cards: "번역 중 0 / 99…" standing still, "no real-time progress": the count could move only after the first answer, and Qwen through the relay takes long for 20 cards; since v261 the row shows the moving bar with the count of the card the AI is on — `TRANSLATE.at`, set as each batch goes out — so the line moves every few seconds) (**the run's state lives in `TRANSLATE`, not in the row** — v257, H the same evening: "pressed Translate all, changed page, pressed again and no reaction": the tab change had re-rendered More, the loop wrote its progress into the old row and a guard swallowed the second tap; now `translateRowHTML` draws the row from the state, `translateRefresh` re-queries the row by id after every batch, a tap during a run shows the progress, the button is disabled meanwhile, the finished line stays until the language is switched (`setLang` clears a finished run), Learn or Cards re-render when the run ends, and a run stops at the next batch when the language was switched meanwhile) (`aiCardPayload` with `translate:true` says "translate the meaning into German (it is in English now); keep zh and p unless clearly wrong"), takes only the meaning and its language from each answer — the text and the pinyin stay —, **stages every answer instead of writing the card** (v264, H: "make translation consistent and make sure that cards are not mixed in different languages if the translation was interrupted"): setting `translateStage` `{lang, m:{id:{m,from}}}` — `from` = the meaning the card had when it was asked, so a meaning typed meanwhile is not overwritten; an answer without a meaning is staged as `skip` so the run can end — and **the cards change together in one IndexedDB transaction** (`idbPutMany`, `applyStage`) only when every card has its answer; until then Learn and Cards show the old meanings, all in one language, with the pill on every card, the row says "Translating 20 of 43 … The cards change together when all are done.", an interruption keeps the stage and the resumed run asks only for the cards not staged yet (`inStage`), a switched language drops the stage (`setLang`, and the follow-up run of v263), `running` stays set until the cards and the settings are written; then "Done — 43 cards translated." and the button goes; a failed call shows "The AI could not be reached. 20 translated, 23 left. It goes on by itself when the AI can be reached again." and keeps the button; offline it says so. **An interrupted run goes on by itself** (v262, H: "Please continue translating after being interrupted"): the run is remembered in setting `translateRun` `{lang, at}` from its start until no card is left (`rememberTranslate`), and `resumeTranslate` picks it up again 2.5 s after boot, on `online` and on foreground (`visibilitychange`) — when the app's language is still the run's, cards are left, an AI is on and the phone is online; **a language switched during or between runs retargets the translation** (v263, H: "If the language is changed during translation then all the cards have to be translated into the new language" — until v262 the switch dropped the run): a running loop stops at its next batch and `translateAll` starts a fresh run for the new language at once, a waiting run is re-remembered with the new language and resumed by `setLang`; each batch stores the language it was asked in (`lang` captured at the request, not `LANG` at the answer), so a batch answered after the switch carries the old language and the follow-up run translates those cards again. So a restart, a lost connection or Android cutting the background page's requests no longer ends the translation, and every card ends in the language the app shows; the row shows the progress when More is open. Suite `verify-translate` (a failed run keeps the setting and the line names the resume, a reconnect resumes and finishes it, a reload with the setting resumes at boot without a tap, a switch while a run waits resumes it in the new language, a switch mid-run — two German batches answered, then French ones — ends with every card French and the French Done line; staged: after two batches no card has changed and the stage holds ten, a cut connection leaves every card in the old language with the stage and the run remembered, the reconnect asks only for the remaining cards — the call count says so — and at the end every card changes together with the stage and the run gone). The parts row stays English (the dictionary's). A card filled without the AI shows an English meaning marked pending until the auto run's suggestion, which carries `ml`, is accepted. Suite `verify-translate` (no row in English on an English deck, the row and the button in German with the count, two batches for 26 cards with the German prompt and the translate ask, every meaning and `ml` changed and persisted, the Add form's autofill and a typed meaning, a suggestion accepted, the Edit form's typed meaning, `saneM` in German, Japanese and Korean, the way back to English dropping `ml`, a dead call's line); the Meanings row checked by screenshot in German, light and dark — the button sits under the sentence, as the Feedback row's Send, since its label is long in every language. The ai, addai, edit, busy, picture, savenow, recrop, aibad, aikept, aizh, admin and english suites pass (the aibad suite reads `aiSystem()` since). **Done in v258 (PR 5, H: "What is PR 5?" → described → "Go"): the pill for a meaning in another language** — a card whose `ml` differs from the app's language carries a grey pill with that language's own name ("English" on a German phone, "日本語"; `mlPill`, `langName`, `.pill.lang` in the plain pill look, title "The meaning is in another language than the app.") after the meaning on the back (Learn and the detail, `backHTML`) and on the pills line of the Cards list beside Traditional; it goes once the card is translated, by Translate all or an accepted AI check. Nothing else changes, no AI call. Suite `verify-translate` checks the list, the detail, the Learn back, the pill gone after a translation and a Japanese one; the back and the list checked by screenshot in German, light and dark; the english, cards, detail, link, trad and flag suites pass. The multi-language plan of v253 is complete with it. **One meaning per language on the card (v265, idea 2 of the improvement list, described first, "Go"):** `ms` = `{en:"…", de:"…"}` holds every meaning a card got, by language — written wherever a meaning gets its language (`setMl` records `card.m` under it) —, `m` stays the meaning shown and `ml` its language. `syncMeanings()` at boot and in `setLang` gives every card the meaning it already has in the app's language, in one transaction (`idbPutMany`), so a switch back to a known language is instant and costs no AI call; a card without one keeps its meaning and the pill, and Translate all fills it. A typed meaning replaces only its own language (the others stay); a changed Chinese text drops the other languages' meanings (`applyCardUpdate`), since they described the old text; the offline model's meaning is recorded under `en`; the Cards search finds every language's meaning. Cards from before v265 get `ms` at the first start. Export and import carry `ms` like any field; an older app ignores it. Suite `verify-translate` (the switch back to English instant with no call, the three cards made in German keeping it with the pill and the row counting them, German back from `ms` on the next switch, a typed meaning replacing one language, a changed text dropping the others, the migration at boot); the english, cards, edit, addai, ai, export, admin, detail and link suites pass. A static check in the harness lists every `t()` key and count word of `app.js` that a column lacks (`node -e` over `lang.js`); at v254 the columns lack only the count words of the More screen and the owner's reports. The owner's tools stay English: Diagnostics, All users, the owner's Feedback row, the usage report, the Unlocked row, the AI setup form, Mirror, Text recognition, Offline translation, Reset, `privacy.html` (their count words go through `nOf` and would follow a switched language — H uses English). Japanese and Korean are mine and unchecked by a native speaker; not yet field-checked on the phone.

### The flow (AI-first — "nobody wants to see OCR if not necessary")
1. **Camera:** `<input type="file" capture="environment">` (Take photo) or `<input type="file" multiple>` (**From album** — several at once, all land in the inbox, the first opens in crop mode; `addPhoto` per file) — or **a screenshot shared to the app from another app** (v163, H: "a fast and easy function to translate screenshots": the manifest's `share_target` posts the files to `./share`, the worker's `shareIn` parks them in the cache `zt-share` and answers with a redirect to `./?share=1`, the page's `takeShared` moves them into the inbox through `importPhotos` — the same path as the album, the first in crop mode; the installed app only, a browser tab has no share target; Chrome picks the new manifest up on a later launch, so the entry in the share sheet may take a day or a re-install — not yet field-checked) (a "Done, no card" link beside Cancel that also deleted the photo, v163–v167, went in v168 — H: "What's the difference between Cancel and Done, no card?" … "Cancel only"; Cancel keeps the photo, the inbox's Delete removes it) → the photo is normalized (EXIF baked in, max 1600 px) and stored in `inbox`. A pulsing "Processing photo …" placeholder appears at once; the inbox re-renders on `visibilitychange`; pull-to-refresh is off (`overscroll-behavior-y: contain`).
2. **Frame:** the photo lands directly in crop mode. **A photo that opens by itself gets its frame drawn by the app** (v203, H's poster photo that needed no framing: "do an automatic image analysis and suggest a crop, which the user can change if needed — or apply no crop at all", described first and built on "Go"): while the reading box says "Finding the text …" with the bar, `textRegion` takes the ink of a 360 px chromaticity copy, the rows that look like text (the inkHeight rule with four stroke edges), the first and last of them, the columns holding ink inside that band, padded by one text height — and proposes **the whole photo** when nothing stands out or the text fills it (H's "no crop at all"); **the proposed frame has a fixed shape whenever the photo allows it** (v207, H: "propose a certain aspect ratio that fits for most images — I want uniformity across image previews", described first with 16:9 recommended, built on "Go"): `shapeBox` widens the padded text box, never narrows it, to `FRAME_RATIO` 16:9 — the shape of the Cards list's 104×58 thumbnails —, centred on the text and shifted to stay inside the photo; a text the photo cannot hold at that shape (a tall sign, a text that fills the photo) keeps its own padded box or the whole photo, so those previews stay irregular; the card image then carries more surroundings, the reader's second look tightens as before, frames drawn by hand are untouched; `proposeFrame` sets `CROP.rect` from the layer's size (`CROP.proposed` = "whole" / "16:9" / "text", a line in Diagnostics' reading steps that says which), draws the frame and hands over to `showCropPreview`, so the usual wait and the reading follow without a finger; a finger, Cancel or another photo during the ~100 ms drops the proposal. The proposed frame is adjustable like a drawn one (corners, moving, the handle). It comes only with a photo that opens by itself — taken, the first from the album, shared (`CROP.auto` set in `importPhotos`); **the Crop button never proposes**, so Cancel, then Crop, gives the empty layer to draw on as before. Measured in the harness: the poster reads its five lines with the frame as the whole photo (the tall 爸爸 also gives a stray line 人刀女 from the reader, not the frame); a single 有 on a red field gets a padded box widened to 16:9 (17–77 % across, 36–81 % down); the decorated composite and the two-line test image fit 16:9 too, H's street photo of the sign does not (its box is taller than 16:9 allows at the photo's width) and keeps its own box; H's street photo of the two-line sign gets the sign with the fence below it — the fence bars look like text rows, the frame is a proposal and the reader's second look tightens; nothing to fix on the ten regression images, which are tight crops and get the whole photo. Not yet field-checked. Otherwise, or after Cancel: a finger stroke draws a rectangle (bright dashed frame with a dark outline, nothing dimmed), the corner handles resize it, dragging inside moves it, a preview of the cut sits below. **Reading starts by itself 1.2 s after the finger lifts** (`READ_TIMER`; a corner drag restarts the wait; "Read now" and a small "Image only" link remain). **The frame can be turned** (v185, H: "enable rotating the crop rectangle", described first and built on "Go"): a round handle with a stem hangs 34 px above the frame's top edge (`.h.rot`, `ROT_HANDLE`); dragging it turns the frame around its centre (`CROP.rect.a` in degrees, the raw pointer, not the clamped one, so the finger may leave the photo; snapped to 0 within 1.5°), the frame is drawn with `transform:rotate(a)` and the corner and inside tests run in the frame's own turned coordinates (`rotPt`, `toLocal`; a corner drag in the old centre's turned system is shifted so the rect turned around its new centre lands on the same spot), moving keeps the angle, a new frame starts at 0°. `cropBlob` draws the photo turned back around the frame's centre, so the cut is the frame's content **upright** — the reader's straightening then finds nothing to do (`sg.angle` 0), the card image is the upright cut, and what the frame covers outside the photo takes the dominant colour of the real pixels around it (`dominantRGB`, shared with `deskewBlob` since v185; probing the canvas outside the photo once made that colour black and the reader read garbage); the enlarged view of a turned frame shows the upright cut itself (`_prevURL`). Measured on the 12° test sign: a frame turned to −13° cuts an upright image (deskew 0) that reads both lines at once. It stays a rectangle — not the four free corners of v127/v128. **The browser's own touch behaviours stay off the app** (v248–v249; H's screenshot while adjusting a frame in Crop again: Chrome's "Copy image / Download image / Share image" sheet over the frame — "Don't make those things pop up while adjusting the crop", then "Find all such useless behaviours and remove them"): one `contextmenu` listener on the document cancels the long-press menu on every picture, canvas and control (`img, canvas, button, .croplayer, .shotwrap, .drawsheet, .picbox, .thumbbox, .reticle, .ck, .chip, .tab, .grade, .seg, .linked`), and styles.css gives pictures and canvases `-webkit-touch-callout:none`, `-webkit-user-drag:none` and `user-select:none`, the controls (buttons, tabs, chips, grade tiles, strips, the picker, the switch, the drawing sheet, the crop layer, the text box, the photo boxes, the enlarged view, the linked row) `user-select:none`, and `body` `touch-action:manipulation` — no double-tap zoom, the crop layer and the pad keep their stricter values (the callout is Safari's, for the iPhone). **The badges and the forms' labels are non-selectable too since v272** (`.badge`, `label`, `.topline`, `.mrow .t` in the same rule — H's screenshot: a long press on the Edit badge had opened the Xiaomi's dictionary card for "edit" under the form); plain text — More's sentences, the fields, the meaning — keeps its menu, so a meaning can still be copied (measured: `user-select` none on the badge, the label and the hint, auto on the meaning field and More's sentence). **The keyboard's autocorrect, capitalisation and spell-check are off** on the fields that hold Chinese, pinyin or a search — the Add and Edit forms' characters and pinyin, the context word and its pinyin, the line inputs under the strips, the preview's pinyin and the Cards search (`autocorrect="off" autocapitalize="off" spellcheck="false"`; Android capitalised the first letter of a pinyin textarea and underlined every syllable) — the meaning fields keep them, English prose wants them. Measured in Chromium: the contextmenu event is cancelled on the front picture and text box, a tab, a grade tile, a list thumbnail, a chip, the Edit form's crop, a strip character, the pad, the reference view and the sheet, and not on the meaning field; the attributes sit on the pinyin, line and search fields; the phone's long press not yet field-checked. **Once a frame exists it is the only frame** (H, v129–v132, seen on the phone): a stroke outside it draws nothing and **scrolls the page** like anywhere else (the layer carries `.framed` with `touch-action:pan-y` from the finger's release and whenever `CROP.rect` is set in the render; the frame and its handles keep `touch-action:none` and take the finger), adjusting the frame is allowed at any time, during a reading too, and re-reads; a new frame needs **Cancel** first, which removes the frame and stops a running reading. **A tap outside the frame enlarges the framed area** (v169, H: "toggle between full image display and only cropped area display by tipping with a finger outside the cropped area", described first and built on "Go"): a short tap without movement (under 8 px and 600 ms; a swipe still scrolls) sets `CROP.zoom` — **the tap is taken from the click event** (v206, H: "tipping outside a cropped frame and then reducing the visible image to the cropped part does not seem to work with iPhone": until v205 it was read from `pointerup` with the distance and time check, and on a layer that lets the page scroll (`touch-action:pan-y`) iOS Safari turns the touch into a scroll gesture and sends `pointercancel`, so no pointerup ever came; a click is the platform's own "a tap, not a scroll" on every browser — the press position and time are kept for the same check, so a mouse drag outside the frame, which also ends in a click, does not toggle, and a frame gesture marks the layer (`layer._gesture`) so its closing click is ignored; measured in Chromium with mouse and with emulated touch, the iPhone itself not yet — no WebKit in the sandbox), the photo box then shows the cut alone with a 4 % margin, enlarged to the box's width (`.shotzoom`, a background image positioned by percentages, `zoomStyle`), the frame's handles hidden; a tap on the enlarged view goes back, the frame is where it was; adjusting the frame stays in the whole-photo view; Cancel clears it with the frame (`CROP=null`). **Each reading has a run number** (`READ_RUN[id]`): a reading that a corner drag or Cancel superseded abandons at its next step — no status text, no result, no AI call (v117: two readings of one frame once asked DeepSeek twice within a second). **The frame stays on the photo** through reading and the card preview (`cropRectStyle` re-draws it from `CROP.rect` in percentages after any re-render) and goes only on Save or Cancel. No dialogs in the flow: no frame → nothing happens; the same text from the same photo is an inline note ("This text is already saved from this photo.", `d.c===c&&d.shot===id`), from another photo it becomes a second card (v118).
3. **Read** (`cropSign`): the framed area is **straightened first** (`deskewBlob`: projection-profile skew estimate on a ≤ 360 px copy, ±20° in 1° steps then half degrees, text pixels = far from the median brightness so both polarities count; below 1.5° nothing happens; **the rotated copy is for the reader only — the card image is the crop as framed, never rotated** (v145, H: "don't show the cropped images rotated in the preview, I don't want to see that gray frame around them"); the corners the rotation opens in the reading copy are filled with the crop's dominant colour — the most frequent 4-bit colour bin, then the mean of its pixels (v138, H's 美团 logo: one edge pixel was the dark logo frame, the dark corners became the "ink" of the chromaticity copy, the black characters fell on its background side, the ink height came out as 0 and the reader never saw 美团 at a fitting size; a channel-wise median is no real colour and a border colour is the frame line again — both changed the ink height of 业主直租 and lost its winning pass); H: a tilted 首都铁骑 read as garbage) → on-device OCR line by line (Tesseract.js 7, `chi_sim`, PSM 6, symbols under confidence 35 dropped; `readPass`; **digits are kept** when fairly sure (confidence ≥ 70) and near the characters' height (0.4–1.6 × the line's median box height — the boxes' heights swing: a ribbon ring once read as a 1 at 2.1 ×, the real 30 of 30分钟 measured 0.55–0.7 ×; H: the number was missing from the translation); letters stay out; a number reads as itself in `lineMeaning` and stays one token in the pinyin, `pySpaced`) — the status text lives in `READING[id]` and is re-queried on every update, so a re-render (foreground return, AI answer for another photo) cannot swallow it or the error; the user sees **one line with a moving bar** (`readingHTML`: "Reading the text …") for every step, only failures show as text (H: the steps are of no use to the user); a step that lasts longer than `READ_STUCK` (20 s) shows its own text under the bar ("still at: loading the reader …", `READ_AT`), because a stuck step is a failure; `cropSign` is guarded from its first line (the cut-out, the reader load) and every failure shows as "Reading failed: …" and goes to the error log — → second look (`secondLook`) and competing readings (below) → `SIGN[id]` = `{lines, orig, conf, boxes, img, angle, tightened, region, alts}` (`img` = the straightened, maybe tightened crop the text was read from, `boxes[k][i]` = each symbol's box in it; `region.passes` = the competition and `region.tightBlob` the tight crop, for diagnosis; **`alts`** (v96) = up to six other readings, distinct texts best first, always including the traditional reader's best and the best reading of another length — the AI gets them as `alt` ("the true text is often a mix of them, or a well-known name they circle around"; 养兴多 / 义乐多 / 和准浴多 → 养乐多), the picker offers their characters at the same position (`altCharsAt`, lines with the same character count only), and they are saved on the card as `alts` for a later AI check) → the editor, laid out **like the Edit form** (H, v90): a **Characters** field (H, v114 — not "Text"; the Edit form and the Add form say the same instead of "Word" / "Sign text") with a **character strip** per line (and a **line input under the strip** for typing the correct text (v120, H: "like in Edit mode" — it went in v109 as "unnötig" and came back for the 拉 door plate that no reading got right; on `change` the strip is redrawn from the typed line and the AI check runs again, `wireSlines(root,onInput,onCommit)`; the Edit form has the same input, and its strip follows typing the same way since v120)), a Pinyin field and a Meaning field that follow the reading and the AI check unless edited by hand (`sg.pinTouched`/`meanTouched`, `pinEdit`/`meanEdit` — a hand-written meaning saves as verified, no offline model), a **⚑ Flag for review** checkbox with a note field (`sg.flag`/`flagNote`, `data-sflag`/`data-snote` — H, v94: flag a new card at once instead of opening it again; the Add form has the same pair, `f-flag`/`f-note`, kept in the draft), the source under the meaning ("from the phrasebook" / "composed word by word, unverified"; after a good AI answer nothing — **never "checked by the AI"**, H v105: "not relevant for user", and since v154 not the model's remark either, H: "don't show the OCR slip message to the user"; the remark stays in the post-save suggestion box and in Diagnostics); "Tap a character to change it, or type the line below." sits directly under the first strip, before the line field and the simplified reference, in the preview and the Edit form alike (`slineHTML`, H v111/v112, the second half since v120) and the head line shows the low-confidence note, or in red "This reading looks wrong — frame the text tightly and read again, or fix the characters."); **while the AI check runs, the moving bar with "Checking pinyin and meaning …" sits under the Meaning field** in place of the source line (v200, H: the meaning takes a while to come out clean and nothing said the app was still at it; `busyHTML`/`AI_BUSY_TEXT` in `signPreview` while `sg.aiBusy` — the meaning field holds the word-by-word gloss meanwhile, and the bar tells that it will change; the head line carries no AI status; the Add form and the Edit form show the same bar under their Meaning field while their AI request runs); no per-line pinyin and no gloss table any more; with a red hint when only 1–2 characters were found with low confidence (frame tightly, drag a corner) → **automatic AI check** (`signAskAI`, when the AI is live) → the preview shows pinyin and meaning (no AI status text — H, v105; no remark — v154) → **Save card**. **Traditional characters on the photo** (v101, H: "the hanzi doesn't match the image" — the Yakult logo is 養樂多, the card 养乐多): the photo is taken to show traditional characters by a **vote** (`tradPhotoOf`, v113): every pass that read a line of the final text (the same line, or one character off) casts its effective score for its reader, the merged composite abstains, and traditional wins only with more than 1.5 × the simplified reader's votes (`r.trad`, `sg.trad`); **a silent simplified reader is no vote** (v142, H's 美团 photo: the only pass agreeing with the final text was the traditional reader's lone 国, the simplified reader had read 加 and 权, so 0 votes against 54 marked the photo traditional and H's typed 美团 came out as 美團 — "Richtig ist aber simplified"): with no simplified vote at all, traditional needs at least two traditional passes on a text of at least three characters. **A line typed over the reading drops the flag** (v142): when the typed line keeps none of the reading's characters, `sg.trad` is cleared and the preview goes back to plain characters (`wireSlines`' `onCommit`); the reading is no evidence for a text that has nothing left of it. On a simplified sign both readers read the same lines, so it stays off — H's vertical escalator sign 儿童及行动不便者乘梯须有成年人陪同 was marked traditional in v101–v112 because one line from the traditional reader inside the merged winner was enough; a text with no traditional form at all (推) is never marked — both readers give the same glyph, so there is nothing to vote on (v116); on the Yakult logo the simplified reader never produces 乐 (樂), so the traditional side wins. **The mark can be changed by hand** (v146–v150, H's 9楼 was marked traditional and shown as 9樓: "Falsch erkannt als traditional. Bitte änderbar machen im Crop modus und unter Edit"): a two-way switch **Simplified | Traditional** under the line input in the Read preview and the Edit form (`scriptSwitchHTML`/`setScript`, `.seg`/`.segbtn` in the chip look, `data-scriptseg`/`data-scriptset`, `.scriptline` with the switch at the right, no "Simplified …" reference line beside it — the switch starts on the side the reader detected and shows the state). It appears **only when the reader detected traditional characters on the photo, or the card carries the traditional form** (`sg.tradDetected`), and once shown it stays for that preview or form so Simplified can be undone; a plain reading has no switch. Traditional sets the mark through the character table, Simplified drops it; the strip, the line input and the label follow at once, the AI is not asked again, `sg.tradUser` keeps the v142 typed-line rule from overriding a hand-set mark, and the Edit form saves the choice (`upd.trad` set or deleted). Rejected on the way: a link "Not traditional" / "Traditional on the photo" (v146, H: "works, but could be misunderstood"), the switch on every text with the Traditional side greyed where both scripts are the same (v149, after the vanishing switch on 工作 had looked like a bug — H: "show the toggle option only if traditional chinese was detected in the photo"); the checkbox of v101–v103 and the separate field went earlier (v104, v110). Otherwise the preview shows the **text itself in traditional characters** (H, v104: no checkbox — "show text in traditional and add simplified for reference"): the strip buttons and the line input show the traditional form (`tradLine`, from `sg.tradText`, which follows the text unless edited: the AI's `zht` when it matches in length — the payload carries `script:"traditional"` and the prompt asks for `zht` then —, else the character table, which cannot tell 分鐘 from 分鍾), the label reads "Characters (traditional, as on the photo)" (no reference line under it since v148; the front and the Cards list keep theirs); typing into the line converts to simplified underneath (`t2s`, `sg.tradTouched`) and the picker keeps the traditional line in step (`s2t` of the chosen character); the taps and candidates act on the simplified line, the card's key. The Edit form shows a traditional card the same way (label, strip with its line input in traditional, reference line); the separate traditional-characters field went in v110 (H: redundant) — the strip's line carries the form, and there is no switch back to simplified. Saved as **`card.trad`** (line breaks like `c`, the key `c` stays simplified for dictionary, pinyin and dedupe). The **front** then shows the traditional form with **one pill "Traditional" under the box** and the back carries "Simplified 养乐多" above the pinyin (`simpRefHTML`, `.script.back`; v227, H's "Go" on the design review — until v226 the front had the two lines "Traditional characters, as on the photo" and "Simplified 养乐多", H v102/v106; the pill is the Cards list's), the Cards list shows the traditional form with a "Traditional" pill and "Simplified 养乐多" on the line below, and searches both, the Edit form edits it through the strip's line. One line of ≤ 4 characters becomes a word card (`t:"Custom"`, `seg`, crop image), anything longer a sign card (`kind:"sign"`, lines joined with `\n`, `segs`, `gloss`, `img` + `shot`). Cards checked here are stored `mt:{src:"llm",verified:true}`.
   - **Second look** (H: a Maotai label in a loose frame with stripes, ribbon and neck label was read as one false character from the ribbon): **on every reading** (v96 — until v95 only when the first pass found text in under 60 % of the frame or was unsure; H: "time is no issue", and the copies read 脊 where the first pass had 疹) that text area is cut out — its vertical band from the boxes (heights are reliable) plus half a height, its **horizontal ends from the image** (`textRowExtent`: within the band, walk the columns outward from the reader's span until a gap wider than 1.3 text heights or the edge; the boxes' ends drift and the first pass may have lost a character altogether — 骑 was cut in half once, then lost altogether) plus one text height, then 1.5 text heights of plain margin in the crop's **median colour** (the reader wants margins, real ones bring the clutter back, and a corner-sampled red ribbon once framed a white label in red) — straightened on its own and read at four character heights (45/60/75/90 px, `scales`) plus native while cheap. When a tightened reading wins, the **card image** is the same area with a real photo margin of one text height, cut from the crop as framed (`cutUnrotated`: the area's corners turned back by the straightening angle, their bounding box cut — v145; until v144 it was rotated like the reading crop and carried the filled corners), not the padded reading crop. Runs silently — the reading crop is not shown (H: "run it in the background, the user doesn't have to see it"; it served to diagnose the cut-off crops of v73–v74). Not yet timed on the Xiaomi.
   - **Black-on-white and chromaticity copies** (H: 业主直租, white on red behind glass, read as 本; v95: 活菌型乳酸菌乳饮品, red print on a curved cream Yakult bottle, read as nothing — and the logo 養樂多 as 夸交多 / 一汰 / 洛相本): the reader's own thresholding gives nothing on such crops. The tight crop is always read as a **black-and-white copy** (`toBW`: Otsu on the grey image, polarity so that the majority is white — reads the glass case) and as a **chromaticity copy** (`chromaCanvas`/`toChroma`: each pixel's r/(r+g+b), g/(r+g+b), distance from the median chromaticity, Otsu on that, minority = ink — shading falls out, so the bottle's shadow no longer matters; measured on the Yakult line: grey nothing at any size, chromaticity 菌型乳酸菌乳饮品 at 96 %; on black-on-white text it is noise and simply loses) at the same sizes (45/60/75/90/110 px plus native up to 200 px). **Still weak (< 180)? The traditional-character reader** (v96, `chi_tra` from tessdata_fast, 1.7 MB, a second worker made on first need by `traWorker` from the same recipe, `makeWorker`) reads all three copies at the same sizes; its lines are converted to simplified characters (`t2s`, `vendor/t2s.txt` = OpenCC's character table, 3 222 pairs; `s2t`/`vendor/s2t.txt` the other way, 3 881 pairs, both loaded by `loadScriptTables`) and compete like any other reading. H's Yakult logo 養樂多 is traditional: the simplified model can only answer with the nearest simplified shapes (养兴多, 和准浴多), chi_tra reads 義樂多 → 义乐多; on a simplified sign it reads the traditional forms of the same characters (專業脊柱矯正 → 专业脊柱矫正), so it never hurts. The "best" (float) models were measured too and bring nothing on these crops — not shipped. When the best reading is still weak (`WEAK_READ` 180 since v96 — the logo's garbage scored 58, nothing found is 0), the **whole frame** is read as both copies by both readers at sizes from the ink itself (`inkHeight`: the longest run of ink rows in a small chromaticity copy — the old guess from the frame height read a line filling 40 % of a loose frame at 19 px). Cost: 15 passes on the tight crop always, 15 more with the traditional reader and 12 on the frame when the reading is poor (H: "time is no issue"); the Yakult crops take 3–9 s in headless Chromium, not yet timed on the Xiaomi.
   - **Readings compete and merge:** the lines of all tight readings are **merged by vertical band** (a line's band = the median centre ± half the median height of its boxes, not their extremes — inflated boxes once spanned both lines of a two-line sign and folded them into one cluster, so the first line vanished; each reading tends to get one line right and lose another; the most confident line per band is kept, short low-confidence strays dropped) and the merged result competes with the single readings; `readingScore` = mean confidence **cubed** (cf × (cf/100)², v96: real text reads at 90–97 %, the reader's garbage at 79–86 %, and a linear weight let one more character outweigh that) × n^0.35 × fragment penalty for lines under three characters (v96: /3 — /4 punished a real three-character logo against four garbage characters) × (1 − 0.5 × share of 1–2-character lines; v95: a soup of seven short lines, n = 15, once outscored the real three characters) — **both waived for a single line whose characters are as tall as the frame's ink** (median box height ≥ 0.75 × `inkHeight`; v116, H's door plate 推: ten passes read the one character at 98–99 %, it scored 18 as a "fragment" and lost to a merged soup of eight garbage characters at 34 — the chromaticity copies of a white character on a black plate are noise; a merged soup has several lines, so it is not exempt) × (0.75 + 0.5 × dictionary word coverage — a stronger coverage weight was tried in v96 and let a garbage soup with accidental two-character words win), because the model's output swings with scale even on a clean crop (measured: the same image perfect at 0.7×, garbage at 1×); at the competition a pass with **more lines than the frame can hold** is penalised quadratically (`lineFit`: `maxLines` = ⌊frame height / (0.9 × ink height)⌋, at least 1; `over` in the diagnostics — v137, H's bicycle sticker 减震单车: a whole-frame pass read the sticker's wave pattern as a second line 一一八位, the count doubled and that soup beat four passes agreeing on 减震; cutting the extra lines instead was tried first and removed a soup's fragment penalty, so a three-character wave line won — the text stays as read, only the score drops; the regression images, whose frames are tall enough for their lines, are unaffected), the score gets an **agreement bonus**, × (1 + 0.1 per other pass with the identical text, ≤ 1.5) (v96: 业主直租 from three passes lost a tie to 业主直祖 from one), and a **size check** against the frame's ink height (`inkHeight`, computed once per reading, `r.ink`/`r.frameH`; `sizeFitOf`: a pass whose median box height is under 0.5 × it is penalised quadratically — v98, from H's phone diagnostics: the tight crop, placed by a garbage first pass, read the Yakult logo as a four-line soup of 17 stroke-sized "characters" at 87 %, and the count outweighed ten passes agreeing on X乐多 / 养X多 at 90–92 %; real readings measure 0.4–2.5 × the ink height, `h` in `region.passes` — the boxes' heights swing and the ink measure is noisy, hence the low bar). **The gates use the effective score too** (`effScore`, v100 — H's granite sign 等候区: a soup of seventy fragments scored 183 raw, so neither the copies nor the traditional reader nor the whole-frame passes ever ran, five passes instead of forty). The ink height counts only **stroke-rich rows** (some ink, not solid, ≥ 6 edges — v100: the stripes of the decorated test sign made it the frame height, and the real text looked like fragments); **a dense row inside a text band still counts** (v144, H's bicycle sticker 减震单车 framed with the wave pattern above: the bold blue characters with their white outline fill 70–83 % of the middle rows of a tight frame, the band broke into slivers of 21 px on 130 px characters, so every fallback size clamped to k = 1.5, the second look never came, the frame was allowed twelve lines and the phone showed a two-line soup marked traditional — ink up to 92 % with ≥ 8 edges extends a run a text row began, trailing dense rows do not count; measured: 21 → 129 on the sticker cut, which then reads 减展单车 with 减震单车 among the alternatives, the ten regression images unchanged); the whole-frame fallback runs each **distinct scale once** (v144: with the tiny ink height all three sizes were the same pass, counted three times in the agreement bonus and in the traditional vote); it also sets the second look's sizes when the first pass's boxes disagree with it by more than 3× (garbage boxes of 15 px on a 150 px sign once left only the native scale). **Dictionary consensus** (v99, `dictConsensus`, from H's phone diagnostics: after the size check the logo's passes gave 养浴多, 次乐多, 开乐多, 养举多, 养座多 — each one character away from the dictionary's 养乐多 "Yakult", and the AI still refused): when the best reading is a single line and no dictionary word itself, and two or more distinct readings are within one character of the same dictionary entry of the same length (2–6 characters, CJK only), that entry becomes the reading and the old best joins the alternatives (`region.consensus`); a scan of the dictionary keys, ~80 ms. Only entries **of the best reading's length** count, and the best must be a supporter itself unless three readings agree (v100: 业主直租 once became 下人, a two-character word that garbage fragments circled). Measured: the phone's list → 养乐多 with five supporters, both screenshot crops → 养乐多; 业主直租 / 专业冰矫正 / the Yakult line stay untouched (no entry within reach). Inside a pass, lines of tiny characters beside a tall line are dropped (`readPass`, 0.45 × the tallest line's median box height; v96: a soup of five such lines, 18 characters, outscored the logo's three). A merged band is kept only when two passes saw it or it reads at ≥ 85 %, and when its height is at least 0.45 × the tallest band (v95: the oval of the Yakult logo became 和一一 in the colour pass and rode along). **Known limit:** two characters read as one at a small scale (专业脊柱矫正 → 专业冰矫正 at 98 %) beats the right six-character reading; the LSTM's symbol boxes are evenly spaced guesses, so a fused glyph leaves no geometric trace (a box-width penalty was tried and did nothing) — the AI fixes it from the alternatives. Measured on a striped, ribboned composite: garbage at 86 % → 本区域禁止违规 at 99 %; the 12° sign still reads both lines; H's granite sign 等候区 (yellow paint in carved characters on speckled stone) reads at 93 % once the fallbacks run (v100, 44 passes, ~30 s in headless Chromium); the Yakult screenshots read 5菌型乳酸菌乳饮品 (the faded 活 at the bottle's edge becomes a stray 5) and, since v99, 养乐多 by dictionary consensus (the passes themselves give 养兴多 / 义乐多 / 和准兴多 — 楽 is a Japanese glyph neither model knows, and the 和 is the oval's left arc). Diagnostics: `region.passes` (the competition) and `region.tightBlob` (the tight crop).
   - **Several readers at once, and a clear reading skips the copies** (v209, H: "wherever possible try to improve speed" — measured first in headless Chromium: a reading of 19–49 passes took 1.7–4.5 s, 60–75 % of it inside the passes, which ran one after another on one worker): the second look's and the whole-frame fallback's simplified passes go to a **pool of readers** (`ocrPool`/`runPasses`: the first worker plus up to two more `chi_sim` workers made on first need — one fewer than the phone's cores, at most three, only one on a phone with 3 GB or less by `navigator.deviceMemory`, since each holds the model; `poolSize`), every free worker takes the next pass and the results come back in the jobs' order, so the competition sees the same passes as before; in the whole-frame fallback the traditional reader's chain runs on its own worker at the same time. **A clear reading skips the black-and-white and chromaticity copies** (`r.clear`, the step "the reading is clear …"): when two colour passes of the second look agree on the same text of dictionary words at 95 % or more (`meanCf`, `dictCover`, at least two characters) — those copies exist for light-on-colour and shaded text, where the colour passes are not clear, and on a clean print sign they only lose. Measured: the ten regression images and the consensus case read the same (the two-pass and consensus suites pass); the clean test sign 坚果供应 drops from 19 passes to 3 (1.6 s → 0.2 s), the decorated composite 4.4 → 3.8 s and the Yakult logo 4.5 → 3.6 s in the four-core sandbox, where the pool of three shares the cores with the page — the phone's eight cores should gain more; not yet timed there. What stays: the traditional reader is one worker, the AI check is the network's time (v208 took Qwen's thinking out of it). **The copies overlap the readers and the traditional chain starts early** (v236, H: "Optimize everything for speed" — profiled first in headless Chromium with the pipeline's functions wrapped in timers: a 3 s reading spent 0.6–1.5 s on the main thread making the black-and-white and chromaticity copies, one by one at the moment a worker asked for it, so the workers waited for each copy and the main thread sat idle while they read; the traditional reader made the same copies again; and its 18-pass chain on one worker waited for every simplified pass — H's granite sign took 13.9 s): `staged(makers)` makes the first copy at once and every later one after a turn of the event loop, so a finished worker gets its result handled and its next job meanwhile; the second look caches each copy per mode and scale (`srcCache`, the traditional reader reuses them) and starts the black-and-white and chromaticity copies while the colour passes run (a clear reading throws them away, the fast path loses at most one copy); when the colour passes are already weak the traditional chain runs beside the simplified copies and its results are appended after them in the old order, so the competition sees the same passes; the whole-frame fallback stages its copies the same way; `poolSize` allows four simplified readers on a phone with 8 GB or more (three before, each ~100 MB). Measured, same passes and lines on every image: the decorated composite 3.1 → 2.4 s, the 12° sign 2.8 → 2.0 s, the Yakult logo 3.0 → 2.2 s, the granite sign 13.9 → 11.6 s (the sandbox's four cores are saturated by three readers plus the traditional worker; the phone's eight should gain more, and the fourth reader is not measurable here); the two-pass, consensus, picture and english suites pass. Also measured and left alone: boot 162 ms, the Cards list 53 ms, a 4000×3000 photo's import 115 ms, a save 10 ms, the deskew estimate 11 ms, the dictionary consensus 9 ms — nothing to gain there. What a further step could bring: a second traditional worker on an eight-core phone (its sequential chain is the long pole of a hard reading, 24 passes), not built.
   - **What the reader sees must be a JPEG** (`READ_JPEG` 0.95): this Tesseract build misreads large canvas PNGs and WebPs (same pixels: JPEG → the full line at 99 %, PNG → one character); intermediate crops stay PNG so only the last step is lossy — two JPEG generations in a row lost the line too. `deskewBlob` always hands back a JPEG; `jpegOf` re-encodes card images to JPEG 0.85 at save. The pad's small black-on-white images are indifferent (measured).
   - **Fixing one character** (H: "sometimes one character is wrong while the others are right"): every character of a line is a button (`charStripHTML`, low-confidence ones ringed in the tint). Tap one → picker (`openCharPick`) under the line, **two levels: suggestions, then drawing** (H: "you are mixing typing and drawing" — no type field in the picker; typing goes into the line input under the strip, in the preview and the Edit form alike). Any change drops the AI check and re-checks the line automatically.
     - **The character as it looks in the photo** is shown in the drawing sheet only (H, v89: no thumbnail in the picker's "Replace with" row — it crowded the candidates). The sheet's view starts at `charBox(sg,k,i)` — **not** at Tesseract's symbol box: those drift along a Chinese line (measured on the test sign: from the third character on, a box covers the right half of one character and the left half of the next; H saw a blank patch). The line's overall extent and the box heights are right, so the span from the first x0 to the last x1 is split evenly among the characters (signs are monospaced; a line mixing Latin or digits will be less precise) and the size comes from the median box height.
     - **Level 1, candidates:** while the AI is live it is asked at once for that position (`aiCharAlternatives`, a small separate text request; green-ringed buttons first); what the other readings saw at that position comes next (`altCharsAt`, v96), then the dictionary adds characters that form a word with the neighbours (`charCandidates`: 2–4-character words fitting around the position, longer context first, then by how common the character is, `CHARFREQ`); or **Remove** it — a red button "Remove 人" in the picker's action row (v119; until v118 a plain text link at the right that H did not find: "even when I draw one character by hand, I cannot delete the other"); **the last character of a line has no Remove** (v123, H: an empty strip leaves nothing to tap, so no way to draw). **Several characters at once** (v204, H: "after scanning, I want to remove multiple characters at once — select them first, then remove them all together", described first and built on "Go"): a soft **Select** button under the strips (`selRowHTML`, `.selrow` in `.fieldacts`, after the script switch) puts the reading into selection (`sg.sel`, the set of "k,i" positions): the hint reads "Tap the characters to remove, then Remove.", a tap on a character marks or unmarks it (filled in the tint, `.ck.on`) across all lines instead of opening the picker (an open picker closes), **and a finger drawn along the strip marks every character it passes** (v205, H: "swipe over multiple characters in select mode and select them all", described first and built on "Go": `pointerdown` on a character captures the pointer, the first character decides — starting on a marked one, the stroke unmarks —, `elementFromPoint` under the moving finger marks across the line break too, a tap is a stroke of one character; the strips carry `.selecting` with `touch-action:none` while selecting, so the stroke does not scroll, and the click handler stands back), and the row shows a red **Remove N** and **Done**; Remove is disabled while nothing or everything is marked, so the text can never be emptied. Remove (`removeSelected`) drops the positions and an emptied line with them (the per-line `orig`, `conf` and `boxes` stay aligned; a hand-edited traditional line loses the same positions, else it is re-derived), then re-checks like the picker's apply; Done leaves selection with nothing changed; typing into a line input drops the selection, since it shifts the positions. The row sits in the Read preview and in the Edit form, whose strips are the same component (`wireSlines`/`wireSel`, refreshed in place by `selRowRefresh`, no full re-render until Remove). The picker's single Remove stays. **Where the row sits (v272–v273, H's screenshots of the Edit form: "Wofür ist der Select button? … it looks like it belongs to traditional/simplified", then on a two-line card "With 2 lines, that position doesn't make sense neither"):** at the right end of the **Characters label's line** (`.labelrow` around the label and the `.selrow`, in the Read preview and the Edit form alike) — above every strip, since it acts on all lines at once; Select beside "Characters", in selection Remove N and Done on the same line (the long traditional label wraps beside them, `flex:1 1 60px`). Until v271 the row followed the script switch and read as part of it; v272 put it on the first strip's hint line, which on a two-line card read as line 1's. In the Edit form the row lives outside the `#e-lines` box, so the wiring looks in the whole field (`selScope`) and `drawLines` refreshes the row after a Remove; a card without text gets the row on the label's line with its first typed line. The trad and multiremove suites read the row from the label's line since; the savenow, edit, english, charpick and recrop suites pass; the Edit form checked by screenshot on the two-line bakery card idle and in selection, light and dark, and on the traditional card in selection. **Adding a character** (v119, back from v91 — taken out in v92 as "not convinced", asked for again on the 拉 door plate read as 人人): "+ before 人 / + after 人" links under the picker's actions (`ck-ins0`/`ck-ins1`) open the picker in insert mode (the + tile at the end of the strip of v119–v120 went on H's request in v121 — the strip shows the characters only) (`openCharPick(...,"ins")`, head "Add a character at the end / between 坚 and 果"): candidates are dictionary words that continue the neighbours (`charCandidates(line,i,true)`, a placeholder at the gap), the AI is asked for the gap (`aiCharAlternatives(line,i,true)`), or draw it (the sheet shows the whole crop, `charBox` has no box for a new character); no Remove and no insert links in insert mode. The picker closes with an **× in its top-right corner** (`.ckhead`/`.ckx`, v125 — the "close" text link at the right of the action row "looked stupid") or by tapping the highlighted character again (from insert mode that tap goes back to replacing, `box.dataset.mode`). The Edit form has the same links; its line input takes typing too.
     - **Level 2, Not here? Draw it** (when the right one is missing): a **full-screen drawing sheet** (`openDrawSheet`, `.drawsheet` fixed over everything, `body.noscroll`) with **two equal squares** (H: "symmetrical, same size as the drawing field"): the photo character on top (`attachRefView`: a 600 px square view of `sg.img` that starts at `charBox` × 1.5 and is **moved with one finger and zoomed with two** — or the mouse wheel —, photo edges padded in `--fill`; H: "why can't I just drag the image into position and enlarge it?" — the automatic crop is a guess, the hand corrects it) and the pad below (faint 田字格, pointer events); `fitRef` gives both one side = min(sheet width, half the height left after heading, status, result row and buttons), ≥ 160 px (~295 px on a 844 px viewport), refit on resize; Undo/Clear and a red **Done** at the bottom.
     - **Reading the strokes:** **the lines are guided first** (`guideStrokes`, v139, H: "there are ways to guide the lines"): every stroke is smoothed, reduced to its corners (Douglas–Peucker, tolerance 3.5 % of the character), and segments within 12° of horizontal or vertical are snapped to the axis, so the print model sees straight strokes and clean corners — measured on 16 characters drawn as jittered skeletons, three wobbles each: top-1 23 of 48 raw, 46 of 48 guided (top-5 29 → 47); diagonals and curves keep their shape. H felt no improvement on the phone (v140), and his real strokes from the diagnostics showed why: the print model recognised the box of 团 but guessed the inside (回 园 加 / 包 句 问 / 团 园). **Since v141 the strokes are matched first** (`strokeMatch`, described and built on "Go!"): the stroke medians of 9,534 characters (Make Me a Hanzi, from the Arphic fonts, `vendor/strokes.txt.gz`, loaded on first use like the reader files and listed in `OCR_FILES`; `loadStrokes`, `prepStrokes`) — every stroke scaled into the unit square with the whole character and resampled to eight points, a candidate with a stroke count within one (two from eight strokes on) scored by the **best assignment** of drawn strokes to its strokes (Hungarian method, `assignCost`; a stroke drawn backwards costs a little extra, a missing or extra stroke a fixed skip), so **H's own stroke order does not matter** (he closes the box of 团 third, the standard order closes it last); matches under cost 0.4 come first, the print model's readings after them, then the neighbour ranking as before; the diagnostics show both lists. Measured: H's three real drawings of 团 → 团 first each time (cost 0.08–0.13, ~30 ms per match after a 1.2 s load); the synthetic set 36 of 48 first (47 of 48 among five) — the skeletons there are rough. **Nothing is read until Done is tapped** — no timer (H: an inline pad below the fold went unseen, and reading after every stroke "takes it without me confirming"). On Done the strokes are rendered black on white at ~80 px and read by the same Tesseract model (`recognizeStrokes`: the model gives no per-symbol alternatives, so three readings are combined — single-word PSM 8 reads hand strokes far better than single-character PSM 10 — ranked by confidence, then what fits the neighbours first); the results appear as big buttons under the pad; tapping one replaces the character and closes the sheet; Clear to draw again. Nothing downloaded, offline, nothing leaves the device; a print-trained model on handwriting, so clean strokes matter — field accuracy unknown.
4. **Meaning priority** (H's rule): online AI → phrasebook → offline model → word gloss. `aiLive()` = key set + auto on + `navigator.onLine`. While the AI is live the offline model is not run and Save waits for the running AI check (`sg.aiPromise`); if the call fails, `sg.aiErr` is set and the offline model takes over; with neither, the card gets the gloss and `mt.pending:true` and is completed by the post-save auto run when online.
5. **After saving:** the note under the photo says "Card saved — 养乐多." plus "Translation pending." / "Flagged for review." when so (no sources, no AI — H, v105) (the **Next photo** button of v39–v93 is gone — H, v94; the camera tab's own button is enough). Photos picked from the album that are still waiting keep their CROP button below. `QSCARD[id]` remembers the card; if the AI answers later, the green suggestion box with **Accept / Dismiss** appears right there.
6. **Save now — a card before the reading is done** (v237, H: "take a photo and make a crop in a rush, hit Save and move on. App will finish everything in the background and when resources are available", described first in three lines and built on "Go"): while the bar reads "Reading the text …", a soft **Save now** button sits at the right of that line (`readingHTML` with `data-savenow` in `.readrow`, delegated in `renderShots`; it appears when the reading starts, 1.2 s after the finger lifts, and only while a frame stands). A tap makes the card at once (`saveNow`): id `reading#<timestamp>`, the crop as framed as `img` (`cropBlob(id,rect)` takes the frame as an argument since), `shot`, empty `c`/`p`/`m`, `mt` gloss-pending and **`reading:{rect,at}`** — the frame it was saved with; the frame goes, the box shows the bar with one green line "Card saved — the text follows when the reading is done." in place of the reading text (a stuck step appends "Still at: …"; `PENDING[shotId]` = the card's id; `renderShots` shows the bar, never the preview, while the shot is pending, and the photo's row offers Delete alone — no Crop, since a second frame would feed the waiting card), and H takes the next photo or leaves the tab. The reading, the second look, the picture path and the AI check run on as before (`cropSign` keeps `cardImg` and `weak` on `SIGN[id]`, and never touches `S.pendingImg` while a card is pending); at the end `finishPending` awaits the AI check, builds the card through `readingCard(id,sg)` — the former body of `saveSign` up to the fields, DOM-free, `saveSign` calls it too — copies every field but `id`, `at`, `img`, `imgFull` (the inbox's Delete may have copied the photo onto it meanwhile), `shot` and `tags` onto the placeholder, replaces `img` with the tightened cut when there is one **and drops the list's cached thumbnail** (`dropThumb`, v242 — H after a re-crop saved during the reading: "the card with a photo before the re-crop currently remains" — the Cards list had made its thumbnail from the crop saved first and kept it after the fill), drops `reading`, and **flags the card for review** (v245, H: "flag cards that were saved before the final stage, with an appropriate comment" — nobody saw the preview: the note reads "saved before the reading was done — check text, pinyin and meaning", or "saved before the reading was done, and the reading is weak — …" when `mt.suspect`, `sg.weak` = the best pass under `WEAK_READ`, or a `bad` AI answer; until v244 only the weak ones were flagged, with "check the reading") and the note under the photo becomes "Card saved — 坚果供应. Flagged for review."; a reading that finds nothing or fails (`failPending`) leaves the card empty with `reading.failed` and the flag "the reading failed — edit the card or frame the photo again". **Meanwhile** the Cards list shows the crop with "Reading …" in the text's place in the row's own size ("Nothing read" once failed; `.crow .c .lbl`), and **the detail shows the waiting card without a back** (v238, H's screenshot of a Save-now card in the detail with a bare grey "Reading …" in the reticle, a speaker for nothing and a Test button: "Please polish design"): the text box carries the reading bar with "Reading the text …" (`waitingHTML`, `.reticle .glyph .wait` — the bar of the inbox, centred at 58 % of the box, no crosshair for an empty text) and the line "The text, pinyin and meaning follow when the reading is done." under it, the head badge says "Reading …" instead of "Unverified, translation pending", no pinyin, speaker, meaning or parts row, no **Test this card** (Learn skips the card anyway), Edit, Flag and Delete stay ("Delete this card?" in the sheet, since there is no text to name); a failed reading shows "Nothing could be read." in the box, the badge "Nothing read yet" and the flag box with its note, no extra line; the "N photos" pill and the linked-cards row never count empty texts as the same text (`byText`, `sameText`). `buildQueue` skips cards without text (Learn never shows an empty card), and `aiQueue`/`aiAuto` skip them too (the failed card is flagged and pending, but has nothing to check). **Edit** on such a card shows no empty strip and no Select — the hint says "Type the text below." and the strip and the Select row appear with the first typed line (`slineHTML`'s `empty`, `selRowHTML`, the `change` handler in `wireSlines`; v238) — and takes the typed text: `applyCardUpdate` drops `reading` (from the card and from the form's copy) and abandons the background reading (`abandonReading`, `PENDING` cleared) **when the saved text differs from the card's** (v243: an edit of pinyin or meaning on a card whose re-crop is still being read lets that reading finish and fill the card; typing another text is H's decision and stops it). **The work needs the page open** (Android cuts a background page's work as it cuts its requests, v201): a card still waiting at the next start is read again 1.5 s after boot (`resumePending`, sequentially) — from the inbox photo with the saved frame, or from the crop itself when the photo is gone (`cropSign(id,{blob})`); a reading that is not on screen is allowed while a card waits for it (`cropSign`'s "no box" exit). Delete of the placeholder while it reads is harmless (`finishPending` finds no card). The usual flow — wait for the preview, then Save card — is unchanged; `saveSign` takes the picture from `sg.cardImg` (the reading's own) and falls back to `S.pendingImg`. Not yet field-checked.

### Online AI review (T3, opt-in, text only)
- **Setup (the owner's, since v190 — H: "move the AI review details into the locked area and only leave the switch for the users"):** the AI review row shows everyone the state line ("On, through the app owner's relay: DeepSeek (deepseek-chat) checks the text, Qwen (qwen3.7-plus) reads the framed area when the reading is weak." / "On: DeepSeek (deepseek-chat) checks the text. Photos never leave the phone." / "Off. DeepSeek (deepseek-chat) and Qwen (qwen3.7-plus) are set up through the app owner's relay — tick the box to check new cards." while the checkbox is off (v196, H: "it cannot say On when I've unticked the checkbox") / "Off. The app's owner sets it up under Advanced settings." — v195; v193 had tried a short line without model names, H: "I liked the previous text more, revert and polish the first paragraph"), the sentence "What is sent: the Chinese text, pinyin, meaning and your note of flagged, doubtful or pending cards. The framed area of a photo only when the reading is weak, to a provider that takes pictures. Without a key of its own this phone sends through the app owner's relay, which forwards to the provider and keeps only a count." and one checkbox **"Check every new card with the AI automatically (when online)"** (`#ai-auto`, saved at once, setting `aiAuto`; **since v193 it covers the picture too** — H: the picture went out while the check was off, "counterintuitive"; `aiAutoOn()` gates the picture path beside `pictureProvider()`); the **Set up / Settings** button and the form below render only while `S.admin` is set, like Reset and the downloads. A friend's phone has no key of its own — since v191 it uses **the owner's relay** (below). The form: More → Online AI review → a **chip row of providers** (v172, H: "several AI providers to switch between" — "I don't want to delete DeepSeek": until v171 the app held one key, and choosing another provider overwrote it; `AI_PROVIDERS`: DeepSeek default — **field-verified from the phone, no VPN** —, Qwen/Alibaba Bailian, GLM/Zhipu, Claude, Other = custom OpenAI-style base URL; the Chinese ones take WeChat Pay/Alipay, H could not buy Anthropic credit from China). **One account per provider**: setting `aiAccounts` = `{deepseek:{key,model,base}, qwen:{…}}`, `aiProvider` names the active one; `aiKey(pv)`/`aiModel(pv)`/`aiBase(pv)`/`aiAcct`, `setAiAccount`. The form shows one provider at a time (`form.dataset.pv`, the lit chip, `data-aipv`), its key, model and base from its account, and a line "DeepSeek: key saved, in use." / "Qwen (Alibaba Bailian): no key yet." (`#ai-acct`); a tap on a chip **with a key switches at once** (the row reads "On: Qwen (Alibaba Bailian), qwen3.7-plus."), a tap on one without a key only shows its empty form and the active provider keeps working; Save stores the shown provider's account and makes it active when it has a key; Remove key takes the shown provider's key only — when that was the active one, the next provider with a key takes over, without any the AI is off. **Migration** (`migrateAi` at boot): the old `aiKey`/`aiModel`/`aiBase` become the active provider's account and are deleted; a bare `aiKey` set later still counts as the active provider's key (the suites set it that way). Model defaults `deepseek-chat`, **`qwen3.7-plus`** (since v172 — Bailian labels it multimodal, the model H chose for the picture path), `glm-4-flash`, `claude-sonnet-5`; checkbox "Check every new card with the AI automatically" (default on, `aiAuto`). Keys stay in the settings store, device only, never exported, never in the usage row.
- **Qwen's thinking is switched off on every request (v208, H with Qwen as the active provider: "Check pinyin and meaning takes way too long"):** the hybrid `qwen3…` models think by default and the thinking takes many seconds before the short JSON comes; until v207 only the picture path sent `enable_thinking:false`, the text check (`aiAsk`) and the picker's candidates (`aiCharAlternatives`) did not — `noThinking(pv,model,body)` now sets it on all three OpenAI-style bodies (the relay forwards the body unchanged, so it holds there too). The cause is the code's, the timing on the phone is not measured: the AI log now carries each answered call's duration (`ms` in `logAi`, shown in Diagnostics as "→ 200 in 3.2 s"), so the next diagnostics text tells. Not yet field-checked.
- **The model's pinyin is taken only when it fits the characters (v187, H's calligraphy 志在千里 read from the picture: Qwen answered "zhì zài qiān l" twice, the ǐ lost, and the card showed it — "he forgot the i of li"):** `saneP(p,zh)` counts one vowel group per character (syllables may be joined into words, as some models write them; digits aside) and rejects any token without a vowel; on a mismatch the app's own pinyin (`pySpaced`, pinyin-pro loaded on demand) replaces the answer's, line by line. Applied to the text check (`aiAsk`, not for `bad` answers) and the picture answer (`aiReadPicture`) alike; the meaning is English and cannot be checked that way ("a thousand li" in the first answer was the model's wording for the unit 里, not a lost letter). Measured in the harness: "nài sī cān b" → the app's "nài sī cān ba"; the older mocked answers with joined syllables ("gōngyìngshāng", "zì hōngbèi") still pass, a one-letter "x" no longer does.
- **A brand name gets what it is in brackets (v280, H's 蜜雪冰城 card with the meaning "Mixue Bingcheng": "Warum ist hier die meaning nicht richtig übersetzt?" → described, "Go"):** the prompt's brand rule of v97 made "zh" the brand's name and left "m" as the bare romanised name, which tells a learner nothing. Both prompts (`aiSystem()` for the text check, the picker's candidates and Translate all; `picSystem()` for the picture path) now say: when the text is a brand, shop or product name, "m" is that name as it is known (romanised or international), followed in brackets by what it is, in the app's language — "Mixue Bingcheng (ice-cream and bubble-tea chain)", on a German phone "Mixue Bingcheng (Eis- und Bubble-Tea-Kette)" —, never the bare name alone. `saneM` accepts such a meaning in every language (Latin letters, or kana or hangul beside the name). Existing cards keep their meaning until the next AI check or Translate all; nothing else changes. The aibad, ai, addai, aikept, picture, translate and english suites pass. Not yet field-checked — the next brand card from H's phone is the check.
- **Answers are checked (v97):** every `zh` is normalised to simplified characters (`t2s`, v103 — told that the sign is traditional, the model answered with traditional `zh`, and 養樂多 has no dictionary entry, so the meaning lost "Yakult"; a traditional `zh` becomes `zht` when the model gave none; the prompt says "always simplified"); a meaning that is the Chinese text echoed, or all Han characters outside Japanese (the model once echoed the Chinese text as `m`), is dropped and logged (`saneM`, by language since v256), the fields keep the reading's own gloss; the prompt says "in English only" — in the app's language since v256 (`meaningLangName()`) and that the text is usually a sign, menu item, product name or brand — "when the readings circle around a well-known brand, zh is that name"; before calling a text bad it must try the `alt` readings and answer with the plausible one (v98 — on the phone it called the soup garbage, correctly, and left 养浴多 / 次乐多 / 开乐多 unused).
- **A clearly read character is not the AI's to change (v143):** H's bicycle sticker 减震单车 — every pass read 减 at 98 %, DeepSeek answered 共享单车 "shared bicycle", a common phrase that is not the sign, and the preview swapped the right 减 for 共 ("Zunächst wird der erste character richtig angezeigt und dann jedoch zu einem falschen geändert"). `aiSettled(sg,lines,zh)`: when the answer keeps the line count and each line's length, every changed position must be open — the reading's confidence there under `AI_SETTLED` (90), or another reading of the same line length shows the AI's character at that position (养兴多 → 养乐多 with 义乐多 among the alts, 和 → 活 with the 活菌型… alt). A settled position blocks the whole answer: the reading stays, `sg.ai={zh:c,proposed,kept}`, the fields keep the reading's own pinyin and gloss, the remark reads "The AI suggested 共享单车, but 减 was read clearly, so the reading stays. Meaning composed word by word, unverified.", Save stores the card unverified with its gloss (the post-save auto run may then offer the AI's text as a suggestion with Accept — a suggestion, not a silent replacement). A line edited by hand before the check has no confidences left and is never guarded; an answer of another length (专业冰矫正 → 专业脊柱矫正) is not guarded; the guard lives in the Read preview only (saved cards carry no confidences). Field-confirmed with v144 on the sticker (H: "OK. Works.").
- **Garbage is garbage (v95):** the prompt tells the model that the OCR text may be unreadable and asks for `bad:true` then — keep `zh`, empty `m`, say so in the note, **never replace an unreadable text with a guess** (H's Yakult logo: the AI turned 习/汶… into 了 "completed action marker" and waved 夸交多 / 一汰 / 洛相本 through as checked). A bad answer changes nothing in the Read preview: red head "This reading looks wrong — frame the text tightly and read again, or fix the characters.", the fields keep the reading's own pinyin and gloss, the review flag is pre-ticked with the note "the reading looks wrong", Save stores `mt.suspect` ("the text looks misread") and unverified; after saving, a bad suggestion shows as "AI: this text looks misread" with **⚑ Flag for review** (`aiFlag`) instead of Accept, and Accept-all flags such cards.
- **The framed area goes to the AI when the reading is weak (v173, H after 奈斯餐吧: "Send the crop to the AI when the reading is weak" — the one exception to "photos never leave the phone", under H's own switch; described first, built on "Go"):** a provider that takes pictures (`vision` in `AI_PROVIDERS`: Qwen with its multimodal model, GLM with `vmodel` `glm-4v-flash`, Claude) — `pictureProvider()` = the active provider when it takes pictures, else the first one with a key that does; `pictureModel`; setting `aiPicture` (absent = on), the checkbox "Send the framed area to the AI when the reading is weak" under the Model field, shown on the chips of picture-capable providers only (`#ai-picfield`). In `cropSign`, after the competition: when the best pass scores under `WEAK_READ` by `effScore` or nothing was found, and a picture provider is set, the automatic check is on (v193) and the phone is online, the bar says "Asking the AI about the picture …" and `aiReadPicture` sends the **whole straightened frame** (`dk.blob` — never the second look's band: v173 sent `best.img`, and on H's two-line sticker 骑车勿盯 / 还车勿忘 the tightened band held the lower line only, so the AI read 还车勿忘 alone and the card image was that band; since v175 the frame goes, the card image is the crop as framed, `S.pendingImg=r.blob`, and the sheet shows the frame) as a JPEG of at most 800 px (`pictureJpeg`, `PIC_MAX`; never the whole photo) with up to six distinct reader texts as guesses (`picSystem()`: one JSON object `{zh,p,m,note,bad}`, simplified, the picture's lines, no Latin letters; `bad` only when no Chinese text is readable). OpenAI-style `image_url` with a data URL, `enable_thinking:false` for Qwen models starting with `qwen3` (the hybrid models think by default), Claude's `image` block with base64; the log entry carries `[picture 800×442 JPEG, 24 KB]` and never the base64; tokens and the picture itself count in the usage counters (`pics`, v178). A good answer **replaces the reading**: the AI's lines with no confidences (every character open in the picker) and no boxes (the drawing sheet shows the whole frame), the reader's texts become the alternatives, `sg.ai={…,ok:true,pic:true}` fills pinyin and meaning, nothing under the meaning, and a **small mark at the right end of the Characters label** — the green AI capsule of the Cards list (`--ok` on `--ok-soft`, radius 999, 11 px) with a filled sparkle and "AI", floated right, title "Read from the picture by the AI" (`picMark()`, `.picmark`; v180 — H: "polish the layout, place the AI icon more sexy" — until v179 a grey outline sparkle in `--label3`; v176 — H: "a small unobtrusive AI icon so that I know the picture analysis was used" — put it under the meaning, v177 moved it to the characters — H: "the icon belongs under the Chinese characters, not the translation"; v173 had the sentence "Read from the picture by the AI." under the meaning, gone in v174 — H: "not relevant to the user"), no text check follows, Save stores `mt:{src:"llm",verified:true}`. A `bad` answer keeps the reading, marks it wrong (red head, the flag with "the reading looks wrong") and skips the text check; a failed call (`r.picErr`, logged) falls through to the reading and the text check as before. A good reading never sends the picture; without a picture provider or with the switch off nothing changes. The AI row reads "Sends card text, and the framed area of a photo when the reading is weak (Qwen, qwen3.7-plus).", the "What is sent" line and About (`aboutText`, `#about-s`, live with the settings) say it, `privacy.html` has the bullet. Qwen recommended over GLM for the sign fonts; the key path is above. **Field-confirmed the same day:** the first call from H's phone failed with "no connection … refuses calls from a browser" while the VPN was on; with the VPN off, 奈斯餐吧 came back read from the picture (H: "Worked without vpn", "Yes") — so Bailian's compatible-mode endpoint does accept a browser page and `qwen3.7-plus` takes `enable_thinking:false`, and **Qwen must be called without the VPN**; a Supabase edge function as a relay was described and is not needed. GLM (`glm-4v-flash`, free) remains unverified. Measured in the harness with a mocked Qwen: 奈斯餐吧 scores 164 and goes to the picture, 业主直租 scores 174–181 and goes too (a correct reading under the gate costs one call and comes back unchanged), the two-line sign at 266 does not.
- **The owner's relay (v191, H: "Why would a friend enter a key of their own?" → "Go with the relay"):** a phone without a key for a provider sends its OpenAI-style requests to H's Supabase edge function `ai-relay` (`relayUrl()` = `SHARE_URL + /functions/v1/ai-relay`, `RELAY_PROVIDERS` = deepseek and qwen, `viaRelay(pv)`, `relayFetch`, setting `aiRelay` absent = on): body `{provider, body}`, headers `apikey` and `Authorization: Bearer` with the publishable key and `x-install` with the installation id; the function adds H's provider key from its secrets, counts the call in the table `relay_usage` through the SQL function `relay_bump` (per phone and per day) and refuses with 429 above `CAP_PER_PHONE` 200 or `CAP_ALL` 2000 calls a day ("the daily limit of the owner's relay is reached — try again tomorrow"; 404/503 = "the owner's relay is not set up"), then returns the provider's JSON unchanged with CORS headers. **The phone's own key always wins**; `aiOn()` and `pictureProvider()` count a relay provider as available, so a fresh install has DeepSeek text checks and Qwen pictures from the first card. The three transports (`aiAsk`, `aiCharAlternatives`, `aiReadPicture`) each carry the relay branch; the status line says "…, through the app owner's relay", the form's provider line "no key on this phone — the owner's relay is used.", the What-is-sent line and `privacy.html` describe the relay (it forwards and keeps only a count; H's key never reaches the phone). The code lives in the repo: `supabase/functions/ai-relay/index.ts` (Deno, deployed by H from the dashboard's editor with "Verify JWT" off and the secrets `DEEPSEEK_KEY`, `QWEN_KEY`) and `supabase/relay.sql` (the table and the counter function, run once in the SQL Editor); neither is in the shell. **Field-confirmed the same evening:** H deployed the function from the iPad (the dashboard's editor, the name and Deploy button in the footer below the code, the JWT switch under the Settings tab as "Verify JWT with legacy secret"), the first call answered 400 "unknown provider" until the secrets were in (the function says that for a missing key too), then 焦香蜜汁肠 came back through the relay from H's phone without any key ("Looks like it worked"). Remove key on the last key had switched the automatic check off (the v172 rule for a phone without AI) while the relay still read pictures — since v192 the check goes off only when no AI is left. The sandbox cannot reach supabase.co, the relay is mocked in the harness.
- **Every AI request is tried twice (v201, H switched apps during "Checking pinyin and meaning …" and came back to "no connection (offline, or this provider refuses calls from a browser — try another provider)": Android cuts a page's requests when it goes to the background, and the text named a provider problem the user could do nothing about):** the three transports and the relay fetch through `aiFetch` — a request that fails is sent once more, after the page is back in the foreground when it went to the background meanwhile (`HIDDEN_AT` from `visibilitychange`, `whenVisible`), else after `AI_RETRY_MS` 1.5 s; the bar keeps moving meanwhile. A second failure is `AI_NET_ERR` "The AI could not be reached" everywhere — the Read preview adds ". Tap Ask AI to try again." and keeps the Ask AI button (`aiErrText`, which also turns a provider's lowercase answer into "The AI check failed: …"), the Edit form ". Tap the button to try again.", the picker and the Add form say it as before; the detail ("Failed to fetch (tried again after the app came back to the foreground)" / "(tried twice) — offline, or this provider refuses calls from a browser" / "— the API may be blocked without a VPN" for Claude) lives in the AI log only. Not yet field-checked.
- **Request:** OpenAI-style `POST {base}/chat/completions` with `Authorization: Bearer` (system + user message, temperature 0), or `api.anthropic.com/v1/messages` for Claude (`anthropic-dangerous-direct-browser-access`). Payload per card: hanzi, pinyin, meaning, note, why (flagged / uncertain reading / pending), gloss, alt (the other readings, v96) — **never photos**. Reply = JSON array `[{c,zh,zht,p,m,note,ok,bad}]` (`zht` only for `script:"traditional"`). Cost ≈ 400 input + 100 output tokens per card, well under ¥0.01 at DeepSeek; a hundred cards a day ≈ ¥0.10–0.20.
- **Where it acts:** in the Read preview before saving (result shown in green, Save stores it verified); after saving as a suggestion stored `ai:{zh,p,m,note,ok,at,model}` with **Accept** (applies pinyin/meaning, fixes the text in place via `applyCardUpdate`, clears flag and `suspect`, `mt.src:"llm",verified:true`) or **Dismiss** — on the study back, in the card detail, under the photo, and via the **"N AI suggestions waiting — Accept all"** bar on the Cards tab; in the **Edit form** ("Ask AI to check text, pinyin and meaning" fills the fields in place, stored on Save). **Ask AI about N cards** in More sends the whole queue (`aiQueue()` = flagged + uncertain + pending) in one request; the auto run (`aiAuto`, `aiAutoSoon` 1.5 s after a save, and on `online`) does the same for pending/uncertain cards.

### Offline translation (T2, opt-in)
Firefox Translations `zh-Hans→en` v2.0a2 (Mozilla, MPL-2.0; model 44 MB + shortlist 4.7 MB + vocab 0.7 MB gzipped = 50 MB) in the Bergamot WASM engine (`vendor/nmt/translator-worker.js` + `bergamot-translator-worker.{js,wasm}`, npm `@browsermt/bergamot-translator` 0.4.9, no SharedArrayBuffer). Fetched by the **GitHub Action "Fetch zh→en translation model"** (workflow_dispatch) from **Mozilla Remote Settings** (the source Firefox uses; the Git LFS objects on GitHub are gone, 410; the build sandbox cannot reach Mozilla) — it writes `nmt-model.json` (sizes, sha256), bumps both version markers and pushes branch `nmt-model`; Actions may not open PRs here, so the PR is opened by hand. Enable under More → Translation (size prompt; files cached in `zt-ocr-v1`), setting `nmt`; used **only without a connection** once the AI is set up. The preview's translation is kept on the reading (`sg.nmt`, the lines it was made for) and Save reuses it for the same lines (v194, H: the Save button read "Translating …" — until v193 the model ran a second time at Save; the label stays for the rare case that the lines changed after the preview; a re-render of the preview no longer translates the same lines again either). Suite `verify-nmtsave` (a mocked model: one run in the preview, none at Save, the card carries the preview's meaning). `signMeaning(lines)` → `{m, src: phrasebook|nmt|gloss, pending}`; "Translate pending" in More completes gloss-only cards. Verified on the build machine (~40 ms per sentence after load), **not yet timed on the Xiaomi** — the More row shows "loaded in N s, last translation N s" (`NMT.loadMs`, `NMT.lastMs`) for that field test.

### Learning and the library
- **Learn:** SM-2 light; progress rows are `{c, interval, ease, due, reps, fails, last}` (`last` = day of the last review); every review day is appended to setting `days` (max 400) for the **streak**; `learnStats()` → cards learned / reviewed this week / streak, shown on the All-clear screen and in More → Progress — `fails` counts consecutive "again" (hard keeps it, good/easy reset it) and at `LEECH_FAILS` = 4 the card is **flagged automatically** ("failed N times in a row — check text, meaning and photo"): a leech is usually a bad card, not a bad memory. Front = photo (crop) above the text box, **both boxes the same shape on every card** (v223–v231, from H's two screenshots of a square 養樂多 beside a wide 嘉悦芭蕾: "Warum sind manche Hanzi felder quadratisch und andere rechteckig? Quadratisch nimmt zuviel vertikalen raum in anspruch. Bitte consistency!", then the design review that followed). **The photo box** (`.picbox`): a fixed 16:9 box at the card's inner width on `--card2` with the card radius and the text box's hairline edge (`box-shadow: inset 0 0 0 1px var(--sep)`), the crop fitted inside (`object-fit:contain`); behind it a **blurred copy of the crop** (`.picbg`, `object-fit:cover`, blur 24 px, saturation 55 %, brightness 85 %, opacity 55 % over the card's grey, enlarged by 20 % so the blur's edges never show, hidden from the accessibility tree) fills the room beside a tall crop in a **subdued hint of the photo's colours** — a 16:9 crop covers it entirely (H's steps: a flat grey fill v224, desaturated blur at 45 % v229 "washed a red crop to pink", the colours at full strength v230 "match the soft blurred fill with the color of the photo", darkened to 65 % "how it's usually done" v231, then "Mach die Hintergründe bitte dezenter" v234 — the current values; a fill in the card's grey was built on a misread and dropped unmerged). The whole-photo tap keeps the photo's own shape, the linked card's peek uses the box too, the sign card's picture sits in the same box. **Rejected: the photo edge to edge across the card** (v228, the review's item 5 — the box past the card's padding and up to its top edge on Learn; reverted in v229, H: "Nein, Bild in der Box gefiel mir besser" — do not bring it back unasked). **The text box** (`frontBox`): the wide box at the card's width, `FRONT_H` 150 px tall for one photo line, the font capped at `FRONT_FS` 72 px so a short word fills it without growing it (1–3 characters 72 px, 4–8 the usual 58, longer fitted to the width), a second photo line adds its height; until v222 up to three characters sat in a 260 px square (see **Line breaks** and Design). No tag row, no Reveal button — **tapping the character toggles the back on and off; tapping the photo swaps the crop for the whole photo and back** (`S.fullPic`, `frontPic`, reset on the next card). **The tap hints show only while the app is new** (v226, H's "Go" on the design review: "Tap the character to reveal, or the photo for the whole picture." under every card forever is noise): `showHints()` = fewer than `HINT_REVIEWS` 20 reviews all time in the usage counter; the Learn front's hint and the detail's two hints ("… to show / hide the answer …") go together, the card gets shorter, nothing else changes. Back = pinyin with a **speaker button** (the phone's own zh-CN voice via `speechSynthesis`, nothing downloaded; shown whenever the phone can speak at all — v164, H: "the speaker icon doesn't appear on my Android phone": until v163 it showed only with a Chinese voice found, and Android hands the voice list over late and sometimes in two parts, so a "no voice" answer stuck for the session; now `ttsVoice()` looks again on every tap until a voice is found, a tap without one still speaks with lang zh-CN, and the hint "No Chinese voice on this phone — add one under Settings, Text-to-speech output." (`#say-hint`) comes **only when speaking fails** — the utterance's error, or neither start nor error within three seconds — never from the voice list: **H's Xiaomi reports no voices at all and speaks Chinese anyway** (v165, diagnostics "voices (0): none reported", H: "Sound works now, but app shows [the hint]. This is wrong"); Diagnostics lists the voices the phone reports, `voiceList`), meaning, a **parts row** — the card's dictionary words as buttons (from `gloss`/`segs`/`seg`; a single-word card shows its characters instead), tap one for pinyin and meaning, a multi-character word then offers its characters as a sub-row (`cardParts`/`charsHTML`/`charInfo`; **the dictionary and the pinyin library load in the background the moment a back is shown**, in Learn and in the detail — `warmParts` on the revealed render, v235, H's "Go" after "Why loading the dictionary?": until v234 the first word tap of a session waited a second or two while the 2.5 MB `cedict.tsv.gz` parsed, and the line "Loading the dictionary …" read like a network wait — it never was: the file sits in the reader's cache after the first reading or the download under More → Text recognition; the tap still loads on demand when the warm-up did not run, and the loading line shows only when the files are not ready yet; CC-CEDICT clutter like `[Tian1 jin1 shi4]` and `CL:` stripped by `cleanSense`). No word or gloss tables on the back (H: redundant) —, (context word), **the other cards with the same text** (v122, H: "if one character connects to various photos, then link them" — `linkedHTML`/`sameText`: a row "Also on another photo / N other photos" with their crops, newest first, `wireLinks`; on the study back and in the card detail alike — in the card detail a tap opens that card's detail; **in Learn a tap shows that photo on the card in place** and a second tap returns (`S.peek`, the thumb ringed in the tint, reset on the next card; v155, H: "when tapping an 'also on another photo' card, I'm getting out of the learn mode. That should not happen" — until v154 the tap left the session for the Cards tab)), then **Again / Hard / Good / Easy** with interval preview (after grading the next card appears at the top of the screen), and underneath **⚑ Flag for review** and **✎ Edit** (`S.editFrom="study"`, Back/Save return to the revealed card). No example sentences (H's decision). Empty deck → "No cards yet" + Take a photo.
- **Line breaks — the card text follows the photo, automatically (H's rule):** the front never wraps on its own; every word card gets the wide box and the font is fitted to the longest line (`frontLines`, `frontBox`; the 260 px square for up to three characters went in v223). **One exception (v152, H's 16-character escalator sign ran past the card: "statt überlaufende Zeile bitte zweizeilig"):** a photo line too long for the box is shown on two (or more) lines, cut nearest its middle between the card's words (`fitLines`, words from `seg` via `frontWords` or `segs`), while the fitted font would fall under 30 px in the reticle or 22 px in a sign card's text — display only, the stored lines stay; and the box width is measured from the card (`frontWidth`: main's inner width capped at 440, minus the card's padding — until v151 it was guessed from the window and overflowed the card on the phone). Where the lines come from: the reader keeps Tesseract's lines (sign cards: `c` with `\n`; word cards: a `"\n"` token in `seg`, chars carry `ln`); the AI's answer is re-cut to those lines when the model drops the `\n` (`recutLines`: old break offsets, so a corrected character keeps them and an added one only lengthens the last line); a rename keeps them the same way (`segWithBreaks`); the Edit form shows word cards with one strip and one line input per photo line (the hint sentence about it was confusing and is gone). Cards without any line data (before v39) get their lines **estimated once at boot** from the crop's shape (`autoBreaks`: L ≈ √(n·h/w), cut at word boundaries, stored with `lb:"auto"`); cards with real line data are `lb:"photo"` and never touched.
- **Wording and labels** (the consistency passes of v126 and v134, H: "check for consistency throughout the app", "check the app for consistency and professionalism"): every button label in sentence case (Crop / Delete / Cancel in the inbox, Cancel in the preview, Crop / Whole photo in the Add form, "Delete card" in the detail as in the Edit form), the flag checkbox reads "⚑ Flag for review …" everywhere and its note field shows only while the flag is on (the Edit form too since v136), every More row is a full sentence in sentence case with no `·` and no "OCR" ("Progress and cards as one file, via the share sheet.", "Not persistent yet. Install the app so the system keeps the data.", "On: DeepSeek, deepseek-chat. Sends card text only, never photos.", the Review queue says "N cards waiting: …" and its button just "Ask AI", the translation button just "Download", a section "Share" with Share the app and Feedback opens More (v211–v212), the sections are ordered for the learner since v257 (see More in the learner's order), About reads "PWA vN. Works offline. Cards and photos stay on this phone; anonymous usage counts go to the app's owner." since v170), the done screen's button is "Pull the next cards forward", pinyin fields use the UI font (no `mono` — the design rule), the list's "Traditional" and "N photos" pills sit on the pinyin line so the text keeps its room, the "27 of 27" count never wraps, the linked-photos row is centred on the card, and `cleanSense` also strips "(Taiwan pr. …)" and is applied to the composed word-by-word meaning too. Since v134 also: the study hints read "Tap the character to reveal, or the photo for the whole picture." (no `·`), the back's flag action is "⚑ Clear flag" / "⚑ Flag for review" like the detail, the status pills and badges are in sentence case ("New", "Review", "Unverified, translation pending", "Not studied yet.", the SRS line as a sentence), the search placeholder is "Search", the More section is "Start over", the Edit badge "Edit", the preview's fields sit on the photo card's secondary surface like the form's and its Tags field follows the flag as in the Add and Edit forms, the list's pills (Traditional, N photos, tags) have their own small wrapping line under the pinyin, and an AI failure in the picker says "The AI could not be reached." instead of the browser's error text.
- **Questions are the app's own sheet, not the browser's dialog** (v222, H on the card's Delete: the browser's box said "henglicam.github.io says" — "Gehts auch etwas professioneller?", then "Und polish das box layout"; described first and built on "Go"): `askSheet({title,text,ok,danger})` shows a dimmed backdrop and a card that slides up from the bottom (`.ask`, `.sheet`, `askup` 180 ms) with a bold title (19 px), one line of consequence in `--label2` (15 px), a neutral Cancel on `--fill` (`.btn.plain`) and the action button named for what it does — red on soft for a deletion, the filled tint for the import (`danger:false`); Cancel, the backdrop or Escape answer no, the promise resolves true or false. The questions: the card's Delete in the detail and the Edit form ("Delete “学”?" / "The card and its learning progress will be removed." / Delete — **until v267; since v268 the card is deleted at once with Undo**, see above), Photos → Delete N ("Delete 3 old photos?" / "The cards keep their own picture." / Delete), the import ("Import 70 cards and 53 progress entries?" / "Existing entries of the same cards will be overwritten." / Import — `nOf` takes an irregular plural since), Reset ("Start over?" / "All progress, cards and inbox photos on this phone will be deleted." / Delete everything). The plain notices (the clipboard fallbacks, the import's sentence, the errors) were browser alerts until v265 and are the same sheet with OK alone since v266 (`noteSheet`). Suites that go through a delete, an import or the cleanup answer the sheet by overriding `askSheet` with `async () => true` (it is a top-level function, so assignable from the page).
- **Tags** (v133, H: "make the cards sortable, for Chinese class, HSK …" — grouping by own labels, described first and built on "go"): a card carries `tags` (an array of free labels, several allowed; absent when none). A **Tags** field on the Read preview (`stags-<id>`, kept in `sg.tags`), the Add form (`f-tags`, in the draft) and the Edit form (`e-tags`): an input for typing (comma-separated, `parseTags`) plus the labels already in use as chips that toggle (`tagsFieldHTML`/`wireTags`/`allTags`). The Cards tab shows a chip per tag next to Flagged / AI / Unverified (`S.filterTag`, one at a time), the rows show their tags as tinted pills on the pinyin line, and the search finds tags. The Learn tab shows a chip row above the card and the done screen — "All cards" plus one per tag (`learnChipsHTML`/`wireLearnChips`, setting `learnTag`, persisted): `buildQueue` takes only that tag's cards. **Untagged** (v156, H: "provide a default tag for not-tagged cards", described first and built on "Go"): a group, not a label — while tags exist and some cards carry none, the Cards tab shows a chip "Untagged (N)" after the tag chips and the Learn tab a chip "Untagged" after the tags (`UNTAGGED` sentinel in `S.filterTag` / setting `learnTag`, `hasTag`, `untaggedCount`); a card that gets a tag leaves the group by itself, exports are untouched. **The chip rows stay on one line and scroll sideways** (v157, H: "tag list is now 2 lines, takes too much space" — the Cards filter row and the Learn chips, `.chips .chipset` / `.learnchips` with `flex-wrap:nowrap; overflow-x:auto`, no scrollbar, the Learn row centred while it fits); **on Learn the chip row sits at the top edge** and the card or the done screen is centred in the space below it (v158, H: "chips should always be on the upper edge, to avoid distraction from the cards" — auto margins in the centred main, `#main.center>.learnchips`). Tags travel with the export and import like any field. (HSK tagging as an automatic level lookup was built in v45 and removed in v46; these are H's own labels.)
- **Add form (Cards → + New)** (a card from scratch starts without a picture — v188, H: after a photo card, + New opened with the old picture still in it; `saveSign` now clears `S.pendingImg`/`S.pendingFull`/`S.pendingShot` after use and the + New handler clears them too, the photo path still brings its crop in through `cropOk`) (v159, H: "when creating a new card from scratch, please provide a 'draw character' option" and "also add an automatic AI translation function", described first and built on "Do"): under the Characters field a soft button **"Draw a character"** (`#f-draw`, `.btn.mini` right-aligned in `.fieldacts` like the picker's "Not here? Draw it" — v160, H: "it should probably be a button"; v159 had a text link) opens the drawing sheet with the pad alone — no photo square, head "Draw the character below." (`openDrawSheet` with `sg.img` null, `noRef`; an Edit form without an image gets the same) — and the chosen character is appended to the field; **pinyin and meaning fill in by themselves** (`autoFill`, a second after the last change to the characters): while the AI is live it is asked (`aiAsk`, kind word, text only), the moving bar "Checking pinyin and meaning …" under the meaning meanwhile (v200), the fields filled from its answer and `S.draft.ai` remembered so Add card stores `mt:{src:"llm",verified:true}` when the fields were left as filled; without the AI, or when it fails ("The AI could not be reached."), the dictionary and the phrasebook fill them (`lineMeaning`; one dictionary word gives its meaning alone), the red hint "Pinyin and meaning were filled in automatically and are unverified — check the tones and the meaning." shows and the card is stored `mt:{src:"dict",verified:false,pending:true}` for the post-save auto run; fields typed by hand are never overwritten (`pinTouched`/`meanTouched`), a stale answer is dropped when the word changed meanwhile. The empty fields show 你好 / nǐ hǎo / hello as the example (v161, H: "don't always just use shutter or 快门 … it should be more generic and easy to understand for anybody" — 快门 / kuàimén / shutter until v160).
- **Cards:** every card newest first with a 104×58 thumbnail (loaded lazily, v213) of the **crop** (`img`; the whole photo only for cards without one — H, v86, reversing v54; `object-fit: contain`) **in the front's box look** (v232, H: "in der Cards Liste auch" — `.thumbbox` with the hairline and a darkened blurred copy of the crop behind the fitted picture, `.thumbbg`, blur 10 px, the same subdued values as the front since v234; the `img.thumb` keeps its class for the suites) or a glyph tile, pinyin, meaning, ⚑ review / green AI pill and the real SRS status (`due`, `in N d`); a **"N photos" pill** on every card whose text exists on other cards (v122); search over hanzi, pinyin, meanings, context words and flag notes; chips **⚑ Flagged (N)** (flag only), **AI (N)** (waiting suggestions, shown only when there are any) and **Unverified (N)**; **+ New** opens the Add form.
- **Card detail:** no card-type label (the "sign card / word card" badge and the `t` tag in the tags row went in v105 — H: "not relevant"; the badge keeps "unverified / translation pending / reading uncertain", the back says "meaning unverified" without naming a source); front + back preview with the study screen's taps (H: "enable all test functions in the Cards preview" — tap the character to hide and show the answer, `S.detailHide`; tap the photo for the whole picture, `S.fullPic`; both reset on opening a card and on ← Cards; speaker and parts row as on the back), SRS stats, **Test this card** (studies that card and then continues down the list, newest first; ← Cards stops and restores the session queue), **Edit**, **⚑ Flag / Clear flag**, **Share** (one image through the share sheet, v269) and **Delete** (at once, with Undo, v268).
- **Edit form:** **one card** (`.form`, v124 — H: "the edit screen doesn't look like one thing"; the fields, the strip and Save sit on a white/dark card with radius 16, inputs and strip tiles on `--card2`, the picker on `--fill`; Delete card stands below the card; the Add form is the same card), **laid out like the Camera tab's Read preview** (v245, H's to-do "consistency between the Crop and Edit screens", built on the side-by-side: the photo first — the image field at the top of the form, with Crop again's whole photo, its Cancel directly under the photo at the right as the inbox's row, and the preview or result under that —, then Characters, Pinyin, Meaning, Flag, Tags, the AI button where it shows, and **"Save changes" with "Cancel" in one row** (`.cropacts`, `#e-cancel` = Back without saving) like "Save card" and "Cancel"; after a good AI answer nothing — the fields fill, the green "AI: looks right" box of v135 went, H's v105 rule; after a Crop again reading the source line "Meaning composed word by word, unverified" sits under the meaning until the AI answers, as in the preview): the text is edited **like the Read preview** (H): a character strip per line with the picker and the drawing sheet (`SIGN["editN"]` carries the lines and the card's crop as the photo reference; `sg.onChange` lets the form own the re-render and trigger its AI button), the line input below the strip, pinyin following the text unless edited by hand, meaning — both full-width fields that **grow with their text** instead of cutting it off (`textarea.grow`, `autoGrow`/`wireGrow`, also in the Add form; read back as one line — H) —, the image field showing the **crop itself** (not the whole-photo thumbnail — H) with removal, flag; **Crop again** (v239, H: "allow to re-crop a photo in edit mode", described first and built on "Go"): a link beside Remove image, shown only while the whole photo is still on the phone (`fullPhoto(d)` — the inbox photo or the copy); a tap opens the whole photo in place of the crop with the Camera tab's frame layer, **with the frame the card was cut with already drawn** (v244, H: "use the previous cropping area as starting point" — every card cut from a photo since v244 carries `frame` `{x,y,w,h,a}` as fractions of the photo (`frameOf`, set by `saveSign`, `saveNow`, `finishPending` from the saved `reading.rect` and the Edit form's saves; Remove image drops it); `placeFrame(rid,d.frame,{noRead:true})` lays it on the layer once the photo is laid out and shows the preview with "The frame the card was cut with — adjust it to read again." and **no automatic reading** — a corner drag, Read now or Image only start one; **a card without a stored frame — every card saved before v244 — gets its old crop looked up in the photo** (v246, H: "Crop again does not work as specified" — the phone's cards all predate v244, so they had been getting a fresh proposal instead of the previous cropping area: `findFrame(full,d.img)` searches a grey copy of the photo at ≤ 240 px for the crop's position, coarse grid then the pixels around the best, mean difference under 22 counts as found — the crop is a cut of the photo at its own pixels, JPEG twice; a turned frame's cut or a crop from another photo finds nothing; ~100 ms, "Finding the frame …" meanwhile, `CROP.locating`, a Diagnostics line "the old crop found in the photo (difference 4.2)"; the frame found is stored on the card, so the search runs once); **a small frame opens enlarged** (v247, H's photo of a vocabulary sheet whose found frame was a sixth of the photo: "Now that crop area is so small and hard to see and handle. Any suggestions?" — three options offered, "OK, 1"): when the frame is under `WIN_MAX` 45 % of the photo's width, the photo record shows a **window** cut around it — a plain sub-rectangle of the photo at its own pixels (PNG, so the reader's cut stays one JPEG generation), the same fraction of width and height so the layer keeps the photo's shape and the frame's angle, `windowFor`: twice the frame's width or 1.3 × its height, at least 30 % of the photo, centred on the frame and shifted to stay inside — and the frame is laid into the window's coordinates (`toWin`); corners, moving, turning, the preview, the reading with its second look and the picture path run on the window as on a photo. The badge adds "Tap outside the frame for the whole photo."; **a tap outside the frame switches to the whole photo and back** (`onZoom` in `RECROP[rid]`, taken by `wireCrop`'s tap in place of the enlarged view of v169; the frame follows, mapped through the window, `placeFrame` with `silent`; a result under the photo stays with the frame, a running reading lands on it; the window is made anew around the frame as it stands, so a frame moved on the whole photo gets its own window, and a frame grown past 45 % stays whole), the badge then says "Tap outside the frame to enlarge it again." Everything that leaves the form is mapped back to the whole photo — the card's `frame` (`photoFrame`) and the hand-off's `reading.rect` on the photo's pixels (`photoRect`; the record goes back to the whole photo before the background reading, `leaveWindow`) —, so Save, resume and a later Crop again see photo fractions and photo pixels as before. Measured on the 640 × 300 test photo with a frame of 26 % × 33 % around 合同: the window is 52 % of the photo (333 × 156 px, the frame 1.9 × larger on the layer), the toggle maps the frame to within a pixel both ways, Read now in the window reads 合同, the saved frame is the seeded one to four decimals, the hand-off's rect sits on the photo's pixels and the fill lands with the same frame; on H's 1600 px photo a frame of a sixth would be shown about three times larger. Not yet field-checked. Rejected on the way: the frame's handles enlarged (crowded at that size), and a full-screen crop sheet with pinch zoom (option 3, a new screen — not built), and only when nothing is found **the app's proposed frame** (v241, H: "show the crop frame instantly" — `CROP.auto` and `proposeFrame(rid)` as for a fresh photo: "Finding the text …", then the 16:9 or text box, the preview and the reading by itself); adjustable either way, Cancel keeps the old crop) — the photo becomes a photo record outside the inbox (`SHOTS_EXTRA[rid]`, `shotRec(id)` = the inbox's record or that one, used by `cropBlob`, `cropOk`, `cropSign` and `proposeFrame`), `CROP={id:rid}` and the form registers hooks in `RECROP[rid]` (`redraw`: `renderShots` calls it instead of drawing the inbox while such a frame stands, so the zoom tap and the reading's re-renders draw the form's view, `drawRecrop`; `onRead`; `onImage`; `end`) — so drawing, corners, moving, turning, the enlarged view, the preview with Read now and Image only, the 1.2 s wait, the reading with its second look and the picture path all run unchanged, only without Save now (`readingHTML` hides it for a `RECROP` id) and without `S.pendingImg` (the reading keeps its picture on `SIGN[rid].cardImg`). The reading's result goes into the form (`onRead`): the strip, `orig`, confidences, boxes, the straightened crop for the drawing sheet, the alternatives and the traditional verdict, pinyin follows unless typed (`pinTouched`), the meaning takes the word-by-word gloss unless typed (`meanTouched`, new) and the form's own AI button is pressed when the AI is live (`cropSign` skips `signAskAI` for a `RECROP` id, so one call); **the photo and its frame stay open** (v244, H: "don't exit crop mode so fast, do as in the initial crop screen" — until v243 the view closed on the result and the page scrolled to the top): the result sits under the photo as the Camera tab's preview does — the new crop with "Read as “合同条件”. Adjust the frame to read again, or save." (`res` with the frame's `rectKey`, drawn by `drawRecrop` while the frame is unchanged; `RECROP[rid].stage` = idle / waiting / reading, set by `showCropPreview`, `cropSign` and `setResult`), a corner drag re-reads as in the inbox, and the view goes only on Save changes, Cancel, Back, Delete or a tab tap; **Cancel restores** the characters, pinyin, meaning and label from before Crop again (`before`, `restoreBefore`) and keeps the old crop; **the Edit form's AI bar and its error line sit under the Meaning field** since v240 (`#e-aistatus` in the Meaning field, as the Add form's `#f-aistatus` and the preview's bar — until v239 it sat under the AI button at the bottom of the form, far from the field it changes; the busy and background-retry suites read it by id and pass); **the fields grow with a value the app fills in** (v281, H's screenshot of the Edit form after the v280 check: "Mixue Bingcheng (ice-cream and bubble-tea chain)" cut off after the first line — "the meaning needs more than one line": `autoGrow` ran only on typing and at the form's render, and a value set by script fires no input event; now the AI answer's pinyin and meaning, the pinyin that follows the strip, the gloss after a Crop again reading and Cancel's restore all call `autoGrow`; the ai suite mocks a long answer for 识 and checks the field shows it whole, screenshot taken); Image only (`cropOk` → `onImage`) takes the crop alone, the text stays, the view too ("Picture taken, the text stays. Adjust the frame to read again, or save."); a reading that finds nothing shows its line in the box with the frame still there, as in the inbox; Save changes stores the new crop as `img` (`recropImg`, JPEG through `jpegOf`, the thumbnail dropped) with its frame (`recropRect` → `frame`), `shot` stays; a stored frame left untouched saves nothing new; **Save changes while a reading is due or running for the standing frame hands it over** (v241, H: "allow instant saving"; since v244 only while `RECROP[rid].stage` is not idle — a frame already read, or the stored one untouched, saves without a hand-off): the card takes the crop of the standing frame now (`handoff` in the save handler: `cropBlob(rid,rect)` → `img`) and `reading:{rect,at}` beside its old text, the form's hooks go while the photo record stays (`endRecrop` stands back once `PENDING[rid]` is set), `PENDING[rid]` = the card's id and the reading runs on — or starts, when the 1.2 s wait had not passed — and `finishPending` fills the card like a Save-now card (the AI check through `signAskAI`, since the form's hooks are gone; a weak reading flagged "check the reading"); meanwhile the detail's badge says "Reading …" with the line "The new frame is being read — the text follows when it is done." above the back, the Cards row carries a "Reading …" pill beside the old text, and the list or the detail re-renders when the fill lands (`finishPending`/`failPending` render on the Cards tab whenever no form is open); a reading that finds nothing keeps the old text, drops the frame (`reading` gone, v243) and flags "the new frame could not be read — the old text stays" (`failPending`); at the next start `resumePending` takes every card with an unfinished `reading`, text or not — from the inbox photo with the frame, from the photo copied onto the card (`imgFull` as a photo record outside the inbox, `dropExtraShot` when done) or from the crop alone; Back, Save, Delete and a tab tap end the view (`endRecrop`: `abandonReading` — the timer, the run number, the reading's `SIGN` and status, shared with the inbox's Cancel since v243 —, `CROP`, the record and its object URL). Not yet field-checked; the context-word fields show only on cards that already have one; the AI button ("Ask AI to check text, pinyin and meaning") fills text, pinyin and meaning in place — shown only on cards the AI did not verify, or on a verified card once its text was changed here (`#e-aifield`, `showAi`; v135, H: "why ask the AI when everything was made with the AI anyway?"); **Delete card** at the bottom (H) — from the study back the session goes on with the next card, otherwise back to the list. **The Chinese text itself** may change via `applyCardUpdate`: the id stays, so progress, images and queue entries stay where they are (v118; a corrected text that another card already carries is allowed); pinyin/segmentation/gloss are recomputed unless pinyin was edited by hand; editing marks `verified`, clears `pending` and `suspect`.
- **Usage report and the owner's rows** (v162, H shares the app with friends: "track who is using the app, how many times, how many words, how many tokens" and "protect all those administrative functions with a password" — described first, H chose the report through the share sheet over a server, and one master password): the phone counts in setting `usage` — app opens, reviews, cards created by photo and by hand, deleted cards, AI calls with input and output tokens, pictures read by the AI (`pics`, v178 — H: "record that in the user stats too"; bumped on every answered picture call, shown in the Progress row, the report and the daily row as `pics`/`picsMonth`), and **which analysis did the work** (`models{}`, v179 — H: "record all the models used: the reader, DeepSeek, Qwen": `bumpModel` counts "reader" once per reading and every answered AI call by model name — the card check, the picker's candidates, the picture —, all time and this month; the Progress row says "Analyses: on-device reader 12, deepseek-chat 30, qwen3.7-plus 4.", the report has the line "Analyses by model: …", the daily row carries `models` and `modelsMonth` as objects, `workLines`) (read from each answer's `usage` field, OpenAI-style `prompt_tokens`/`completion_tokens`, Claude `input_tokens`/`output_tokens`, `countTokens` in `aiAsk` and `aiCharAlternatives`) — all time and for the current month (`bump`, `usage()`), nothing is sent anywhere. More → Progress shows the learner's dashboard (v274, see above) and a **Share report** button (since v277 an image of the dashboard, `progressImage`/`shareProgress`; until v276 `usageText`/`shareUsage`, a `.txt` via the share sheet with days used, streak, opens, reviews, cards learned and created, AI calls and tokens, provider, phone model — H asked his friends to send it; the daily row made that unnecessary). **The owner's rows open with a password**: Reset, Diagnostics, All users (v197), Mirror and its address, the Text recognition and Offline translation downloads, and since v190 the AI setup form render only while `S.admin` is set (the usage report too until v169) — a section "Advanced settings" at the end of More holds a password field and Unlock (`ADMIN_HASH`, the SHA-256 of the password via `crypto.subtle`, `sha256`), "Wrong password." on a miss, a Lock button when open; unlocked for the session only. It stops taps, not a reader of the source (the hash is public, the app has no server); changing the password is a new version. The password is not written here — H has it. **Usage sharing (v170, H: "Ich möchte die App sharen und user stats bekommen" — automatic reports from every phone; built on "Go" after H had set the project up from the phone):** once a day, while online and the switch is on, the app inserts one row into the table `reports` (`id`, `created_at`, `data jsonb`; row level security with an insert-only policy for the anon role, H reads the rows in the dashboard's table view) of H's free Supabase project — `SHARE_URL` `https://xttuotninuwxnpcbyejn.supabase.co` and the publishable key `SHARE_KEY` sit in the code, the key can only insert. `reportData()`: a random installation id (`installId`, 16 hex digits, setting `installId`, made on first use), app version, phone model from the user agent, first-use day, days used, streak, opens, reviews, cards learned and in the deck, cards by photo and by hand, deleted, AI calls and tokens (all time and this month), provider and model, inbox count, tag count, language, and since v221 `installed` (`isInstalled`: display-mode standalone or Safari's `navigator.standalone`; the usage report text says "Installed on the home screen: yes/no", `privacy.html` names it) — never card text, never photos, never the API key. `sendReport()` runs at boot, on foreground and on `online`, at most once per day (`lastReport` = the day of the last successful send; a failed send leaves the day open, so the next start or reconnect retries; offline = no attempt; `_reporting` stops overlaps); **a second row the same day when cards changed** (v219, built on "Go" after the first all-users report): `bump` of byPhoto, byHand or deleted sets `REPORT_DIRTY`, and when the app goes to the background (`visibilitychange` hidden) `sendReport(true)` posts another row with `keepalive:true`, so the All users list shows the day's cards the same day — until v218 the counts were as of the day's first start, and a friend's first day always read "0 cards" beside live relay calls. **The first all-users report (2026-09-05, 18 rows) and what it taught:** every row is one browser storage, not one person — a link tapped in WeChat opens in WeChat's built-in browser (Android "wv", Windows a plain desktop UA from WeChat for Windows, iPhone plain), which cannot install the app and may hand out a fresh storage on the next tap, so 12 of the 18 rows were "days 0, cards 0" link taps and the real users were H's Xiaomi (95 cards, 31 of 54 readings weak enough for the picture), H's iPad at v171 and three or four friends with relay calls and no card yet; since v219 `reportData` appends "; WeChat" to the device when `inWeChat()` (`MicroMessenger` in the user agent) and Learn shows a line at the top edge, above the chips — "You are inside WeChat. Open this page in your browser to install the app and keep your cards." (`WX_NOTE`, `wxNoteHTML`, `.wxnote` in the tint on `--tint-soft`, on the card, the done screen and the empty deck, nowhere else). The second report the same evening showed both at work — H's Xiaomi with the day's cards, the WeChat tap marked — and H's "18 phones is incorrect" gave the v220 head line above. More → **Usage sharing**, a row for everyone above Photos: the sentence "Sends anonymous usage counts to the app's owner once a day: days used, reviews, cards, AI calls. No card text, no photos.", the state ("Last sent today." / "Last sent 2026-09-03." / "Not sent yet." / "Off. Nothing is sent.", `shareNote`, `#share-status`) and a checkbox **Send once a day** (setting `shareUsage`, absent = on; off stops it at once); the row also shows **"Your id: 6b3aa0b038fe197f."** (`#share-id`, v171 — H: "Show the id under More → Usage sharing", so a friend can tell H which row is theirs; the per-user query in the dashboard groups by `data->>'install'`; **what is used how much, across all phones** (v179, H: "I want to know what's used how much"): the latest row per install in a CTE, then `jsonb_each_text(data->'models')` summed and grouped by key — one line per analysis with the total and the number of phones, `modelsMonth` for this month; rows from before v179 carry no `models` and drop out of it). The **Progress** row now shows the counts and **Share report** to everyone (until v169 behind the password — a friend without it could not send the report); the owner's rows are Reset, Diagnostics, Mirror and the downloads. About reads "PWA vN. Works offline. Cards and photos stay on this phone; anonymous usage counts go to the app's owner." plus, with a picture provider, "Only when the reading is weak, the framed area of a photo goes to Qwen." (v216–v217, field-confirmed: "goes to your AI provider" was wrong on a friend's phone, which has no provider of its own, so `aboutText` names the provider's short name; the relay stays out of About — H: "Do you have to mention the relay?" — the AI row's What-is-sent line and `privacy.html` describe it) and `privacy.html` (updated 5 September 2026) describes the counts, the switch and the random id. Reachability: `https://xttuotninuwxnpcbyejn.supabase.co/rest/v1/` answered H's phone without a VPN ("No API key found in request", screenshot 2026-09-04); the build sandbox cannot reach supabase.co at all, so the first real row is H's to confirm in the dashboard. The Cloudflare Worker fallback was not needed. **The report inside the app (v197–v199, H: "show an all-users report, password protected"; field-confirmed 2026-09-05, "Works!"):** the owner's row **All users** (behind the password, beside Diagnostics) with **Show** and **Share**: the app posts the password it kept in memory since the unlock (`S.adminPw`, dropped on Lock) to H's second edge function `usage-report` (`reportUrl()`, `supabase/functions/usage-report/index.ts`, "Verify JWT" off, since v212 also `{password, what:"feedback"}` for the feedback rows, **no secret**: the function hashes the password and compares it with `PASSWORD_HASH`, the same SHA-256 as the app's `ADMIN_HASH` — a new password is a new hash in both files), which answers with the SQL function `usage_latest()` (`supabase/report.sql`: the latest row per install with today's relay calls from `relay_usage`, security definer, no rights for anon) — the table itself stays unreadable for the publishable key. `allUsersText` — **every label in plain words since v252** (H: "Make all labels easy to understand", after "What does 'the reader' mean?"): "Phones N" with the note "(one line per browser — a phone that opened the link in WeChat and Chrome is counted twice)", then made cards / read a photo, saved no card / only opened the app / opened it inside WeChat / installed on the home screen / used in the last 7 days; "All phones together" with cards, from photos, typed by hand, cards reviewed, AI checks, with the photo, via the owner's key today, and "work done by" with "on-device reader, readings 112", "deepseek-chat, checks 94" — one figure per line, the numbers in one column, every line but the device's within 40 characters (the box's width at 390 px; 46 wrapped); the sections "Phones with cards (N)", "Read a photo, saved no card (N)", "Only opened the app (N)"; a phone's block: "Phone <id>", "app version 196", "installed on the home screen" / "used in the browser", "device …", "cards 94 (80 from photos, 14 typed)", "cards reviewed 310", "days used 12, app opened 40 times", "AI checks 120, 30 with the photo", "photos read 100, 30 of them poorly", "checks via the owner's key today 12", "first used 2026-08-20", "last report 2026-09-06". The same words in the Feedback report ("app version 212", "from phone <id>"), in More → Progress until v273 ("App opened 40 times, 310 cards reviewed, 120 AI checks, 30 photos checked by the AI. Work done by on-device reader 100 readings, deepseek-chat 90 checks." — since v274 the row is the dashboard and these counts live in the shared report only), in the Usage sharing sentence ("days used, cards made and reviewed, AI checks") and in the shared usage report (`usageText`, since v252 in blocks — the head with app version, first used, device and installed; Learning: days used with the streak, app opened, cards learned, cards reviewed; Cards: from photos, typed by hand, deleted, photos waiting in the inbox, tags; AI: set up, checks, photos checked by the AI, tokens sent and received, work done by, this month), `workLines` giving each model its unit; the old words were reviews, AI calls, pictures, relay today, analyses, readings, weak, opens, installs. **Laid out to be read at a glance since v250** (H: "Kannst du bitte die all user stats ein bisschen übersichtlicher machen? Das dauert immer, bis ich da durchsteige"): the head as two short tables with the counts aligned in a column — "Installs 18" with its note line "(one per browser storage, not per person)", then with cards / only tried the reader / only opened the page / in WeChat's browser / installed on a home screen / reported in the last 7 days, and "Totals" with cards (by photo, by hand), reviews, AI calls (pictures), relay today and the analyses one model per line —, then the sections "Users with cards (N)", "Read a photo, no card yet (N)" (until v250 "Tried the reader" — H: "What does 'the reader' mean?"; the app's word for the on-device text recognition, and since v251 the group counts an install whose reader ran at all, `models.reader`, beside the AI and relay calls) and "Only opened the page (N)" as headings with the count, and **one block of short lines per install** instead of one long line: the id, then "v196, installed, Linux; Android 10; K", "cards 94 (80 by photo, 14 by hand)", "reviews 310, days 12, opens 40", "AI 120 calls, 30 pictures", "readings 100, weak 30", "relay today 12", "first 2026-08-20, last 2026-09-06"; every line but the device's stays under 46 characters, so nothing wraps in the 12 px monospace box at 390 px; checked by screenshot in light and dark. Until v249 the head was one sentence — "18 installs (one per browser storage, not per person): 3 with cards, 3 that only tried the reader, 12 that only opened the page; 2 inside WeChat's browser. 2 installed on a home screen, 18 reported in the last 7 days." (v220, H on the second report: "you show 18 phones and that's incorrect" — a row is a browser storage; with cards = cards > 0, tried the reader = AI calls or relay calls today without a card, the rest only opened the page, WeChat from the device string; the row's status line says "N installs, fetched …" too) —, cards by photo and hand, reviews, AI calls and pictures, today's relay calls and the analyses by model summed over the phones, then (v221, H: "All 3 please" on the report proposals, then "Do only 1 for now" — parts 2 and 3, the day-by-day query and the feature and AI-failure counters, were built and reverted unmerged, to come later on their own) **the real users first, by cards**, one line each (id, version, installed or browser, first day, last report, days, opens, cards, reviews, AI calls with pictures, the reader's readings with the weak share "reader 112, weak 63 of 112", relay today, device), the installs that only tried the reader under their own head, and **the rows that only opened the page folded into one line** — "12 installs only opened the page: 8 Windows, 3 iPhone, 1 Android (2 in WeChat)."; Show toggles a `.diag` block, Share sends it as a `.txt`. **A failed answer names its sender:** Supabase's own JWT gate ("Verify JWT with legacy secret" on) and the function's "wrong password" both answer 401, so `fetchAllUsers` reads the body — `Invalid JWT` → "the report function still checks the JWT — switch off \"Verify JWT\" under its Settings in the dashboard", `wrong password` → "the report function does not accept the app password — deploy the current usage-report code from the repo, it carries the password's hash", 404/503 → "the report function is not set up", anything else with the body's first 120 characters — and every failed answer goes to the error log (`logErr("report",…)`) so Diagnostics shows it. Why the hash and not a secret: the function first read a secret `REPORT_PASSWORD` (v197–v198); H set it in the dashboard, re-deployed, and the function still answered "wrong password" on every call while the app's hash proved the phone sent the right password — why the secret never reached the function was not found from the phone, so the secret went and the hash sits in the code (v199), and the report rendered at once. **Reading the rows on the phone:** the dashboard's Table Editor hides the table list on a narrow screen (H saw only "Create a table"), so H uses the SQL Editor (`…/project/xttuotninuwxnpcbyejn/sql/new`): `select created_at, data from reports order by created_at desc;` for the raw list, and a saved per-user query — `select distinct on (data->>'install') …` with the counts cast from `data`, ordered by `data->>'install', created_at desc` — for one line per phone; the id under Usage sharing (v171) is what a friend reads out so H finds their line. (Supabase's project creation was down in Singapore on 2026-09-04; H created the project later the same day.)
- **Share the app** (v210, H: "add a share app function", described first and built on "Go"; v211, H: "place the share function more easy to find, for example on top — and does it really only work with Chrome? I don't think so" — right: the app installs from Safari, Chrome, Edge and Samsung Internet alike, only the menu entry differs): **the first row of the "Share" section** (the first section of More until v274, the second since v275, under Learning) — "Send the link to a friend. The app installs from any browser, no store." with a **Share** button (`shareApp`, `#app-share`): the share sheet with `APP_SHARE_TEXT` ("识字 Zeichentrainer — learn the Chinese characters you see around you. Take a photo of a sign, get the card. Open the link in Safari or Chrome, not inside WeChat, and add it to the home screen:" — the browser sentence since v219, see the first report below) as the text and `APP_URL` as the url — **the text carries no link** (v215, H's WeChat screenshot: the link stood twice in the message, because Android's share sheet appends the url field to the text; v210–v214 had the link in both); without a share sheet the text plus the link is copied and the row says "Link copied.", without a clipboard it shows the link. Nothing is sent by the app itself. A friend behind the wall needs github.io for the first install — the mirror only serves an installed app; the text does not say so. **Field-confirmed 2026-09-05** (H: "Share link works now, only one link in WeChat").
- **Feedback** (v212, H: "add a provide feedback function — of course I must receive this feedback", described first and built on "Go"): the second row of More's "Share" section for everyone — "Tell the app's owner what works and what does not.", a text box (`#fb-text` — since v218 in the forms' field look on the row's `--card2` surface, full width, placeholder in `--label3`, the tint ring on focus, growing with its text through `wireGrow`; `.mrow textarea` in styles.css — H: "looks a little bit old school"; v212 had a bare browser textarea) and **Send** (`sendFeedback`): one row into the table `feedback` of H's Supabase project with the publishable key (insert only, at most 2 000 characters — `supabase/feedback.sql`, run once after report.sql) carrying the message, the app version and the installation id — no name, no cards, no photos unless typed; "Thank you, sent." and the box empties, "Write a few words first." on an empty box, "No connection. Try again when online." offline with the text kept, "Could not send: the feedback table is not set up" on a 404 (every failure in the error log as `feedback`). **The owner's row Feedback** (behind the password, under All users, `#fb-show`/`#fb-share`/`#fb-out`): Show lists the messages newest first (`feedbackText`, laid out like the All users report since v251 — H: "Same for feedback": a head with the day and "messages N (newest first, up to 500)", then one block per message with a blank line between — "2026-09-05 10:00, v212", "from <id>" and the message indented; until v250 the blocks ran together on one meta line each), Share sends them as a `.txt` (`shareFeedback`); both go through the report function with `what:"feedback"`, which calls the SQL function `feedback_list()` (the latest 500) — `fetchReport(what)` carries the v198 error mapping for both rows, `FEEDBACK` is cached like `USERS` and dropped on Lock. H re-deploys the report function once and runs `feedback.sql` once (done 2026-09-05); `privacy.html` has the bullet. **Field-confirmed 2026-09-05** (H: "Feedback works, message arrived in the owner's row").
- **Review flag:** `flag:true` / `flagNote` on the card; included in export. More → **Flagged cards → Show** opens the Cards list with the Flagged filter on; **Share** sends a plain-text list via the share sheet (`.txt`) for a teacher.
- **Export/import** (More): progress + cards as JSON, `zeichentrainer-YYYY-MM-DD.json.txt` via the share sheet; import validates by content and upserts by `id`, keeping local images (exports before v118 carry the text as the key and get it as the id). **Photos in the export** (v166, H: "could you also export photos? Now only text", described first, H chose crops and whole photos): a checkbox "Include photos (adds about N MB)" under the Export row (setting `exportPhotos`, `photoBytes` × 1.37 for the base64) puts every card's crop and whole photo into the file as base64 (`imgB64`/`imgFullB64` with their type, `blobToB64`, the file then unindented, `photos:true`) and Import turns them back into blobs (`b64ToBlob`, the thumbnail cache dropped); a file without photos keeps the photos already on the phone, as before. Roughly 3 MB for 70 crops, 15–30 MB with the whole photos; WeChat takes files up to 100 MB. Field-confirmed (H's 70 cards, 19.5 MB through WeChat, the photos on the other phone: "OK works"). **An older app imports such a file without a word and without the photos** — the first try landed on a phone still at v165; the receiving app must be at v166 or later (More → About), then the same file imported again restores them. Since v167 the import ends with one sentence on what it did — "Imported 70 cards and 53 progress entries, 68 with photos.", "(N could not be read)" when a photo failed to decode, "The file carries no photos; the photos on this phone were kept." for a text-only file — so a silent loss cannot happen again. The Export row shows **"Last export: N days ago"** (setting `lastExport`), red after 30 days or when never exported — the cards exist only on this phone. **Photos** row: inbox count, and a **Delete N** button for photos older than 30 days that already became a card (cards carry `shot:<inbox id>`; the cards keep their crop, and the whole photo is copied onto them just before the deletion — v214, `keepPhoto`). **Reset** wipes cards, progress and photos. **Diagnostics** (v93, H: "crop draws, nothing happens afterwards" could not be reproduced in Chromium — the phone has to tell): the last 20 uncaught errors and unhandled rejections (`ERRLOG`, `logErr`, persisted in setting `errlog`), the steps of the last reading with their times (`READLOG`) and its competing passes (`LAST_READ.passes`), the last three **drawings** (`DRAWLOG`, v140 — the strokes as drawn, rounded to pixels of the 720 px pad, and what the model answered; H: "I feel no improvement" after v139, and the synthetic strokes of the harness are not a finger — the phone's own strokes are the only field data), the last three **AI exchanges** — request payload and raw reply, never the key (`AILOG`, `logAi`, v97: the phone is the only place DeepSeek can be watched from; it once returned the Chinese text as the meaning and waved 二和玖 through) —, plus page/script version, online state, AI provider (never the key; since v267 with "(text deepseek)" when the text goes elsewhere than the active provider), service-worker presence and user agent — **the reading log keeps its steps since v285** (H's diagnostics of the SF Express card, 2026-09-07: forty lines of "recognizing … 61%" and nothing else — the reader's progress lines had pushed every step and the proposed-frame line out of the 40-entry log, and `cropSign` had wiped the proposal line at the reading's start anyway): a progress line now overwrites the previous progress line (`readingStatus`), and the frame's own lines — "frame proposed by the app: …" and "the old crop found in the photo …", marked `pre` — stay at the head of the new reading's log; the diag suite expects the proposal first, "cutting out the frame …" second and no two progress lines in a row; the autoframe, twopass, recrop and english suites pass — **the last reading's steps and passes survive a restart since v267** (setting `readlog`, written by `saveReadLog` 0.8 s after each step and after the competition, restored at boot into `READLOG`/`LAST_READ` — H's first diagnostics after the v266 update said "Last reading (0 steps)": the update's reload had emptied the in-memory log; the diag suite reloads and finds the steps) — **Show** expands the text in place (`.diag`), **Share** sends it as a `.txt` via the share sheet (`shareDiag`; a third share-sheet dance on purpose, the two others keep their own texts).

### Field lessons that shaped the UI (keep)
- **Flow and UI:** four free corners on the crop frame with a perspective warp of the cut were built in v127 (H: characters photographed from the side are not recognised) and taken out again in v128 on H's request ("No, I don't want this. Leave it as it was.") — the frame is a rectangle with resizing corners; do not bring the quadrilateral back unasked · No tab switching for the AI (v31/v32) · button always visible, never replaced by the auto result (v32) · one action after framing (v39) · plain words instead of jargon and `·` shorthand (v25/v39) · three top-bar pills overflow at 390 px, so Deck only shows off the Learn tab (v25) · Chrome/Android does not repaint after the camera until touched (v34) · the card text keeps the photo's lines, a horizontal word is one line (v42) · reading starts by itself after the frame is released, no Read tap (v47) · the crop frame stays on the photo until Save (v50) · after grading the next card must start at the top of the screen (v53) · two taps on the front: photo = whole picture, character = answer (v55) · the static HTML must already match the first render (Deck pill hidden in index.html), otherwise the top bar jumps at load (v57) · `html` needs the page background, a `color-scheme` and an inline fallback in index.html, otherwise a line in the wrong colour flashes above the page at load (v58, kept for light and dark in v82).
- **Storage, deploys and network:** Android shares `.txt` but not `.json` (v3) · a cached 404 poisoned the dictionary once (SW caches only OK responses) · status text written into a DOM node captured before a re-render disappears silently — keep status in state and re-query (v47) · the inbox re-render previews only the inbox's readings — the Edit form's text state survives a tab tap, and previewing it before the pinyin library is loaded threw "pinyinPro is not defined" into every reading (v108) · github.io is unreliable from China, jsDelivr is not (v48/v49) · the reader's files must be reachable without the VPN too, hence the mirror fallback for `vendor/` (v56) · a new worker must fetch the shell past the browser's HTTP cache, or rapid deploys leave the old app under a new cache name (v71) · the script carries its own version and checks it against the page's label at boot, so a mixed shell repairs itself (v72) · "the frame draws, nothing happens afterwards" on v92 was not reproducible in Chromium and vanished with the v93 update (VPN on) — cause never seen, most likely a mixed shell from the mirror, which caches each file on its own; first ask for More → Diagnostics before touching the reader (v93) · a reading that a corner drag superseded must abandon, or two results and two AI requests land on the same frame (v117) · the error log persists across updates, so an error in Diagnostics may come from an older script — compare the stack's line numbers with the current file before chasing it (v117) · an older app imports a file with fields it does not know without a word — every new export field needs the import to say what it did (v166–v167, the photos lost on a phone at v165) · Chrome's voice list says nothing about whether the phone can speak — the Xiaomi lists no voices and speaks Chinese (v165) · a tap on a layer that lets the page scroll must be read from the click event, not from pointerup — iOS Safari sends pointercancel there (v206, H's iPhone; the frame's own handles carry touch-action none and are unaffected) · an installed app keeps its old manifest for days; a new share entry needs a re-install through "Install app", and a home-screen shortcut never gets one (v167) · an app that holds one API key loses it the moment another provider is chosen — H's DeepSeek key had to survive the Qwen setup, hence one account per provider with the old key migrated (v172) · Android's share sheet appends the url field to the shared text, so the text must not carry the link itself, or WeChat shows it twice (v215) · a Blob put into two IndexedDB stores is stored twice — the inbox photo copied onto the card as `imgFull` doubled every phone's photo bytes until v214; keep one copy and a reference, copy only when the original goes · a long-running action must keep its state outside the row it was started from — a tab change re-renders the screen, the old row's status goes nowhere and a "running" guard swallows the next tap (v257, Translate all: "changed page, pressed again and no reaction"; the same rule as v47's status text in state).
- **The reader:** a tilted frame is straightened before reading — Tesseract fails beyond a few degrees (v59) · Tesseract's per-symbol boxes are not to be trusted for Chinese: the heights are right, the horizontal ends drift, stop early on the last character or lose it altogether — take the line's ends from the image columns (v69/v75) · **what the reader sees must be a JPEG** — this Tesseract build misreads large canvas PNGs and WebPs (same pixels: JPEG 0.95 → the full line at 99 %, PNG → one character); intermediates stay PNG so only the last step is lossy — two JPEG generations in a row lost the line too (`READ_JPEG`, `deskewBlob` always hands back a JPEG, `jpegOf` re-encodes card images to JPEG 0.85 at save) · the reader is chaotic on large text: the same clean crop reads perfectly at one scale and as garbage at another, so read at several scales and let the readings compete (v73) · light text on a strong colour defeats the reader's own thresholding — a binarised copy reads it (v77) · shading (a curved bottle) defeats every brightness threshold, a chromaticity copy does not care (v95) · the score must not reward a soup of short lines, and a band only one pass saw or that is tiny beside the others is decoration (v95) · the AI must be allowed to say "unreadable" — asked only to correct, it invents (v95) · a traditional logo needs the traditional model, the simplified one can only approximate (v96) · confidence must weigh more than character count, and a text several passes agree on beats a lone near-equal score (v96) · the AI sees every competing reading, not just the winner (v96) · the AI may fix what was read weakly or what another reading offers, never a character every pass read clearly — asked for a sign, it answers with a common phrase (减展单车 → 共享单车, v143) · when the readings circle around one dictionary word, the dictionary decides before the AI is asked — the AI refused 养浴多 / 次乐多 / 开乐多 twice (v99) · every gate must judge by the same score as the competition, or a long soup of fragments switches the fallbacks off (v100) · the ink height must ignore solid decoration, or the text looks like fragments (v100) · a single line whose characters are as tall as the frame's ink is the text, however short — the fragment penalties are for soups beside real text, not for a lone 推 that ten passes agree on (v116) · a white character on a black plate has no chromaticity, so the chromaticity copies read noise there — they must lose, not be skipped (v116) · a frame one text height tall cannot hold two lines — a second "line" in it is decoration, and the pass must lose score for it, not be rewritten (v137) · bold outlined characters fill a tight frame's rows almost solid — the ink height must let dense rows ride inside a text band, or a one-line sticker reads as a twelve-line soup (v144) · whatever fills the corners of a rotated crop lands in every copy and in the ink height — it must be the crop's own dominant colour, not an edge pixel, not a median, not the border (v138) · six stroke edges per text row is the rule that keeps stripes out; four was tried for big characters and lost 业主直租 through a changed ink height (v138). · **the stroke medians do not read photos** (tried after v142, H: "can the stroke analysis also improve the word recognition in the photo?"): a shape re-ranker was measured in the harness — at every character position the candidates (the reading, the other passes, the dictionary, the true character) were compared with the binarised, thinned photo glyph at `charBox` by symmetric chamfer distance, truncated chamfer and the overlap of the medians drawn at the photo's stroke width, with a small shift-and-scale search. On 56 positions of 14 images the best measure kept 29 right characters, broke 24 and fixed 1; even on the clean print signs it broke 5 of 22, and the clean 台 of a bold banner lost to 后, 富 and 特. The medians are one 楷 style, the signs are bold, rounded and brush fonts, and the glyph box carries the neighbours, a logo oval or speckled stone. Both of H's actual failures (美团 read as 人美加, 减震单车 as 减震单生;) were wrong in length, where a per-position re-ranker cannot act at all. Not built; the matcher stays in the drawing sheet, where the strokes are real. · **A rounded bold display font can be a model limit — check the clean copy before touching the reader** (v169, H's 奈斯餐吧: gold rounded characters on a shaded blue panel, first framed loosely under the Latin "NESTO", then tightly around the four characters; H: "Why can't he figure this out?", "Still nonsense"): the loose frame reads two-line soups on the phone and in the harness — the Latin letters as a Chinese line (和人叨时) above the text as garbage — and the tight frame gives 广包看中 on the phone, 本了了 in the harness (a blob of the colour copy at 95 %). The deskew is right (the text sits at −9.5° in the frame, the straightened black-and-white copy is a clean, perfectly legible 奈斯餐吧), and the model still reads that clean copy as 夫斯均肥 / 雯斯豆妈 / 索斯餐妈 / 去斯餐凤 at six sizes from 80 to 370 px: 斯 always, 餐 half the time, 奈 and 吧 never; thinning the strokes makes it worse, the traditional reader gives 泰新宴吧 / 素箕窒旋. So no copy, size or angle helps — the model does not know this font's 奈 and 吧. The AI then invented "Guangbao Kanzhong (possibly a shop or brand name)" for 广包看中 instead of calling it bad. What would read it: a vision model that sees the crop — ruled out by H's own rule that photos never leave the phone, so it is H's decision, not a reader change. Not built.
- **Fixing a character and drawing:** one misread character is fixed by tapping it, not by retyping the line; the AI is asked first, the dictionary is the fallback (v61) · candidates first, drawing as the second level when the right character is missing; the photo character stays in view while drawing, because the system keyboard's handwriting pad hid it (v62) · the Tesseract worker has **one** logger: progress goes to `_ocrLog`, the handler of the running job, never to the status closure of whoever created the worker — the pad's reading once wrote "recognizing … 100%" into the photo's box and wiped the editor (v63) · action rows in the picker are flex (`.ckacts`), not the auto-fit grid, which broke the pad's buttons (v63) · the picker is two levels only, suggestions then drawing; no type field in it (v64) · the word-picking overlay (pinyin boxes on the photo, selection bar) went unused once the AI-first preview existed — removed with its Split into words button (v91) · inserting a character through the picker (+ before / + after, a trailing + on the strip) did not convince H in v91 and was removed in v92 — asked for again in v119 together with a Remove that can be found: a red button in the action row, not a text link at the right edge · the drawing pad is a full-screen sheet with a Done button — an inline pad below the fold went unseen, and reading after each stroke felt like being cut off (v65) · an automatic crop is a guess — let the hand correct it (drag/pinch on the photo square) (v70) · small black-on-white pad images are indifferent to PNG/JPEG (measured) (v73).

## Hard constraints (learned in the field — do not violate)
1. **No external dependencies / CDNs.** Must run offline and behind the GFW. System CJK fonts only. Libraries and data go into `vendor/` (licenses in `vendor/LICENSES.txt`).
2. **All paths relative** (`./…`). The app lives under a subpath (`/zeichentrainer/`).
3. **Persistence only via IndexedDB** (do not use localStorage/sessionStorage). `navigator.storage.persist()` is requested — Xiaomi/MIUI aggressively evicts storage of non-installed sites. The SW must **never cache non-OK responses** — a 404 cached during a deploy window poisoned the dictionary permanently once; `loadDict` additionally heals such entries.
4. **Phone-only deploys:** H uses GitHub only in the phone browser. Ship changes as small, clearly described PRs. As few files as possible; generate/commit binaries (icons, vendor data) in the repo, never ask H to upload them.
5. **Privacy rule:** Confidential text (real contracts, suppliers, company internals) must **never** end up in the public deck or repo. General vocabulary only. H's provider keys live in the Supabase function's secrets, never in the code (v191). Photos and card images stay on the device — the one exception (v173): the straightened framed area, at most 800 px, goes to a picture-capable AI provider when the reading is weak and H's switch is on, see the AI section; the daily usage row (v170) carries counts only.
6. **Files leave the device only via the share sheet:** programmatic blob downloads (`<a download>`) are silently blocked by Android/MIUI. Use `navigator.share` with a file — and Chrome/Android only shares whitelisted types (`.txt`/`.csv`/images/PDF yes, **`.json` no**), hence the `.json.txt` extension with `text/plain`.

## Design (iOS-style since v82, on H's request: "orient on a modern Apple app")
The lacquerware look (INK/BONE/VERM/JADE) was replaced in v82 by the treatment of a current iOS app. Light and dark follow the phone (`prefers-color-scheme`; `color-scheme: light dark`, two `theme-color` metas, inline fallback background in index.html so nothing flashes at load). The seal red stays as the tint, the big characters keep the serif.
- Tokens in `styles.css` (light / dark): `--bg` #F2F2F7 / #000 · `--card` #FFF / #1C1C1E · `--card2` #F2F2F7 / #2C2C2E · `--fill` #E9E9EE / #3A3A3C · `--label` #000 / #FFF · `--label2` #6E6E73 / #A1A1A6 · `--label3` #AEAEB2 / #6E6E73 · `--sep` hairline · `--tint` #C8372D / #E0483E (+ `--tint-soft`) · `--ok` #2FA36B / #3DBE7A (+ `--ok-soft`, for AI and verified) · `--bar` translucent bars · `--shadow` (light only). Radii: cards 16 (`--rc`), buttons and inputs 12/10 (`--r`).
- Fonts: UI = Apple system stack (`-apple-system`, SF Pro, PingFang SC, then Roboto/system-ui on Android); Hanzi = `'Songti SC','STSong','Noto Serif CJK SC','Source Han Serif SC','SimSun',serif` for the big characters, strips and rows; `--mono` only for timestamps and Diagnostics (the version label is hidden since v225). Pinyin is set in the UI font, semibold.
- Components: large title `识字` in the sticky, blurred top bar (no version label beside it since v225; the version sits in More → About) with stat capsules (Due/Done on Learn, Deck elsewhere — three overflow at 390 px) · translucent bottom tab bar, active tab in the tint · the study card and the done screen are white/dark cards with radius 16 · the **田字格 reticle** stays the signature element (since v223 one shape for every word card: the wide box at the card's width, 150 px tall per photo line, on `--card2`, hairline lines and corner ticks, dashed tint crosshair only for single characters; font fitted to the longest photo line — `frontLines`/`frontBox`; base font by character count 1→150px, 2→104, 3→74, 4–8→58, 9–12→44, 13+→34, capped at 72 px so the box keeps its height — the 260 px square for up to three characters went on H's "Bitte consistency!") · lists (Cards, More) are inset grouped rows with hairline separators and rounded ends · buttons: `.btn.primary` filled tint with white text, `.btn` tinted soft, `.btn.danger` red on soft, `.del` plain tint text, grades as four `--card2` tiles · inputs are white/dark fields with a hairline inset, tint ring on focus · chips are `--fill` capsules, active = filled tint · the character strip buttons (`.ck`) are white/dark tiles with a hairline; low confidence = tint ring, AI candidate = green ring, chosen = filled tint (`.signed .ck.on` — the surface rule once outranked it and the chosen character showed white on white) · the reading bar (`.reading .bar`) is a 4 px `--fill` track with a tint segment sliding across.
- The script draws nothing in fixed colours: inline SVG uses `style="stroke:var(--…)"`, canvases read the tokens at paint time (`cssVar`) — the drawing pad's strokes are `--label`, its grid `--sep`, the photo view's padding `--fill`.
- The crop frame is independent of the theme: white dashed edge with a dark outline and white handles, nothing dimmed — it must stay visible on any photo colour.
- Readability rules kept from the old design: body 17 px, hints and labels 13–14 px in `--label2`, meanings 18 px, touch targets ≥ 44 px, inputs ≥ 16 px (no zoom), safe-area padding top and bottom, plain words instead of jargon.

## Didactics / SRS
- Loop: photo + character/word → tap to reveal → pinyin (+ pronunciation), meaning, (context word). No example sentences (H's decision).
- SM-2 light, grades `again / hard / good / easy` with interval preview. `again` re-queues the card within the same session.
- Session = due cards + max. 8 new ones. "Pull forward" pulls future cards when nothing is due. **Card order** (v153, H: "provide choices for the order in which the flash cards are shown", described first and built on "Go"): More → Learning → "Card order" with the chips Oldest first (the old order: due cards by due date, new ones by creation), Newest first (both groups by creation, newest first) and Random (a fresh shuffle per session); due cards always come before new ones, the up to eight new cards are picked in that order, a tap rebuilds the session at once (setting `learnOrder`, `LEARN_ORDERS`, `orderCards` in `buildQueue`).

## Deck format
No built-in deck; all cards live in IndexedDB `custom`. Schema:
```
{ id:"坚果", c:"坚果", p:"jiānguǒ", m:"nuts", t:"Custom", at:1756800000000,   // id: the key (text while free, else 坚果#<at>) · t: Custom | Sign · at: creation time
  tags:["Chinese class","HSK 3"],     // optional: H's own labels (v133) — the Cards filter and the Learn filter
  seg:["坚果","\n","供应"],           // optional: word boundaries + photo line breaks for the front
  lb:"photo"|"auto",                    // where the line breaks came from (auto = estimated from the crop's shape)
  w:"…", wp:"…", wm:"…",              // optional: context word + pinyin + meaning
  shot:"shot_1756800000000",           // inbox photo the card came from (photo cleanup)
  mt:{ src:"llm"|"dict"|"phrasebook"|"nmt"|"gloss", verified:true|false, pending:false, suspect:"reason" },
  flag:true, flagNote:"…",            // review flag
  alts:["养兴多","义乐多"],               // optional: the other readings of the photo (v96), for the AI check
  trad:"養樂多",                          // optional: the text as it stands on the photo, in traditional characters (v101); shown on the front, c stays simplified
  ml:"de",                               // the language of the meaning (v256) — absent = English: the dictionary, the phrasebook, the offline model and every card before v256
  ms:{ en:"nuts", de:"Nüsse" },          // every meaning the card got, by language (v265) — the app shows the one for its language when the card has it; a changed text keeps only the current one
  ai:{ zh, p, m, note, ok, bad, at, model }, // a waiting AI suggestion (bad: the AI calls the text OCR garbage)
  frame:{ x:0.02, y:0.52, w:0.96, h:0.46, a:0 }, // the frame the crop was cut with, as fractions of the photo (v244) — Crop again starts from it; cards before v244 have none and get the app's proposal
  reading:{ rect, at, failed }              // a card saved before its reading was done (v237, id reading#<at>): the frame it was saved with; c is empty until the reading fills it, failed = why it never did
}
```
Sign cards add `kind:"sign"`, `segs` (per line) and `gloss` (`[{w,p,m}]`, feeds the parts row); word cards from the Read path carry `gloss` too; any card saved from a photo carries `img` (crop) and `shot` (the inbox photo's id); **the whole photo is stored once** (v214, H's "Go" on the v213 audit's storage finding): `imgFull` exists only when the inbox photo is gone — `fullPhoto(d)` = `imgFull`, else the inbox photo through `shot` (the front toggle, the detail, the list thumbnail of a card without a crop — not repeated on the back, H); `saveSign` stores no copy while the inbox photo exists, `delShot` (the inbox's Delete, Photos → Delete N) first copies the photo into every card that references it (`keepPhoto`), `dedupePhotos` at boot drops the copy from cards of v213 and earlier whose inbox photo is still there (and the import drops it the same way), the export takes the whole photo from `fullPhoto`, so files stay the same, `photoBytes` counts it once, and the Edit form's Remove image drops `shot` too (until v213 the front still showed the inbox photo after the removal); saves about half of a phone's photo bytes (field-confirmed 2026-09-05, H: "Look both good" on v214 and the About line) — blobs, local only; in the export only with the "Include photos" checkbox, as `imgB64`/`imgFullB64` (v166).
Rules: **Pinyin only verified, with correct tones — never guess.** Meanings in English, or in the app's language from the AI (`ml`, v256). Dictionary/phrasebook prefills are `verified:false` until a human or the AI checked them; when unsure, flag instead of inventing.
New words come from photos (menus, signs, packaging).

## Roadmap
Done and verified on the device: PWA install · offline operation · export/import · OCR v2 (crop-first, word segmentation, card images) · sign cards M1 · online AI review with DeepSeek (T3) · AI-first Read flow · updates through the jsDelivr mirror without VPN · the reader's fallbacks on hard photos (v95–v100: the Yakult logo 養樂多 ends as 养乐多 "Yakult" by dictionary consensus, the granite sign 等候区 as "waiting area", both on H's phone) · the traditional form on the card (v101–v112, seen in H's screenshots) · a single character that fills the frame (v116–v117: H's door plate 推 lost to a soup of fragments, and two readings of one frame asked the AI twice — both fixed and confirmed on the phone) · the same text saved again from another photo as its own card (v118, the second 拉 on H's phone) · the frame that stays and lets swipes scroll (v129–v132, H: "It works now") · Diagnostics with the AI exchanges (v93–v97, the only window into the phone — every reader fix since came from a shared diagnostics text). · the AI guard on clearly read characters and the ink height on bold outlined text (v143–v144, H's bicycle sticker 减震单车: "OK. Works.") · the traditional mark by hand and the Untagged group with its chip rows (v146–v157, seen in H's screenshots: the link, the switch vanishing on 工作, the two-line chip row that then scrolled). · the speaker button and the export with photos (v164–v166: "Sound works now", "OK works" after the receiving phone had updated) · the app re-installed as a proper web app from Chrome's "Install app" (v167, after the share entry had not appeared; a photo then reached the inbox in the installed app — whether through the share sheet, H has not said) · the picture path with Qwen (v173: 奈斯餐吧 read from the picture on H's phone, VPN off) · the owner's relay (v191: 焦香蜜汁肠 read through the relay on H's phone without a key) · the all-users report (v197–v199, once the function checked the password by hash: "Works!") · the feedback row and the owner's row that reads it (v212, "Feedback works, message arrived in the owner's row"; the polished box of v218 is not confirmed in words) · Share the app through WeChat with the link once (v210–v215, "Share link works now, only one link in WeChat") · the whole photo stored once (v214) and the About line naming the AI (v216–v217), both "Look both good" (2026-09-05) · the second daily row after cards changed and "WeChat" in the device (v219, seen in the second all-users report the same evening: H's Xiaomi listed with the day's 106 cards, H's own tap inside WeChat marked) · the All users and Feedback reports laid out in tables and blocks with every label in plain words (v250–v252, H: "Reports look good") · **the language stretch and what followed it, v253–v266** — the six languages in the relaxed tone, the meanings from the AI with Translate all (staged, resumed, retargeted on a switch), one meaning per language on the card, the pill, More in the learner's order, the guide, DeepSeek for the text, the notices as the app's sheet and the empty deck's pointer to the guide (H, 2026-09-06 evening: "Looks all good" — a broad look, not every screen named; the field lessons on the way were the standing counter on 99 Korean cards and the second tap after a page change) · undo after Delete and Share a card (v268–v269, H the same evening: "Both look fine").
Built, not yet field-checked: the Progress dashboard (v274 — do the tiles, the 30-day strip and the deck bar read well on the Xiaomi, and is the strip's shading visible?) · the app's errors in the daily row and the All users report (v271 — does the first row from the Xiaomi carry `errors`, and does the section read well on the phone?) · from the language stretch (v253–v266, "Looks all good" as a whole) the details H has not named — a friend's phone opening in its own language, the tone for a French or Korean friend, the guide's six sections against a friend's first questions, a Japanese or Korean meaning from the AI · the browser's long-press menus, the selection toolbar on controls, double-tap zoom and the keyboard's autocorrect on the pinyin and characters fields all kept off (v248–v249 — does a long press on the Xiaomi stay quiet everywhere now, and does the pinyin field stop capitalising?) · a small frame opened enlarged in Crop again, with the tap outside the frame switching to the whole photo and back (v247 — is the window big enough on the vocabulary sheet, and does the toggle feel right beside the frame's own handles?) · an old card's crop found in its photo for Crop again (v246 — the fix for H's "does not work as specified"; if it still differs on the phone, a screenshot and Diagnostics' reading steps tell whether the crop was found) · the Edit form laid out like the Read preview and every background-filled card flagged with its note (v245) · Crop again in the Edit form (v239–v244 — the card's own frame at once, the view that stays open with the result under the photo, Cancel restoring the text, Save changes during the reading; is the frame comfortable inside the form, and does the reading's text replace the old one as expected?) · Save now (v237–v238 — a card saved from the reading bar, filled in the background, the detail's waiting state with the bar in the box; does the reading survive a tab switch on the Xiaomi, and is the flag on weak readings enough of a hint?) · the faster reading (v236 — does the Xiaomi read the same, and how long does a hard photo take now?) · the dictionary warmed on reveal (v235 — is the first word tap instant on the Xiaomi?) · the design review's pass on the front (v223–v232 — the two boxes of one shape on every card, the subdued blurred fill beside a tall crop, on the front and in the Cards list's thumbnails (v234), the Traditional pill on the front with the simplified form on the back, the tap hints gone after 20 reviews, the header without the version label; does the blurred fill look right on real photos, and are the 16:9 previews uniform enough?) · the question sheet in place of the browser's dialog (v222) · the WeChat line on Learn and the share text naming Safari or Chrome (v219) · the All users head line counting installs by what they did (v220) and the report with the real users first, installed or browser, opens, first day, the weak share and the lookers folded (v221) · the switch shown only after a traditional detection (v150) · the card image as framed, unrotated (v145) · the front's wrap of overlong photo lines and the box width from the card (v152) · the card order in Learn (v153) · the hidden AI remark (v154) · the linked photo shown in place in Learn (v155) · the Learn chips at the top edge (v158) · the Add form's draw button and its automatic pinyin and meaning (v159–v161) · the usage report and the owner's password (v162; H asked where the stats are — they sit behind the password under More → Progress) · the share target for screenshots (v163; the entry was missing until the app was re-installed as a web app in v167 — Chrome does not refresh an installed app's manifest promptly, and a home-screen shortcut never gets share entries; the first shared screenshot is not confirmed in H's words) · the import sentence (v167) · the usage sharing and the id in its row (v170–v171 — the first row from H's phone in the dashboard is the check; H's phone was at v169 when the table was last looked at, still empty) · the provider chips with one account each (v172 — H's DeepSeek key survived and the Qwen key went in beside it: the v173 picture call ran as Qwen while DeepSeek stayed, seen in H's diagnostics "AI qwen live" — the migration is field-confirmed, the chip row itself not in H's words) · the whole frame to the picture model on a two-line sticker (v175 — H's 骑车勿盯 / 还车勿忘 came back as one line in v173; the fix is not yet seen on the phone) · the small AI mark on the Characters label (v176–v177) · the picture and per-model counters in Progress, the report and the daily row (v178–v179) · the busy bar under the meaning (v200) · the AI request tried again after the app comes back to the foreground (v201) · Qwen's thinking off on the text check and the picker, and the call duration in Diagnostics (v208 — the next diagnostics text says how long a check takes) · the pool of readers and the clear-reading exit (v209 — how long a reading takes on the Xiaomi now, and whether three workers fit its memory; Diagnostics' reading steps carry the times) · the frame proposed by the app on a photo that opens by itself, widened to 16:9 where the photo allows (v203/v207 — is the whole-photo proposal on a cluttered photo a nuisance, does the fence-and-sign case need a tighter rule, and do the 16:9 previews look uniform enough in the list?) · several characters removed at once, marked by tap or by a stroke along the strip (v204–v205) · the turning frame (v185 — the handle under a finger, and whether H turns frames at all) · the tap outside the frame that enlarges the framed area (v169 on Android, not confirmed in H's words; v206 on the iPhone, where the pointerup version did nothing — the click version is not yet field-checked) · vendor files through the mirror (does text recognition work without the VPN now?) · deskew of tilted frames (v59) · per-character correction (v61 — are the character buttons comfortable on long sign lines, where the strip wraps?) · adding and removing characters in the picker, the × and the second tap that close it (v119–v125) · the line input in the Read preview (v120) · the linked cards with the same text (v122) · the forms as one card (v124) · the tags and the Learn filter (v133) · the stroke matcher in the drawing sheet (v141 — H's three 团 drawings match in the harness; the phone has to confirm on other characters; is the automatic photo crop usually right, or does H drag it every time?) · the second look, the merged readings and the image-based line ends (v73–v75 — 首都铁骑 reads in full since v75; does the Maotai-style brush font on a bottle ever read? if not, that is a font/model limit, not a framing problem — the first such limit confirmed on the phone is the thin gold 奈斯餐吧 of v169, see the field lessons) · the binarised copy on 业主直租 (v77 — the screenshot crop reads, the phone's own photo not yet tried) · the Yakult line 活菌型乳酸菌乳饮品 on the phone (the screenshot crop reads it minus the faded 活) · how long a full reading takes on the Xiaomi now that up to forty passes run on up to three workers (v209; the bar names the step after 20 s).
Settled at v189: H's diagnostics saying "SW no" (2026-09-04, at v169 and again at v188 with a desktop user agent) come from Chrome's "Desktop site" mode on the app's tab — no worker, no cache, every vendor file from github.io; the installed app from the home screen has the worker. Settled at v165: the Xiaomi speaks Chinese through its system engine while Chrome's voice list stays empty — the speaker button never depends on that list ("Sound works now"). · time the offline model on the Xiaomi (download, first load, memory) · tune `OCR_DOUBT` from field data · M4 field polish per `SPEC-sign-cards.md` (phrasebook growth from misses, tone verification pass) · GLM browser calls unverified (Qwen verified in v173, VPN off). Later candidates: stroke-order animations. Measured and dropped: the stroke medians as a shape check on the photo's characters (after v142, see the field lessons — it broke more than it fixed). (HSK tagging was built in v45 and removed again in v46 on H's request.)

## To-do (H's reminders, 2026-09-06 — not started; each one is described first and built on "Go")
1. ~~Multi-language UI and translations~~ — built v253–v258 (see "The app's language" under Current state): six languages, all five PRs done (v253–v258: Learn, Cards, the Camera tab, More and the forms with the notices, the meanings from the AI with the "Translate all cards" button, the pill for a meaning in another language). Field check pending.
2. ~~A user guide ("Gebrauchsanleitung")~~ — built in v259 as More → Help → How to use the app (see Current state); field check pending.
3. ~~Consistency between the Crop and Edit screens~~ — done in v245 (see the Edit form): the side-by-side found five differences — the order (text first in Edit, photo first in the Camera tab), Cancel's place, the green AI box after a good answer, the missing source line under the meaning, and the Save row —, all five aligned to the Camera tab.

## Play Store launch (started 2026-09-03, H: closed test first, then sell)
The app goes to Google Play as a Trusted Web Activity — a thin Android wrapper (built with PWABuilder/Bubblewrap) that opens the live GitHub Pages URL full screen; the web app itself does not change. Facts established: H registers a **personal** Play Console account as a China resident (supported; Chinese ID for verification, $25 fee needs a Visa/Mastercard, whole flow over VPN); a personal account created now must pass a **closed test with ≥ 12 testers opted in for 14 continuous days** before production; Google Play does not distribute to users in mainland China at all; Chinese app stores (GetApps/AppGallery) would need a 软著 filing — out of scope.
- **Done:** `privacy.html` in the repo root (Play requires a privacy-policy URL; not in the SW shell, so no version bump; light/dark checked by screenshot). The manifest is TWA-ready (maskable icon, standalone, scope).
- **Decided rules:** the signing keystore never goes into the public repo. `assetlinks.json` must live at the **origin root** — `henglicam.github.io/.well-known/assetlinks.json`, i.e. a separate one-time repo `henglicam/henglicam.github.io` (project-path `.well-known` does not work for asset links); it gets the SHA-256 of the **Play App Signing** key, which exists only after the first upload — until then the TWA shows a browser bar, which is fine for testing.
- **Open decision (irreversible, before the Console listing is created):** a Play listing can go paid → free but **never free → paid**. Recommendation: create the app as **paid** from the start and add the 12 testers as license testers so they get it free; the alternative (free + in-app unlock) needs Play Billing inside the TWA (Digital Goods API) — real code. Known tension, accepted for v1: the web app at the public URL stays free (GitHub Pages, public repo — a hard constraint), so the paid Play app sells packaging and store presence, not exclusive access; true gating would be a re-architecture (hosting, licensing check).
- **Still to do:** H creates the Play Console account and confirms paid/free → TWA package via pwabuilder.com from H's phone (keeps the signing key on H's side, out of the repo) → first upload to a closed track → H pastes the Play App Signing SHA-256 → `assetlinks.json` content for the root repo → recruit 12 testers, 14 days → merchant account + price + store listing (screenshots, descriptions — Claude prepares texts) → production. A "Privacy policy" row in More (Play's User Data policy wants the link in-app too) is a UI change: described, waiting for go.



## Testing
No test files in the repo. A full audit (dead code, unused CSS, duplicated helpers, stale comments) was done at v81 and again at v151 (H: "a complete audit and polish" — found: an unused `.narrow` rule and two dead `.scriptref` rules, the reader's "Loading OCR" and "OCR failed" texts, five lowercase status badges; the identifier check against this file passed; layout checked by the twelve design screenshots); the mechanical scan for unreferenced functions, constants and CSS classes, and the check that every identifier named here exists in the code, are worth repeating after larger changes. **The third audit at v202** (H: "complete audit and polish") found: a dead variable in the second look (`frac`), a stale comment block on the selection bar removed in v91, four status texts not in sentence case ("failed: …" twice in More, "translated N cards", the study badge "testing from the list"), a stale CSS comment placing the picture mark under the meaning, the `.croprect` rule split in two, and the error-body snippet of the AI transports written three times (now `apiErrText`); no unused CSS class or id, every identifier named here exists (the names left over are the edge functions' and the SQL's), the twelve design screenshots in light and dark showed nothing to fix. **The fourth audit at v213** (H: "complete audit and polish — anything we can optimize?") found: four stale comments (the AI section's head still said "never photos" and "doubtful OCR", the owner's rows still listed the usage report and not All users, Feedback and the AI setup, a thumbnail comment said the whole photo where the code takes the crop, the `CROP` shape lacked the angle, the proposal and the zoom), the privacy page's date behind its feedback bullet, and nothing unreferenced (the audit script's two hits, `altCharsAt` and `vocabNames`, are used through a spread and a destructuring); the Cards list's thumbnails load lazily since. **The fifth audit at v233** (H: "Optimize and polish", after the design review's stretch v222–v232) found: the text box's CSS default still the 260 px square of v82–v222, overridden inline on every card since v223 (now the wide box's size), the whole-photo image rule without a note that it serves the `.full` view alone since v224, and the design line naming the version label as a `--mono` use; nothing unreferenced (the audit script's one hit is the `$` query helper), every identifier named here exists, the eighteen design screenshots in light and dark showed nothing to fix. **The sixth audit at v243** (H: "a complete audit and polish, also for consistency", after the Save now and Crop again stretch v237–v242) found: the abandon-a-reading steps written three times (the inbox's Cancel, the Edit form's `endRecrop`, the edit over a pending reading — now `abandonReading`), two comments still placing Save now "beside the bar" and the resume path without the copied photo and the Edit form's hand-off, the "Still at:" of a stuck step capitalised mid-sentence since v237 (lowercase again after "Reading the text …", capitalised only after the saved card's own sentence), a failed re-crop leaving its frame on a card with text, and the edit guard of v237 dropping a re-crop's pending reading on any save — even one that only touched pinyin — where only a changed text should; nothing unreferenced (the audit script's one hit is the `$` query helper), every identifier named here exists, the eighteen design screenshots in light and dark showed nothing to fix, the recrop, savenow, english, lock and edit suites pass. **The seventh audit at v260** (H: "Audit und Polish", after the language stretch v253–v259) found: a dead helper (`modelsText`, replaced by `workLines` in v252), a stale CSS rule (`#e-aibox`, the green box that went in v245), two stale names here (`PIC_SYSTEM` and `PIC_MARK` became `picSystem()` and `picMark()` in v254–v256), and **fourteen local variables named `t`** in functions that also call `t()` — arrow parameters and locals in `renderAiRow`, the Edit form's `pinyinFollow`, `cropSign`, `openDrawSheet`'s status, `wireSlines`' pinyin and meaning inputs, `signTranslate`, the Cards list's and the Tags field's tag maps — none of them wrong today (each shadowed `t` only inside its own arrow), all renamed so no later edit trips over them; the audit script counts `lang.js` since, the identifier check reads it too (the names it lists are the harness's suites and the workflow's), no unused CSS class, the eighteen design screenshots in light and dark showed nothing to fix; the tags suite read the Add form's fields right after `S.custom` grew and raced the clearing — it waits for the ok line since. **The eighth audit at v270** (H: "Make a final check and polish", after the improvement list v266–v269) found: the shared card's blurred fill drawn through the canvas filter, which Safari does not have — on the iPhone an unblurred enlarged copy would have shown behind the crop (now the fill is skipped where `filter` is missing, and a WebKit without `roundRect` gets square corners); a code comment naming the notice sheet "idea 4", the number of a different list than the dropped card history; and two stale sentences here still calling the plain notices browser alerts (the sheet since v266). The audit script finds nothing unreferenced (its one hit is the `$` query helper), the identifier check lists only the harness's suites and the names this file records as removed, the static key check reports no missing key, the eighteen design screenshots in light and dark showed nothing to fix; the share, ask, english, diag, guide, cards, edit, detail, translate, providers and picture suites pass. Measured again at 300 cards with the blurred thumbnails: boot 162 ms, the Cards list 53 ms, a search keystroke 7 ms, 62 frames a second after a scroll — the blur costs nothing measurable (the perf probe reads the whole photo as optional since v214). **Measured for the optimisation question** (headless Chromium, 390 px, cards with a 20 KB crop and a 60 KB whole photo each): boot to the first card 185 ms with 300 cards and 270 ms with 1 000, the Cards list renders in 56 ms and 150 ms, a search keystroke 6 ms and 26 ms, the study screen under 2 ms, the inbox with 30 photos 15–24 ms, 60 frames a second after a scroll to the end — the code is not the bottleneck at H's 70 cards, nor at ten times that; the one real saving was the whole photo stored twice, built as v214 on "Go" (see Deck format). Suite `verify-photostore` (v214: a card with the duplicate drops it at boot while its inbox photo exists, one whose photo is gone keeps it, a card with only `shot` is untouched, `fullPhoto` finds all three, the front hint offers the photo, the export file carries the whole photo from the inbox as base64, the inbox's Delete and Photos → Delete N copy the photo onto the card first, Remove image leaves no crop, no copy and no `shot`); the english and sign-card suites check that a saved card carries no second copy; the share-app suite expects the text without a link and the url beside it (v215), the picture suite the About line naming Qwen (v216–v217), the feedback suite compares the posted version with `APP_V` read from app.js instead of a fixed number and the Feedback row was checked by screenshot in light and dark, empty and typed (v218); the users suite expects the v250 layout (five mocked rows — two with cards, one that tried the reader, a Windows looker and a WeChat looker — the head's two tables with the counts, the analyses one per line, the first user's whole block, the order, the tried heading with its block and no cards line, the folded lookers, and every line but the device's under 46 characters; the report checked by screenshot in light and dark; since v252 the suite expects the plain-word layout — the Phones table with its three-line note, the totals with the sub-rows and the work lines with their units, the first phone's whole block, the tried heading with "used in the browser" and no cards line, every line but the device's and the title within 40 characters —, the feedback suite the blocks with "app version 212" and "from phone <id>", the admin suite the usage report's "app opened 1 time (", "cards reviewed 1 (", "from photos 0 (", "typed by hand 1 (", "checks 1 (" and "tokens sent and received 410 + 95", the picture suite its "photos checked by the AI 1 (1 this month)" and the work lines); **the design review's stretch (v222–v232)**: suite `verify-ask` (until v267 the detail's Delete opened the sheet with its title, line, neutral Cancel and red Delete, Cancel, the backdrop and Escape kept the card, Delete removed it — since v268 the Undo line, see above; the import's sheet with the tint Import button and the card imported after it, Reset's sheet, and no browser dialog anywhere — the import's notice is the sheet since v266; the cards, edit, export, photostore, export-with-photos and dupe suites override `askSheet`), suite `verify-box` (cards of 1, 2, 3, 4 and 9 characters all get the same 322×150 text box at 390 px with the font 72/72/72/58/32, a two-line word card the same width and a taller box, nothing overflows in light or dark), suite `verify-picbox` (a flat crop, a tall crop and a sign card all get a 322×181 photo box with the picture fitted inside and the blurred fill behind it, the whole-photo tap leaves the box and a second tap returns, the Learn front uses the same box, light and dark; at v228 it measured the edge-to-edge box at 358×201 and went back with the revert), suite `verify-hint` (a fresh phone shows the three hints, at 19 reviews still, at 20 none on Learn or in the detail, a reload keeps them away), the header check (the english, shell and mirror suites read the hidden label by `textContent`; a header screenshot shows the title and the capsules alone, About names the version), the traditional suite's detail check moved to the pill on the front and the simplified line on the back, and the Cards list's thumbnails by screenshot with a flat, a tall and a 16:9 crop beside a glyph tile; every step also ran the english suite, the front steps the sign-card, link, wrap, flag, detail and photostore suites. suite `verify-recrop` (v239: a card without a whole photo has no Crop again, the photo card has it beside Remove image, the tap opens the frame layer over the whole photo with the inbox untouched, no Save now in the reading box, a frame over the photo's lower line reads 合同条件 into the strip, the pinyin and the gloss, one AI call for it, the view closed with the new crop shown, Save changes stores the new crop and the text with `shot` kept, Image only takes the crop alone with the text unchanged, Cancel keeps everything, and a tab tap during a frame ends it cleanly; the flow was checked by screenshot — the field, the open photo, the frame with the preview, the reading, the filled form; the edit and english suites pass; at v240 the after-read screenshot in light and dark shows the bar under the Meaning field and the form scrolled to its top; since v241 it expects the proposed frame with its preview right after the tap and drops it before framing by hand (`dropRecropProposal`), then Save changes while the proposed frame is being read — the card with the new crop and `reading` at once, the detail with "Reading …" and its line, the fill with both lines as a sign card, one AI call, the detail re-rendered with the new text — and a reload with a seeded card whose inbox photo is gone, read again from the copied photo; since v242 also that the list's thumbnail made during the reading is replaced by the fill; rewritten at v244 for the stored frame: the saved card carries `frame`, Crop again lays it on the photo with the preview and no reading, Save with it untouched changes nothing, a frame by hand reads into the form while the photo, the frame and the result under it stay, Save stores crop, text and the new frame without a hand-off, Cancel after a reading restores the text and the crop, Image only keeps the view, the hand-off during a reading, the fill with `frame` from the saved rect, and a card without a stored frame: its crop found in the photo and stored (v246 — the seeded copy's tightened cut on the left half of the test photo, ~48 % wide), the search's bar, and a crop from elsewhere getting the proposal; since v247 also a seeded card with a small frame around 合同: Crop again opens the window (a PNG record of 333 × 156 px, the frame mapped into it, the badge naming the whole photo), a tap outside the frame shows the whole photo with the frame at the seeded place and the other badge, a second tap the window again, Read now in the window reads 合同 with one AI call, the tap after the reading keeps the result under the photo, Save stores the frame as photo fractions, and Read now then Save at once hands off with the reading rect on the photo's pixels (lw 640) and the record back to the whole photo, the fill landing with the same frame; the window and the whole-photo view checked by screenshot in light and dark; the savenow, english, edit, zoom and sign-card suites pass; the stored frame and the result under the photo checked by screenshot in light and dark; the proposed frame and the handed-off detail checked by screenshot), suite `verify-savenow` (v237: the button under the bar before any preview, the card at once with crop, frame and empty text, the note under the photo and no preview while the shot is pending, the Cards row with "Reading …" and the crop, Learn's queue without it, the card filled by the reading and the mocked DeepSeek with one AI call, `reading` gone, the row and the note afterwards, then a reload with two seeded waiting cards — one with its photo and frame, one whose crop is blank — the first filled, the second failed and flagged with "Nothing read yet" in the list, no textless card sent to the AI, and the Edit form giving the failed card a typed text with `reading` gone; since v238 it expects the box's "Nothing could be read." and the row's "Nothing read", and the detail of a waiting and a failed card, the reading box with Save now on the bar's line, the green line after the tap with Delete alone, the note after the fill, and the Edit form of a waiting card before and after its first typed line were checked by screenshot in light and dark — the bar in the box, no Test button, no speaker, no empty strip; the english, edit, multiremove, charpick, photostore, cards, detail, link, picbox and box suites pass; the insert suite dies on a stale picker handle on the v236 code too, a harness problem), suite `verify-warm` (v235: nothing loads on the front, the reveal starts the dictionary and pinyin-pro fetches, the first part tap answers without the loading line, the detail warms too; the flag, detail and english suites pass). **The cards suite taps `#reveal .glyph` since v232, the ai suite since v245** — a click at the front's centre lands in the photo box since v224 and toggles the whole photo instead of revealing (the ai suite's 供应商 carries a picture and had timed out on `.aibox` since v224, unnoticed; the export, flag and say suites still click `#reveal`, their seeded cards carry no picture); the busy and ai suites wait for the Edit form's AI bar to go instead of the removed AI box, the savenow suite expects the flag with its note on the filled card (v245); suite `verify-wechat` (v219: a WeChat user agent gets the line on the card, the done screen and the empty deck and not on Cards, its row's device ends in "; WeChat", a card made and the page hidden posts one more row and clears the flag, hiding again or returning posts nothing, a plain browser gets neither the line nor the word). The design-shots script had gone stale at v118 (its seed card had no `id`) and was repaired at v213; the twelve screenshots in light and dark showed nothing to fix. The harness itself: the two-pass reader suite had failed since v191 because the relay is on by default and the mocked Supabase host answered the preview's AI check with an empty body, so an error line stood in the reading box — it now switches the relay off like the AI suites; the sign, fullsign and handles suites still clicked a Read button that went in v47 (`data-cropocr`) and were dropped (at v203 the ocr, ocr2, ocr3 and sign-real suites went the same way), their ground is covered by the two-pass, lock, zoom and rotate suites; the empty-deck suite's only error is the sandbox's proxy refusing a host the seed does not mock. Left as they are on purpose: the AI request transport exists twice (`aiAsk` with the richer error mapping, `aiCharAlternatives` without) and the share-sheet dance twice (`shareFlagged`, `exportData` with different fallbacks) — merging them would change error texts. The old front-image suite (`verify-front-img`, v86) dies in its `page.evaluate` with "Resulting promise was garbage collected" before any check runs — seen at v181 and again on the reverted v183 code, so a harness flake around its data-URL fetch of the big sign, not the app; the front image is covered by the english and sign-card suites. The pending-picture suite (`verify-pendingimg`, v188) fails since v214 on its one check that the saved card carries the whole photo — the app stores it once now; its other checks still pass (seen at v237). The old offline-translation test (`verify-nmt`) fails on "model present but off" since an earlier version — the app's translate and pending paths still pass inside it; the fake-model seed no longer matches how the cache is checked. Each session verifies in headless Chromium (Playwright with the pre-installed browser, a local static server under `/zeichentrainer/`, the OCR/dictionary/pinyin files from `vendor/`, AI endpoints mocked with `page.route`): a seed script fills IndexedDB before the app loads, then scripts drive the UI and check state (`S`, `SIGN`, `DICT` are globals). Scripts live in the session scratchpad and are gone afterwards — rebuild them when needed; the header check `PWA vN` has to follow every bump. The set at v163 (the v132 set plus the suites of this stretch): the flow (photo → crop → read → flag → save, plus an Edit form left by a tab tap), the two-pass reader on ten images (a decorated composite, a 12° sign, 业主直租 in two polarities, a digits line, a two-line sign, and four regions cut from H's screenshots at the frame's coordinates — the Yakult logo twice, the granite sign and the escalator sign; the door plate 推 is read on its own; the cuts were made with PIL from the uploaded screenshots, 14 px inside the frame, and are gone with the scratchpad), the picker, the picker's add and remove (+ before / + after, no + tile, the red Remove, the Edit form's links) and the preview's line input (typing 拉 redraws the strip), the drawing sheet, the Edit form, the card detail and list, the AI review with a mocked endpoint (a good answer, a "bad" answer, a Chinese-only meaning, a traditional `zh`), two readings of the same photo started 400 ms apart (one result, one AI request), the locked frame (from the finger's release the layer allows vertical panning while the frame does not, a stroke outside the frame changes nothing before, during and after a reading, a corner drag adjusts and re-reads, Cancel during a reading leaves no result, and after Cancel a new frame can be drawn), the dictionary consensus with the phone's own candidate list, the traditional form end to end, the same text saved from two photos with the v2→v3 migration and an old export, the linked cards (the row on the back and in the detail, the tap, the list pill), the tags (typed in the Add form, toggled by chip in the Edit form and the preview, the Cards chip and pills, the search, the Learn chip with the persisted setting, the export), Diagnostics, the mirror, the vendor fallback and the shell self-repair, and since v143: the kept AI answer on a clearly read character, the traditional switch both ways with its saves, the front's wrap at 390 and 412 px, the card order, the Untagged group with the one-line chip rows and the chips at the top edge, the Add form's dictionary and AI fill and a real drawing of 团 through the draw button, the usage counters, the report text and the password gate, the share target with two screenshots posted the way Android sends them (and, since v168, that the preview offers Cancel and no Done link), the speaker with a mocked engine in its four states, the export with photos and the import sentence with H's real 19.5 MB file imported once by hand (68 crops, 60 whole photos, 5.6 s), and the crop zoom (a tap outside the frame shows the cut alone with the handles hidden, a tap brings the photo back with the frame in place, a swipe still scrolls, Cancel clears both), the turning frame (v185: the handle dragged to 13°, the frame's own coordinates kept on a corner drag and a move, the upright cut of the 12° sign at −13° with deskew 0 and both lines read, the enlarged view as the cut, Cancel and a fresh frame at 0°; the lock and zoom suites still pass), the all-users report (v197–v199: the row only unlocked, Show fetches with the password and the publishable key and renders the totals and the lines, Share sends the text, a wrong-password 401 and a JWT 401 show their own messages and both land in the error log, Lock drops the password and the row), the switch over the picture (v193: with the automatic check off no picture goes out), the relay (v191: a phone without keys is on through the relay, the text check, the picker's candidates and the picture go to the relay URL with the key headers, the installation id, the provider name and the OpenAI-style body, a 429 shows the limit message, the relay switched off leaves the AI off, a phone with its own key goes direct and the relay is not called; the AI, providers and picture suites switch the relay off to test the phone's own keys), the AI setup behind the password (v190: the admin suite sees the switch on both sides and the Set up button only unlocked; the AI, providers and picture suites unlock before opening the form), the silent origin (v189: the vendor-mirror suite with an origin that never answers — the reader download completes in under ten seconds, one origin attempt, the rest from the mirror), the pending picture (v188: a photo read and saved, then + New shows no image and the typed card carries none, Image only from a frame still brings the crop), the background retry (v201: a mocked DeepSeek whose first call dies while the page is hidden — no error, the bar stays, one call — and the retry answers when the page is back; a call that dies in the foreground is repeated after 1.5 s; two dead calls show the sentence with the Ask AI button and the detail in the AI log; the Edit form's sentence; the browser's own console line for an aborted request is filtered), the feedback (v212: the row under Share the app, an empty box, a message posted with the key headers, the id and the version and the box emptied, offline with the text kept, a 404 named, the owner's row after the unlock fetching with what:"feedback" and rendering two messages, Share as a file, Lock dropping the row and the cache), the multi-select removal (v204–v205: Select, a stroke along the first line, down to the second and along it marks five, a stroke from a marked character unmarks two, the strips carry touch-action none while selecting and not after Done, three characters marked across two lines with one unmarked again, Remove 3 rewrites both lines and asks the AI once, a whole line marked and removed drops the line with its confidences, everything marked leaves Remove disabled, Done clears the marks and the picker opens again, the Edit form's Remove 2 rewrites the card's text field), the proposed frame (v203: H's poster through the camera input — the frame as the whole photo, the preview, the reading by itself; Cancel and Crop with no proposal; a single 有 on red with a padded box widened to 16:9 that a corner drag adjusts and that reads 有; the hand-framing suites drop the proposal after the photo lands, since a stroke over an existing frame scrolls instead of drawing), the busy bar (v200: a DeepSeek mock that answers after 2.5 s — the bar with its text under the Meaning field in the Read preview with no head line and no Ask AI button, in the Add form and in the Edit form, gone with an empty source line once the answer is in; the AI, Add-form, Edit, kept and bad suites pass), the pinyin check (v187: a broken syllable in the picture answer replaced by the app's pinyin in the preview and on the saved card; the AI, Add-form and picture suites pass), the picture path (v173–v175: H's 奈斯餐吧 crop goes to a mocked Qwen with a JPEG of 800 px, the thinking switch off and the reader's guesses in the text, a frame whose second look tightened still sends the whole frame and keeps the framed crop as the card image, the answer replaces the reading with open characters and no boxes, the mark on the Characters label and nothing under the meaning, the saved card verified with the reader's texts as alternatives, the log without base64, no text check; the two-line sign sends nothing; the switch off, DeepSeek alone, a bad answer that flags, Claude's image block, GLM's picture model, the form's switch on the Qwen chip only and the row, About and privacy wording), the provider accounts (v172: a v171 phone's single key migrated into its account and the old settings gone, the chip row, a tap on a keyless chip that shows its form without switching, a Qwen key saved beside the DeepSeek key and used with its own endpoint, the one-tap switch back, Remove key on one provider with the fallback to the other, and the AI off once no key is left), and the usage sharing (v170: the row posted at boot with the key headers and the field set, no card text in it, no second row the same day on foreground or reconnect, the switch off and on with the persisted setting and the status texts, a 500 that keeps the day open and the retry on `online`, no attempt offline, the Share report button and the About line without the password — the shared seed mocks the Supabase host for every suite, since the app now posts at boot and the sandbox's proxy refuses the host with a tunnel error); the suites that use the owner's rows unlock first (`S.admin=true`). Two of them (mirror, shell) only pass when not run under heavy parallel load, and the suites must run from the scratchpad — run from the repo they write their logs into it (four stray `verify-*.log` files were committed by mistake at v159–v161 and removed at v163; `.gitignore` keeps them out). The mirror suite was found stale at v151 and rebuilt: its fake mirror must carry a label above the current version (it still said v99) **and** a matching `APP_V` in its app.js copy, or the shell check repairs the page back from the origin; the wait for the self-reload polls across the navigation, and the "Up to date" match is case-insensitive since the sentence-case pass. Layout is checked by screenshot at 390×844; anything derived from the reader's geometry is checked against the image itself (v69: a contact sheet of all character crops of the test sign, straight and tilted, exposed the drifting symbol boxes).

## Working with H — the how-to rules (v128, agreed after the four-corner episode)
**What H sends.** One request per message: a sentence, and a screenshot when it is about a screen. When the reader is wrong, More → Diagnostics → Share with it — the phone is the only place the reader can be watched from. "Leave it as it was" means: revert to the previous behaviour, no discussion.

**What Claude does with a request.**
1. **Restate before building.** One sentence: what changes for H on the phone. Two readings that lead to different work → one question, not five. Otherwise no questions.
2. **Size gate.** Wording, layout, a rule in the reader, a bug: build at once. **Anything that changes how H handles the app** (a new gesture, a new field or control, a new screen, a changed flow) is described first in three lines — what H does, what H sees, what it costs — and waits for "go". A "go" is for that description; if the built thing feels different, H says so and it is reverted in the same session.
3. **Only the ask.** No "while I'm at it" changes. Consistency passes, cleanups and refactors only when H asks for them.
4. **Field first.** A reader change is judged by the phone, not by the harness. Every claim about the phone is either seen on the phone (screenshot, diagnostics) or marked "not yet field-checked".
5. **One request, one PR, one version.** Every PR: the three version markers bumped, the suites that touch the change run, CLAUDE.md updated in the same PR, merged by Claude, branch reset onto main. A revert is a PR like any other, with its own version.
6. **CLAUDE.md is the memory.** Decisions, rejected features and field lessons go in with the reason; "Zieh CLAUDE.md nach" means consolidate into the current state, not a version log. What H rejected is not brought back unasked.
7. **Report short, in English.** What changed, what was verified and where, what is open. Honesty over confidence: "I don't know" beats a guess; limits are named.
8. **Wording and design.** Sentence case, plain words, no jargon (no "OCR"), no "·" shorthand; iOS-style tokens; pinyin in the UI font; the rules in Design and Hard constraints are not up for interpretation.
9. **Tone (H, v255): relaxed.** "This is a fun app. No formal, boring translations within the app. Customers are passionate language learners. Same for the design and layout." — every language addresses the learner as a friend (du, tu, tú, 해요체), no formal register, no stiff notices; design and layout stay light the same way.
- Build-first. Physical reality beats spec.
- On every change: confirm the app still runs with no network dependencies and relative paths.
