# CLAUDE.md — 识字 Zeichentrainer

Working language: **English.** Reply to H in English. Short, direct, no excessive politeness.

## What this is
Chinese character trainer for adults (spaced repetition), a reinterpretation of 悟空识字 without the kids' aesthetic.
User: H, product manager, Beijing, **phone-only (Android/Xiaomi, Chrome, VPN), no computer**.
UI language: English. Learning content: Chinese + pinyin + English meaning.

## Live deployment
- Repo: `henglicam/zeichentrainer` · GitHub Pages, branch `main`, folder `/ (root)`.
- URL: `https://henglicam.github.io/zeichentrainer/`
- Push to `main` → Pages rebuilds automatically (~1–2 min). `index.html` must stay in the repo root.
- The site is **public** (free plan). User data lives exclusively on the device (IndexedDB), never in the repo.

## Current state (PWA v75, 2026-09-02)
- **Files:** `index.html` · `styles.css` · `app.js` · `manifest.webmanifest` · `sw.js` · `signs.json` (phrasebook) · `nmt-model.json` (translation-model manifest) · `icon-192/512/maskable-512.png` · `vendor/` (Tesseract + dictionaries ~12 MB, `vendor/nmt/` Bergamot engine 5 MB + zh→en model 50 MB, licenses in `vendor/LICENSES.txt`) · `.github/workflows/fetch-nmt-model.yml` · `SPEC-sign-cards.md`. Vanilla JS, no frameworks, no build step.
- **PWA:** installed and verified on the Xiaomi; runs offline (SW cache-first); `navigator.storage.persist()` granted. **Versioning:** SW cache `zt-vN` in `sw.js` and the header label `PWA vN` in `index.html` move in lockstep — bump both on **every** change to cached files. The app updates itself (update check on load/foreground, auto-reload on `controllerchange`); the worker's install fetches the shell with `cache:"reload"` — GitHub Pages sends `max-age=600`, and after two deploys within ten minutes a new worker once filled its cache with the previous shell (the phone showed the v70 page with the v69 script: header v70, no drag in the sheet). **Boot self-check** (`shellCheck`): `APP_V` in `app.js` must equal the `PWA vN` label — **bump both, plus `zt-vN`, on every change**; if they differ, the page posts `refresh` to the worker, which refills its cache from the server (`cache:"reload"`) and the page reloads once (guard: at most once per 10 min, setting `shellFixAt`); it runs after registration and on the first `controllerchange`; the vendor cache `zt-ocr-v1` survives shell updates — bump it only when vendor files change.
- **No VPN needed to use the app (H's requirement):** the installed app runs from its cache, DeepSeek is reachable from China, and updates also arrive through a **mirror** — More → Updates without a VPN (setting `mirror`, default jsDelivr `cdn.jsdelivr.net/gh/henglicam/zeichentrainer@main/`, **field-verified reachable from H's phone without VPN**). The page posts `mirror-update` (with its own version label) to the worker on start/foreground (at most hourly) or on "Check now"; the worker reads `PWA vN` from the mirror's index.html and, if newer, fetches the shell assets into the current cache — content types by extension, because jsDelivr hands HTML out as text/plain — and the page reloads (`mirrorUpdate` in `sw.js`, `mirrorCheck` in `app.js`). **Vendor files come from the mirror too** (H: "text recognition does not work without VPN" — the reader's ~12 MB are fetched on first use, and github.io was unreachable): the worker tries the origin, and on failure or a non-OK answer fetches `MIRROR + vendor/…` (all OCR/dictionary files are under jsDelivr's 20 MB cap; types by extension), caching the result in `zt-ocr-v1`; the page tells the worker the mirror setting on start (`tellMirror`). More → **Text recognition** shows how many of the seven reader files are on the phone and offers **Download** (`OCR_FILES`, `renderOcrRow`). The worker script itself only ever comes from github.io (it just carries the cache name). Still VPN-only: a first install if github.io is blocked, the 50 MB model download (jsDelivr caps files at 20 MB), the Claude provider.
- **Persistence:** IndexedDB `zeichentrainer` v2 — `progress` (keyPath `c`), `custom` (keyPath `c`, all cards), `inbox` (keyPath `id`, photos), `settings` (keyPath `k`). No built-in deck; `deck()` is `S.custom`, sorted by creation time `at` (oldest first, so cards are learned in the order they were taken; the Cards list shows newest first). At boot, progress rows without a card are pruned.
- **Tabs:** Learn · Cards · Camera · More. A tab tap always leaves the edit form; tapping **Cards** while a card is open returns to the list.

### The flow (AI-first — "nobody wants to see OCR if not necessary")
1. **Camera:** `<input type="file" capture="environment">` (Take photo) or `<input type="file" multiple>` (**From album** — several at once, all land in the inbox, the first opens in crop mode; `addPhoto` per file) → the photo is normalized (EXIF baked in, max 1600 px) and stored in `inbox`. A pulsing "Processing photo …" placeholder appears at once; the inbox re-renders on `visibilitychange`; pull-to-refresh is off (`overscroll-behavior-y: contain`).
2. **Frame:** the photo lands directly in crop mode (bright dashed frame with a dark outline, nothing dimmed, corner handles resize, dragging inside moves, preview below). **Reading starts by itself 1.2 s after the finger lifts** (`READ_TIMER`; a corner drag restarts the wait; "Read now" and a small "Image only" link remain). No dialogs in the flow: no frame → nothing happens, a duplicate text is an inline note. **The frame stays on the photo** through reading and the card preview (`cropRectStyle` re-draws it from `CROP.rect` in percentages after any re-render) and goes only on Save, Cancel or Split into words; a corner drag meanwhile re-reads.
3. **Read** (`cropSign`): the framed area is **straightened first** (`deskewBlob`: projection-profile skew estimate on a ≤ 360 px copy, ±20° in 1° steps then half degrees, text pixels = far from the median brightness so both polarities count; below 1.5° nothing happens; the rotated crop becomes the card image; H: a tilted 首都铁骑 read as garbage) → on-device OCR line by line (Tesseract.js 7, `chi_sim`, PSM 6, symbols under confidence 35 dropped; `readPass`) — the status text lives in `READING[id]` and is re-queried on every update, so a re-render (foreground return, AI answer for another photo) cannot swallow it or the error — → second look and competing readings (below) → `SIGN[id]` = `{lines, orig, conf, boxes, img, angle, tightened, region}` (`img` = the straightened, maybe tightened crop the text was read from, `boxes[k][i]` = each symbol's box in it; `region.passes` = the competition and `region.tightBlob` the tight crop, for diagnosis) → editable text, one input per line with a **character strip** above it (see below), headed by **"Read from this area"** with that crop (`.readarea`, H: "the wrong section is read") and a red hint when only 1–2 characters were found with low confidence (frame tightly, drag a corner) → **automatic AI check** (`signAskAI`, when the AI is live) → the preview shows "Read and checked by the AI" with pinyin, meaning and the AI's note → **Save card**. One line of ≤ 4 characters becomes a word card (`t:"Custom"`, `seg`, crop image), anything longer a sign card (`kind:"sign"`, lines joined with `\n`, `segs`, `gloss`, `img` + `imgFull`). Cards checked here are stored `mt:{src:"llm",verified:true}`.
   - **Second look** (v73, H: a Maotai label in a loose frame with stripes, ribbon and neck label was read as one false character from the ribbon): when the first pass found text in under 60 % of the frame or is unsure (mean confidence < 92), that text area is cut out — its vertical band from the boxes (heights are reliable) plus half a height, its **horizontal ends from the image** (`textRowExtent`: within the band, walk the columns outward from the reader's span until a gap wider than 1.3 text heights or the edge; the boxes' ends drift and the first pass may have lost a character altogether — 骑 was cut in half in v73 and gone in v74) plus one text height, then 1.5 text heights of plain margin in the crop's **median colour** (the reader wants margins, real ones bring the clutter back, and a corner-sampled red ribbon once framed a white label in red) — straightened on its own and read at four character heights (45/60/75/90 px, `scales`) plus native while cheap; the lines of all these readings are **merged by vertical band** (each reading tends to get one line right and lose another; the most confident line per band is kept, short low-confidence strays dropped) and the merged result competes with the single readings; when a tightened reading wins, the **card image** is the same area with a real photo margin of one text height, rotated by the same angle (`deskewBlob(blob, forced)`), not the padded reading crop. **Readings compete** (`score` = mean confidence × n^0.35 × fragment penalty for many 1–2-character lines × (0.75 + 0.5 × dictionary word coverage)), because the model's output swings with scale even on a clean crop (measured: the same image perfect at 0.7×, garbage at 1×). Measured on a striped, ribboned composite: garbage at 86 % → 本区域禁止违规 at 99 %; the 12° sign still reads both lines. Not yet timed on the Xiaomi.
   - **What the reader sees must be a JPEG** (`READ_JPEG` 0.95): this Tesseract build misreads large canvas PNGs and WebPs (same pixels: JPEG → the full line at 99 %, PNG → one character); intermediate crops stay PNG so only the last step is lossy — two JPEG generations in a row lost the line too. `deskewBlob` always hands back a JPEG; `jpegOf` re-encodes card images to JPEG 0.85 at save. The pad's small black-on-white images are indifferent (measured).
   - **Fixing one character** (H: "sometimes one character is wrong while the others are right"): every character of a line is a button (`charStripHTML`, low-confidence ones outlined in VERM). Tap one → picker (`openCharPick`) under the line, **two levels: suggestions, then drawing** (H: "you are mixing typing and drawing" — no type field; keyboard edits go into the line input below the strip). Any change drops the AI check and re-checks the line automatically.
     - **The character as it looks in the photo** heads the picker and the drawing sheet, so the original stays visible while choosing or drawing (H: the keyboard's handwriting pad hid it). `drawCharRef` crops `sg.img` at `charBox(sg,k,i)` — **not** at Tesseract's symbol box: those drift along a Chinese line (measured on the test sign: from the third character on, a box covers the right half of one character and the left half of the next; H saw a blank patch). The line's overall extent and the box heights are right, so the span from the first x0 to the last x1 is split evenly among the characters (signs are monospaced; a line mixing Latin or digits will be less precise) and the size comes from the median box height.
     - **Level 1, candidates:** while the AI is live it is asked at once for that position (`aiCharAlternatives`, a small separate text request; jade buttons first); the dictionary adds characters that form a word with the neighbours (`charCandidates`: 2–4-character words fitting around the position, longer context first, then by how common the character is, `CHARFREQ`); or **Remove** it.
     - **Level 2, Not here? Draw it** (when the right one is missing): a **full-screen drawing sheet** (`openDrawSheet`, `.drawsheet` fixed over everything, `body.noscroll`) with **two equal squares** (H: "symmetrical, same size as the drawing field"): the photo character on top (`attachRefView`: a 600 px square view of `sg.img` that starts at `charBox` × 1.5 and is **moved with one finger and zoomed with two** — or the mouse wheel —, photo edges padded in INK; H: "why can't I just drag the image into position and enlarge it?" — the automatic crop is a guess, the hand corrects it) and the pad below (faint 田字格, pointer events); `fitRef` gives both one side = min(sheet width, half the height left after heading, status, result row and buttons), ≥ 160 px (~295 px on a 844 px viewport), refit on resize; Undo/Clear and a red **Done** at the bottom.
     - **Reading the strokes:** **nothing is read until Done is tapped** — no timer (H: an inline pad below the fold went unseen, and reading after every stroke "takes it without me confirming"). On Done the strokes are rendered black on white at ~80 px and read by the same Tesseract model (`recognizeStrokes`: the model gives no per-symbol alternatives, so three readings are combined — single-word PSM 8 reads hand strokes far better than single-character PSM 10 — ranked by confidence, then what fits the neighbours first); the results appear as big buttons under the pad; tapping one replaces the character and closes the sheet; Clear to draw again. Nothing downloaded, offline, nothing leaves the device; a print-trained model on handwriting, so clean strokes matter — field accuracy unknown.
4. **Meaning priority** (H's rule): online AI → phrasebook → offline model → word gloss. `aiLive()` = key set + auto on + `navigator.onLine`. While the AI is live the offline model is not run and Save waits for the running AI check (`sg.aiPromise`); if the call fails, `sg.aiErr` is set and the offline model takes over; with neither, the card gets the gloss and `mt.pending:true` and is completed by the post-save auto run when online.
5. **Split into words** (secondary): drops back to the overlay path (`onOcr` with the remembered `region`): pinyin boxes over the characters (hidden while the AI is live unless `SHOWBOX[id]`; "Pick words on the photo" brings them back), lines segmented into CC-CEDICT words (`segmentChars`, greedy longest match), tap selects a whole word, selection bar with a **phrase row** (whole string = one card, `seg` keeps word boundaries and a `"\n"` token where the photo breaks the line) and one row per word, each with Save / Edit… / **Ask AI** ("Ask again" once checked; every selection is checked automatically, debounced 1.2 s, cached in `AIFIX[id][text]`). Word cards from here: `mt:{src:"dict",verified:false}` plus `mt.suspect:"<reason>"` when the reading is uncertain (`ocrDoubt`: mean symbol confidence < 70 %, unknown words, no meaning) or `src:"llm",verified:true` after the AI check.
6. **After saving:** the note under the photo says what was saved and where the meaning came from, with a **Next photo** button that opens the camera straight away. Photos picked from the album that are still waiting keep their CROP button below. `QSCARD[id]` remembers the card; if the AI answers later, the jade suggestion box with **Accept / Dismiss** appears right there.

### Online AI review (T3, opt-in, text only)
- **Setup:** More → Online AI review → **provider** (`AI_PROVIDERS`: DeepSeek default — **field-verified from the phone, no VPN** —, Qwen/Alibaba Bailian, GLM/Zhipu, Claude, custom OpenAI-style base URL; the Chinese ones take WeChat Pay/Alipay, H could not buy Anthropic credit from China), API key (settings store, device only, never exported), model (defaults `deepseek-chat`, `qwen-plus`, `glm-4-flash`, `claude-sonnet-5`), checkbox "Check every new card with the AI automatically" (default on). Settings: `aiProvider`, `aiBase`, `aiKey`, `aiModel`, `aiAuto`.
- **Request:** OpenAI-style `POST {base}/chat/completions` with `Authorization: Bearer` (system + user message, temperature 0), or `api.anthropic.com/v1/messages` for Claude (`anthropic-dangerous-direct-browser-access`). Payload per card: hanzi, pinyin, meaning, note, why (flagged / uncertain reading / pending), gloss — **never photos**. Reply = JSON array `[{c,zh,p,m,note,ok}]`. Cost ≈ 400 input + 100 output tokens per card, well under ¥0.01 at DeepSeek; a hundred cards a day ≈ ¥0.10–0.20.
- **Where it acts:** in the Read preview and the selection bar before saving (result shown in jade, Save stores it verified); after saving as a suggestion stored `ai:{zh,p,m,note,ok,at,model}` with **Accept** (applies pinyin/meaning, renames the card if the text was fixed via `applyCardUpdate`, clears flag and `suspect`, `mt.src:"llm",verified:true`) or **Dismiss** — on the study back, in the card detail, under the photo, and via the **"N AI suggestions waiting — Accept all"** bar on the Cards tab; in the **Edit form** ("Ask AI to check text, pinyin and meaning" fills the fields in place, stored on Save). **Ask AI about N cards** in More sends the whole queue (`aiQueue()` = flagged + uncertain + pending) in one request; the auto run (`aiAuto`, `aiAutoSoon` 1.5 s after a save, and on `online`) does the same for pending/uncertain cards.

### Offline translation (T2, opt-in)
Firefox Translations `zh-Hans→en` v2.0a2 (Mozilla, MPL-2.0; model 44 MB + shortlist 4.7 MB + vocab 0.7 MB gzipped = 50 MB) in the Bergamot WASM engine (`vendor/nmt/translator-worker.js` + `bergamot-translator-worker.{js,wasm}`, npm `@browsermt/bergamot-translator` 0.4.9, no SharedArrayBuffer). Fetched by the **GitHub Action "Fetch zh→en translation model"** (workflow_dispatch) from **Mozilla Remote Settings** (the source Firefox uses; the Git LFS objects on GitHub are gone, 410; the build sandbox cannot reach Mozilla) — it writes `nmt-model.json` (sizes, sha256), bumps both version markers and pushes branch `nmt-model`; Actions may not open PRs here, so the PR is opened by hand. Enable under More → Translation (size prompt; files cached in `zt-ocr-v1`), setting `nmt`; used **only without a connection** once the AI is set up. `signMeaning(lines)` → `{m, src: phrasebook|nmt|gloss, pending}`; "Translate pending" in More completes gloss-only cards. Verified on the build machine (~40 ms per sentence after load), **not yet timed on the Xiaomi** — the More row shows "loaded in N s, last translation N s" (`NMT.loadMs`, `NMT.lastMs`) for that field test.

### Learning and the library
- **Learn:** SM-2 light; progress rows are `{c, interval, ease, due, reps, fails, last}` (`last` = day of the last review); every review day is appended to setting `days` (max 400) for the **streak**; `learnStats()` → cards learned / reviewed this week / streak, shown on the All-clear screen and in More → Progress — `fails` counts consecutive "again" (hard keeps it, good/easy reset it) and at `LEECH_FAILS` = 4 the card is **flagged automatically** ("failed N times in a row — check text, meaning and photo"): a leech is usually a bad card, not a bad memory. Front = photo (crop) above the text box (see **Line breaks** and Design). No tag row, no Reveal button — **tapping the character toggles the back on and off; tapping the photo swaps the crop for the whole photo and back** (`S.fullPic`, `frontPic`, reset on the next card). Back = pinyin with a **speaker button** (the phone's own zh-CN voice via `speechSynthesis`, nothing downloaded, shown only when such a voice exists — `ttsVoice()`, voices arrive asynchronously on Android), meaning, a **parts row** — the card's dictionary words as buttons (from `gloss`/`segs`/`seg`; a single-word card shows its characters instead), tap one for pinyin and meaning, a multi-character word then offers its characters as a sub-row (`cardParts`/`charsHTML`/`charInfo`, dictionary loaded on first tap, offline; CC-CEDICT clutter like `[Tian1 jin1 shi4]` and `CL:` stripped by `cleanSense`). No word or gloss tables on the back (H: redundant) —, (context word), then **Again / Hard / Good / Easy** with interval preview (after grading the next card appears at the top of the screen), and underneath **⚑ Flag for review** and **✎ Edit** (`S.editFrom="study"`, Back/Save return to the revealed card). No example sentences (H's decision). Empty deck → "No cards yet" + Take a photo.
- **Line breaks — the card text follows the photo, automatically (H's rule):** the front never wraps on its own; up to 3 characters sit in the square reticle, longer words get a wide box and the font is fitted to the longest line (`frontLines`, `frontBox`). Where the lines come from: the reader keeps Tesseract's lines (sign cards: `c` with `\n`; word cards: a `"\n"` token in `seg`, chars carry `ln`); the AI's answer is re-cut to those lines when the model drops the `\n` (`recutLines`: old break offsets, so a corrected character keeps them and an added one only lengthens the last line); a rename keeps them the same way (`segWithBreaks`); the Edit form shows word cards as a textarea with one line per photo line. Cards without any line data (before v39) get their lines **estimated once at boot** from the crop's shape (`autoBreaks`: L ≈ √(n·h/w), cut at word boundaries, stored with `lb:"auto"`); cards with real line data are `lb:"photo"` and never touched.
- **Cards:** every card newest first with a 104×58 thumbnail of the **whole photo** (`imgFull`, fallback inbox photo, else the crop; `object-fit: contain`) or a glyph tile, pinyin, meaning, ⚑ review / jade AI pill and the real SRS status (`due`, `in N d`); search over hanzi, pinyin, meanings, context words and flag notes; chips **⚑ Flagged (N)** (flag only), **AI (N)** (waiting suggestions, shown only when there are any) and **Unverified (N)**; **+ New** opens the Add form. Detail: front + back preview, SRS stats, **Test this card** (studies that card and then continues down the list, newest first; ← Cards stops and restores the session queue), **Edit** (pinyin, meaning, context word, example, image removal, **and the Chinese text itself** — progress, images, queue entries move to the new key via `applyCardUpdate`; pinyin/segmentation/gloss recomputed unless pinyin was edited by hand; editing marks `verified`, clears `pending` and `suspect`), **⚑ Flag / Clear flag**, **Delete**.
- **Review flag:** `flag:true` / `flagNote` on the card; included in export. More → **Flagged cards → Show** opens the Cards list with the Flagged filter on; **Share** sends a plain-text list via the share sheet (`.txt`) for a teacher.
- **Export/import** (More): progress + cards as JSON, `zeichentrainer-YYYY-MM-DD.json.txt` via the share sheet; import validates by content and upserts, keeping local images. The Export row shows **"Last export: N days ago"** (setting `lastExport`), red after 30 days or when never exported — the cards exist only on this phone. **Photos** row: inbox count, and a **Delete N** button for photos older than 30 days that already became a card (cards carry `shot:<inbox id>`; the cards keep their own crop). **Reset** wipes cards, progress and photos.

### Field lessons that shaped the UI (keep)
No tab switching for the AI (v31/v32) · button always visible, never replaced by the auto result (v32) · one action after framing (v39) · plain words instead of jargon and `·` shorthand (v25/v39) · three top-bar pills overflow at 390 px, so Deck only shows off the Learn tab (v25) · Chrome/Android does not repaint after the camera until touched (v34) · the card text keeps the photo's lines, a horizontal word is one line (v42) · Android shares `.txt` but not `.json` (v3) · a cached 404 poisoned the dictionary once (SW caches only OK responses) · status text written into a DOM node captured before a re-render disappears silently — keep status in state and re-query (v47) · reading starts by itself after the frame is released, no Read tap (v47) · github.io is unreliable from China, jsDelivr is not (v48/v49) · the crop frame stays on the photo until Save (v50) · after grading the next card must start at the top of the screen (v53) · two taps on the front: photo = whole picture, character = answer (v55) · the static HTML must already match the first render (Deck pill hidden in index.html), otherwise the top bar jumps at load (v57) · `html` needs the INK background, `color-scheme: dark` and an inline fallback in index.html, otherwise a white line flashes above the page at load (v58) · the reader's files must be reachable without the VPN too, hence the mirror fallback for `vendor/` (v56) · a tilted frame is straightened before reading — Tesseract fails beyond a few degrees (v59) · one misread character is fixed by tapping it, not by retyping the line; the AI is asked first, the dictionary is the fallback (v61) · candidates first, drawing as the second level when the right character is missing; the photo character stays in view while drawing, because the system keyboard's handwriting pad hid it (v62) · the Tesseract worker has **one** logger: progress goes to `_ocrLog`, the handler of the running job, never to the status closure of whoever created the worker — the pad's reading once wrote "recognizing … 100%" into the photo's box and wiped the editor (v63) · action rows in the picker are flex (`.ckacts`), not the auto-fit grid, which broke the pad's buttons (v63) · the picker is two levels only, suggestions then drawing; no type field in it (v64) · the drawing pad is a full-screen sheet with a Done button — an inline pad below the fold went unseen, and reading after each stroke felt like being cut off (v65) · Tesseract's per-symbol boxes are not to be trusted for Chinese, only the line extent and the heights (v69) · an automatic crop is a guess — let the hand correct it (drag/pinch on the photo square) (v70) · a new worker must fetch the shell past the browser's HTTP cache, or rapid deploys leave the old app under a new cache name (v71) · the script carries its own version and checks it against the page's label at boot, so a mixed shell repairs itself (v72) · **what the reader sees must be a JPEG** — this Tesseract build misreads large canvas PNGs and WebPs (same pixels: JPEG 0.95 → the full line at 99 %, PNG → one character); intermediates stay PNG so only the last step is lossy — two JPEG generations in a row lost the line too (`READ_JPEG`, `deskewBlob` always hands back a JPEG, `jpegOf` re-encodes card images to JPEG 0.85 at save) · the reader is chaotic on large text: the same clean crop reads perfectly at one scale and as garbage at another, so read at several scales and let the readings compete (v73) · small black-on-white pad images are indifferent to PNG/JPEG (measured) (v73).

## Hard constraints (learned in the field — do not violate)
1. **No external dependencies / CDNs.** Must run offline and behind the GFW. System CJK fonts only. Libraries and data go into `vendor/` (licenses in `vendor/LICENSES.txt`).
2. **All paths relative** (`./…`). The app lives under a subpath (`/zeichentrainer/`).
3. **Persistence only via IndexedDB** (do not use localStorage/sessionStorage). `navigator.storage.persist()` is requested — Xiaomi/MIUI aggressively evicts storage of non-installed sites. The SW must **never cache non-OK responses** — a 404 cached during a deploy window poisoned the dictionary permanently once; `loadDict` additionally heals such entries.
4. **Phone-only deploys:** H uses GitHub only in the phone browser. Ship changes as small, clearly described PRs. As few files as possible; generate/commit binaries (icons, vendor data) in the repo, never ask H to upload them.
5. **Privacy rule:** Confidential text (real contracts, suppliers, company internals) must **never** end up in the public deck or repo. General vocabulary only. Photos and card images stay on the device.
6. **Files leave the device only via the share sheet:** programmatic blob downloads (`<a download>`) are silently blocked by Android/MIUI. Use `navigator.share` with a file — and Chrome/Android only shares whitelisted types (`.txt`/`.csv`/images/PDF yes, **`.json` no**), hence the `.json.txt` extension with `text/plain`.

## Design (material language frozen; mobile treatment updated 2026-09-02 on H's request)
Material language: 漆器 (lacquerware) + seal red. Deliberately not the default AI look.
- Colors: INK `#141410` · PANEL `#1C1C16` · PANEL2 `#232319` · BONE `#EDE6D6` · MUTE `#8C8677` · FAINT `#57544A` · VERM `#B23A2E` · JADE `#7C9A86` · LINE `#2E2E24`
- Fonts: Hanzi = `'Songti SC','STSong','Noto Serif CJK SC','Source Han Serif SC','SimSun',serif` · body = system sans · labels/data = mono
- Signature element: the character sits in a **田字格 reticle** (260px square up to 3 characters, LINE border, 14px BONE corner ticks, dashed VERM crosshair **only for single characters**). Longer words get a **wide box** (viewport − 32 px, max 440) and the font shrinks so the longest photo line fits on ONE line — the text never wraps except where the photo does (`frontLines`/`frontBox`, v42, H: "the image is one line"). Base font by character count: 1→150px, 2→104, 3→74, 4–8→58, 9–12→44, 13+→34. Sign fronts use the same fit rule.
- **Readability (v25):** body 17px; labels/hints/list heads are sans 13px medium in `--text2 #B9B19E` (mono is reserved for pinyin and numbers, no more uppercase-mono captions); meanings on the card back 18px BONE; tab labels 12px sans; pills/chips rounded sans; all UI copy uses plain words and parentheses instead of `·` shorthand. Top bar shows Due/Done on Learn and Deck on the other tabs (three pills overflow at 390px).
- **Mobile treatment (v22):** sticky, blurred top bar (logo + `PWA vN` + stat pills, DUE/DONE only on Learn) · **bottom tab bar** in thumb reach with line icons (active icon in VERM) · radius 12px on cards/buttons/inputs, 10px on images · touch targets ≥ 44px (mini buttons 44, grades 58, round `ocr-btn` pills 38) · one-line hints instead of paragraphs · inputs 16px to avoid zoom · safe-area padding top/bottom · `-webkit-tap-highlight-color: transparent`, visible `:focus-visible` in JADE.
- The crop frame must stay visible on any photo color: bright dashed edge with a dark outline (red-on-red lesson); nothing is dimmed, inside or outside.

## Didactics / SRS
- Loop: photo + character/word → tap to reveal → pinyin (+ pronunciation), meaning, (context word). No example sentences (H's decision).
- SM-2 light, grades `again / hard / good / easy` with interval preview. `again` re-queues the card within the same session.
- Session = due cards + max. 8 new ones. "Pull forward" pulls future cards when nothing is due.

## Deck format
No built-in deck; all cards live in IndexedDB `custom`. Schema:
```
{ c:"坚果", p:"jiānguǒ", m:"nuts", t:"Custom", at:1756800000000,   // t: Custom | Sign · at: creation time
  seg:["坚果","\n","供应"],           // optional: word boundaries + photo line breaks for the front
  lb:"photo"|"auto",                    // where the line breaks came from (auto = estimated from the crop's shape)
  w:"…", wp:"…", wm:"…",              // optional: context word + pinyin + meaning
  shot:"shot_1756800000000",           // inbox photo the card came from (photo cleanup)
  mt:{ src:"llm"|"dict"|"phrasebook"|"nmt"|"gloss", verified:true|false, pending:false, suspect:"reason" },
  flag:true, flagNote:"…",            // review flag
  ai:{ zh, p, m, note, ok, at, model } // a waiting AI suggestion
}
```
Sign cards add `kind:"sign"`, `segs` (per line) and `gloss` (`[{w,p,m}]`, feeds the parts row); word cards from the Read/overlay path carry `gloss` too; any card saved from a photo carries `img` (crop) and `imgFull` (whole photo: list thumbnail and the front toggle — not repeated on the back, H) — blobs, local only, never exported.
Rules: **Pinyin only verified, with correct tones — never guess.** Meanings in English. Dictionary/phrasebook prefills are `verified:false` until a human or the AI checked them; when unsure, flag instead of inventing.
New words come from photos (menus, signs, packaging).

## Roadmap
Done and verified on the device: PWA install · offline operation · export/import · OCR v2 (crop-first, word segmentation, card images) · sign cards M1 · online AI review with DeepSeek (T3) · AI-first Read flow · updates through the jsDelivr mirror without VPN.
Built, not yet field-checked: vendor files through the mirror (does text recognition work without the VPN now?) · deskew of tilted frames (v59) · per-character correction (v61 — are the character buttons comfortable on long sign lines, where the strip wraps?) · the drawing sheet (v62–v70 — how often does the print model read a finger-drawn character correctly? if poorly, a small handwriting model in `vendor/` is the next step; is the automatic photo crop usually right, or does H drag it every time?) · the second look and the competing readings (v73 — how much slower on the Xiaomi? does the Maotai-style brush font on a bottle ever read? if not, that is a font/model limit, not a framing problem).
Open: does the Xiaomi have a zh-CN voice (speaker button)? · time the offline model on the Xiaomi (download, first load, memory) · tune `OCR_DOUBT` from field data · M4 field polish per `SPEC-sign-cards.md` (phrasebook growth from misses, tone verification pass) · Qwen/GLM browser calls unverified. Later candidates: stroke-order animations. (HSK tagging was built in v45 and removed again in v46 on H's request.)

## Testing
No test files in the repo. Each session verifies in headless Chromium (Playwright with the pre-installed browser, a local static server under `/zeichentrainer/`, the OCR/dictionary/pinyin files from `vendor/`, AI endpoints mocked with `page.route`): a seed script fills IndexedDB before the app loads, then scripts drive the UI and check state (`S`, `SIGN`, `DICT` are globals). Scripts live in the session scratchpad and are gone afterwards — rebuild them when needed; the header check `PWA vN` has to follow every bump. Layout is checked by screenshot at 390×844; anything derived from the reader's geometry is checked against the image itself (v69: a contact sheet of all character crops of the test sign, straight and tilted, exposed the drifting symbol boxes).

## Working with H
- Build-first. Physical reality beats spec.
- **Honesty over confidence:** better "I don't know" than a guess. Name limits openly.
- Small, verifiable steps. When unclear: one question, not five.
- On every change: confirm the app still runs with no network dependencies and relative paths, and bump all three version markers (`PWA vN` in index.html, `zt-vN` in sw.js, `APP_V` in app.js).
